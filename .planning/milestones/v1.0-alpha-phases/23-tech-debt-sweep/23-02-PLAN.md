---
phase: 23-tech-debt-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/settings/components/plugin-card.tsx
  - frontend/src/features/settings/components/plugin-detail-sheet.tsx
  - frontend/src/features/settings/components/__tests__/plugin-card.test.tsx
  - frontend/src/stores/ai/AISettingsStore.ts
  - frontend/src/features/settings/components/api-key-form.tsx
autonomous: true
requirements:
  - AIPR-05
  - code quality
must_haves:
  truths:
    - "Update Available badge renders in amber/orange color, not blue"
    - "AISettingsStore.validateKey accepts all 6 provider types"
    - "api-key-form validates keys for all supported providers"
  artifacts:
    - path: "frontend/src/features/settings/components/plugin-card.tsx"
      provides: "Amber-colored Update Available badge"
      contains: "amber-500"
    - path: "frontend/src/features/settings/components/plugin-detail-sheet.tsx"
      provides: "Amber-colored Update button"
      contains: "amber-500"
    - path: "frontend/src/stores/ai/AISettingsStore.ts"
      provides: "validateKey with all provider support"
      contains: "kimi"
  key_links:
    - from: "frontend/src/features/settings/components/api-key-form.tsx"
      to: "frontend/src/stores/ai/AISettingsStore.ts"
      via: "validateKey call"
      pattern: "validateKey"
---

<objective>
Frontend tech debt: fix Update Available badge color and extend validateKey for all providers

Purpose: Close AIPR-05 frontend gap (validateKey all providers) and UI spec compliance (amber badge color)
Output: Amber Update badges, validateKey supporting anthropic/openai/google/kimi/glm/custom
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-tech-debt-sweep/23-RESEARCH.md

@frontend/src/features/settings/components/plugin-card.tsx
@frontend/src/features/settings/components/plugin-detail-sheet.tsx
@frontend/src/stores/ai/AISettingsStore.ts
@frontend/src/features/settings/components/api-key-form.tsx
</context>

<interfaces>
<!-- Key types the executor needs -->

From frontend/src/stores/ai/AISettingsStore.ts (line 180):
```typescript
validateKey(provider: 'anthropic' | 'openai', key: string): boolean {
    if (key.length < 10) return false;
    switch (provider) {
      case 'anthropic': return key.startsWith('sk-ant-');
      case 'openai': return key.startsWith('sk-');
      default: return false;
    }
}
```

From frontend/src/features/settings/components/api-key-form.tsx (line 31):
```typescript
const validateKey = (provider: 'anthropic' | 'openai', key: string): string | undefined => {
```

Callers of validateKey:
- api-key-form.tsx: calls with 'anthropic' and 'openai' hardcoded
- ai-settings-page.test.tsx: mocks validateKey
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Update Available badge color blue -> amber</name>
  <files>
    frontend/src/features/settings/components/plugin-card.tsx,
    frontend/src/features/settings/components/plugin-detail-sheet.tsx,
    frontend/src/features/settings/components/__tests__/plugin-card.test.tsx
  </files>
  <behavior>
    - Plugin card with hasUpdate=true renders badge with amber-500 classes (not blue-500)
    - Plugin detail sheet with hasUpdate=true renders button with amber-500 classes (not blue-500)
    - Existing plugin-card tests still pass with updated color assertions
  </behavior>
  <action>
    1. In `plugin-card.tsx` (line 70), replace the Update badge classes:
       - FROM: `border-blue-500/20 bg-blue-500/10 text-blue-400`
       - TO: `border-amber-500/20 bg-amber-500/10 text-amber-400`

    2. In `plugin-detail-sheet.tsx` (line 58), replace the Update button classes:
       - FROM: `border-blue-500/30 text-blue-400 hover:bg-blue-500/10`
       - TO: `border-amber-500/30 text-amber-400 hover:bg-amber-500/10`

    3. Check `plugin-card.test.tsx` for any assertions on blue color classes. Update to amber if found. If tests only check for `data-testid="badge-update"` presence (not class values), no test change needed.

    4. Write a test (or extend existing) that verifies the badge renders with the correct amber classes when `hasUpdate=true`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/features/settings/components/__tests__/plugin-card.test.tsx</automated>
  </verify>
  <done>
    - No blue-500 classes remain in plugin-card.tsx or plugin-detail-sheet.tsx for update indicators
    - amber-500 classes used for Update badge and button
    - All plugin-card tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend AISettingsStore.validateKey for all provider types</name>
  <files>
    frontend/src/stores/ai/AISettingsStore.ts,
    frontend/src/features/settings/components/api-key-form.tsx,
    frontend/src/features/settings/pages/__tests__/ai-settings-page.test.tsx
  </files>
  <behavior>
    - validateKey('anthropic', 'sk-ant-xxx...') returns true
    - validateKey('openai', 'sk-xxx...') returns true
    - validateKey('google', 'AIzaXXX...') returns true (prefix check)
    - validateKey('kimi', 'any-long-key') returns true (length check only)
    - validateKey('glm', 'any-long-key') returns true (length check only)
    - validateKey('custom', 'any-long-key') returns true (length check only)
    - validateKey('any-provider', 'short') returns false (under 10 chars)
  </behavior>
  <action>
    1. In `AISettingsStore.ts`, update `validateKey` signature and implementation:
       ```typescript
       validateKey(provider: string, key: string): boolean {
           if (key.length < 10) return false;
           switch (provider) {
             case 'anthropic': return key.startsWith('sk-ant-');
             case 'openai': return key.startsWith('sk-');
             case 'google': return key.startsWith('AIza');
             case 'kimi':
             case 'glm':
             case 'custom':
               return true; // No known prefix constraint, length check sufficient
             default:
               return true; // Unknown providers pass with length check only
           }
       }
       ```

    2. In `api-key-form.tsx`, update the local `validateKey` function's type annotation:
       - FROM: `provider: 'anthropic' | 'openai'`
       - TO: `provider: string`
       This aligns with the store's expanded signature. The function currently only validates anthropic and openai keys inline, which is fine -- it delegates to the store for format validation.

    3. Update `ai-settings-page.test.tsx` mock if it constrains the provider type union. The mock at line 57 returns `true` unconditionally so it likely needs no change, but verify the type assertion matches.

    4. Run frontend quality gates.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/features/settings/pages/__tests__/ai-settings-page.test.tsx && pnpm type-check</automated>
  </verify>
  <done>
    - validateKey accepts all 6 provider types (anthropic, openai, google, kimi, glm, custom)
    - Type signature uses string instead of narrow union
    - api-key-form.tsx type-checks with updated signature
    - All frontend tests pass, tsc --noEmit clean
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm test -- --run src/features/settings/components/__tests__/plugin-card.test.tsx src/features/settings/pages/__tests__/ai-settings-page.test.tsx` -- all tests pass
2. `cd frontend && pnpm type-check` -- no type errors
3. `grep -r "blue-500" frontend/src/features/settings/components/plugin-card.tsx frontend/src/features/settings/components/plugin-detail-sheet.tsx` -- no blue-500 in update indicators
4. `make quality-gates-frontend` -- full frontend quality gates green
</verification>

<success_criteria>
- Update Available badge and button use amber-500 (not blue-500) classes
- AISettingsStore.validateKey handles all 6 LLMProvider types
- api-key-form.tsx type signature updated
- All frontend tests pass, type-check clean, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/23-tech-debt-sweep/23-02-SUMMARY.md`
</output>
