---
phase: 23-tech-debt-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/pilot_space/api/v1/routers/ai_configuration.py
  - backend/src/pilot_space/api/v1/routers/ai_chat.py
  - backend/src/pilot_space/api/v1/routers/_chat_schemas.py
  - backend/src/pilot_space/api/v1/schemas/mcp_server.py
  - backend/tests/unit/routers/test_ai_configuration.py
autonomous: true
requirements:
  - AIPR-05
  - code quality
must_haves:
  truths:
    - "kimi/glm/custom providers can be tested via the AI configuration test endpoint"
    - "Dead schemas/mcp_server.py file no longer exists in codebase"
    - "ai_chat.py is under 700 lines with no functional change"
  artifacts:
    - path: "backend/src/pilot_space/api/v1/routers/ai_configuration.py"
      provides: "_test_openai_compatible_key helper + updated _test_provider_api_key dispatcher"
      contains: "_test_openai_compatible_key"
    - path: "backend/src/pilot_space/api/v1/routers/_chat_schemas.py"
      provides: "Extracted Pydantic schemas from ai_chat.py"
      exports: ["ChatContext", "ChatRequest"]
  key_links:
    - from: "backend/src/pilot_space/api/v1/routers/ai_configuration.py"
      to: "openai.AsyncOpenAI"
      via: "_test_openai_compatible_key(api_key, base_url)"
      pattern: "AsyncOpenAI.*base_url"
    - from: "backend/src/pilot_space/api/v1/routers/ai_chat.py"
      to: "backend/src/pilot_space/api/v1/routers/_chat_schemas.py"
      via: "import extracted schemas"
      pattern: "from.*_chat_schemas import"
---

<objective>
Backend tech debt: extend provider key testing, remove dead code, refactor ai_chat.py

Purpose: Close AIPR-05 backend gap (kimi/glm/custom key testing) and 3 code quality items (dead file, line count, soft-delete verification)
Output: Updated ai_configuration.py with full provider coverage, deleted mcp_server.py, ai_chat.py under 700 lines
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-tech-debt-sweep/23-RESEARCH.md

@backend/src/pilot_space/api/v1/routers/ai_configuration.py
@backend/src/pilot_space/api/v1/routers/ai_chat.py
@backend/src/pilot_space/api/v1/schemas/mcp_server.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _test_provider_api_key for kimi/glm/custom + remove dead schema file</name>
  <files>
    backend/src/pilot_space/api/v1/routers/ai_configuration.py,
    backend/src/pilot_space/api/v1/schemas/mcp_server.py,
    backend/tests/unit/routers/test_ai_configuration.py
  </files>
  <behavior>
    - _test_provider_api_key(LLMProvider.KIMI, "valid-key", "https://api.moonshot.cn/v1") calls _test_openai_compatible_key
    - _test_provider_api_key(LLMProvider.GLM, "valid-key", "https://open.bigmodel.cn/api/paas/v4") calls _test_openai_compatible_key
    - _test_provider_api_key(LLMProvider.CUSTOM, "valid-key", "https://custom.api/v1") calls _test_openai_compatible_key
    - _test_openai_compatible_key creates openai.AsyncOpenAI(api_key=key, base_url=base_url) and calls models.list()
    - AuthenticationError returns (False, "Invalid API key")
    - RateLimitError returns (True, "API key is valid (rate limited)")
    - schemas/mcp_server.py file does not exist after deletion
  </behavior>
  <action>
    1. Add `_test_openai_compatible_key(api_key: str, base_url: str) -> tuple[bool, str]` function after `_test_google_key`. Pattern: identical to `_test_openai_key` but with `base_url` parameter passed to `openai.AsyncOpenAI(api_key=api_key, base_url=base_url)`.

    2. Update `_test_provider_api_key` signature to accept optional `base_url: str | None = None`:
       ```python
       async def _test_provider_api_key(
           provider: LLMProvider, api_key: str, base_url: str | None = None
       ) -> tuple[bool, str]:
       ```

    3. Add cases for KIMI, GLM, CUSTOM in the dispatcher:
       - KIMI: use `base_url or "https://api.moonshot.cn/v1"`
       - GLM: use `base_url or "https://open.bigmodel.cn/api/paas/v4"`
       - CUSTOM: require base_url, return error if None
       All three call `_test_openai_compatible_key(api_key, resolved_base_url)`.

    4. Update the caller at line 517 to pass `config.base_url`:
       `success, message = await _test_provider_api_key(config.provider, api_key, config.base_url)`

    5. Write tests in `test_ai_configuration.py`: mock `openai.AsyncOpenAI` to test each new provider path (auth error, success, rate limit).

    6. Delete `backend/src/pilot_space/api/v1/schemas/mcp_server.py`. Verify no imports reference it:
       - Check `backend/src/pilot_space/api/v1/schemas/__init__.py` for barrel exports
       - Remove any references found

    7. Verify `ai_configuration.py` stays under 700 lines (currently 637, adding ~30 lines = ~667).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/routers/test_ai_configuration.py -x -q && test ! -f src/pilot_space/api/v1/schemas/mcp_server.py</automated>
  </verify>
  <done>
    - kimi/glm/custom providers return valid test results through _test_provider_api_key
    - All new test cases pass (auth error, success, rate limit for each provider)
    - schemas/mcp_server.py deleted with no broken imports
    - ai_configuration.py under 700 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract ai_chat.py schemas to _chat_schemas.py + verify related issues</name>
  <files>
    backend/src/pilot_space/api/v1/routers/ai_chat.py,
    backend/src/pilot_space/api/v1/routers/_chat_schemas.py
  </files>
  <action>
    1. Create `backend/src/pilot_space/api/v1/routers/_chat_schemas.py` with all Pydantic schema classes currently in `ai_chat.py`:
       - `ChatContext` (line 43)
       - `ChatRequest` (line 62)
       - `AbortRequest` (line 537)
       - `AbortResponse` (line 543)
       - `SkillListItem` (line 593)
       - `SkillListResponse` (line 601)
       - `AgentListItem` (line 634)
       - `AgentListResponse` (line 641)

       These schemas depend only on:
       - `from pilot_space.api.v1.schemas.base import BaseSchema`
       - `from pilot_space.ai.sdk.question_adapter import Question`
       - `from pilot_space.ai.session.session_manager import AIMessage`
       - `from pilot_space.api.v1.routers.ai_chat_model_routing import ModelOverride`
       - Standard library: `uuid.UUID`, `typing.Any`, `pydantic.Field`

       Move the imports these schemas need into the new file. Do NOT import from ai_chat.py (no circular deps).

    2. In `ai_chat.py`, replace schema class definitions with:
       ```python
       from pilot_space.api.v1.routers._chat_schemas import (
           AbortRequest,
           AbortResponse,
           AgentListItem,
           AgentListResponse,
           ChatContext,
           ChatRequest,
           SkillListItem,
           SkillListResponse,
       )
       ```
       Remove the now-unused imports that were only needed by the schemas (e.g., `AIMessage` if only used in schema type hints).

    3. Verify ai_chat.py line count is under 700 lines with `wc -l`.

    4. **Verify related issues soft-delete (Tasks 3+4 from roadmap):** Run `cd backend && uv run pytest tests/api/test_related_issues.py -x -q` to confirm existing tests pass. Per research, Task 3 (`item["issue_id"]` stale ref) appears already correct (`item["id"]` matches schema), and Task 4 (soft-delete behavior) is already correct per docstring and BaseRepository.delete() default. Document findings in SUMMARY.

    5. Run full backend quality gates to confirm no regressions.
  </action>
  <verify>
    <automated>cd backend && wc -l src/pilot_space/api/v1/routers/ai_chat.py | awk '{if ($1 < 700) print "PASS: "$1" lines"; else {print "FAIL: "$1" lines"; exit 1}}' && uv run pytest tests/api/test_related_issues.py -x -q && uv run pyright src/pilot_space/api/v1/routers/ai_chat.py src/pilot_space/api/v1/routers/_chat_schemas.py</automated>
  </verify>
  <done>
    - ai_chat.py is under 700 lines
    - _chat_schemas.py contains all extracted schemas with correct imports
    - No circular imports between ai_chat.py and _chat_schemas.py
    - pyright passes on both files
    - Related issues tests pass (confirming Tasks 3+4 are non-issues)
  </done>
</task>

</tasks>

<verification>
1. `cd backend && uv run pytest tests/unit/routers/test_ai_configuration.py tests/api/test_related_issues.py -x -q` -- all tests pass
2. `test ! -f backend/src/pilot_space/api/v1/schemas/mcp_server.py` -- dead file removed
3. `wc -l backend/src/pilot_space/api/v1/routers/ai_chat.py` -- under 700 lines
4. `make quality-gates-backend` -- full backend quality gates green
</verification>

<success_criteria>
- _test_provider_api_key handles all 6 LLMProvider enum values (anthropic, openai, google, kimi, glm, custom)
- Dead schemas/mcp_server.py removed
- ai_chat.py under 700 lines with schemas extracted to _chat_schemas.py
- All backend tests pass, pyright clean, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/23-tech-debt-sweep/23-01-SUMMARY.md`
</output>
