---
phase: 17-skill-action-buttons
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - frontend/src/services/api/skill-action-buttons.ts
  - frontend/src/features/settings/components/action-buttons-tab-content.tsx
  - frontend/src/features/settings/pages/skills-settings-page.tsx
  - frontend/src/features/issues/components/action-button-bar.tsx
  - frontend/src/features/issues/components/index.ts
  - frontend/src/app/(workspace)/[workspaceSlug]/issues/[issueId]/page.tsx
  - frontend/src/features/settings/components/__tests__/action-buttons-tab-content.test.tsx
  - frontend/src/features/issues/components/__tests__/action-button-bar.test.tsx
  - frontend/src/services/api/__tests__/skill-action-buttons.test.ts
autonomous: true
requirements:
  - SKBTN-01
  - SKBTN-02
  - SKBTN-03
  - SKBTN-04

must_haves:
  truths:
    - "Admin can add, edit, reorder, toggle, and delete action buttons from Settings > Skills > Action Buttons tab"
    - "Issue detail page shows active action buttons in a horizontal bar below the header"
    - "Max 3 buttons visible; overflow goes into a More dropdown"
    - "Clicking a button starts a new chat session with issue context and bound skill/tool activated"
    - "Action bar is hidden when no buttons are defined"
    - "Inactive buttons are visible (greyed) in admin config but hidden on issue page"
  artifacts:
    - path: "frontend/src/services/api/skill-action-buttons.ts"
      provides: "API client + TanStack Query hooks for action buttons CRUD"
      exports: ["actionButtonsApi", "useActionButtons", "useAdminActionButtons", "useCreateActionButton", "useUpdateActionButton", "useReorderActionButtons", "useDeleteActionButton"]
    - path: "frontend/src/features/settings/components/action-buttons-tab-content.tsx"
      provides: "Admin configuration UI for action buttons"
      contains: "ActionButtonsTabContent"
    - path: "frontend/src/features/issues/components/action-button-bar.tsx"
      provides: "Horizontal action button bar on issue detail page"
      contains: "ActionButtonBar"
    - path: "frontend/src/app/(workspace)/[workspaceSlug]/issues/[issueId]/page.tsx"
      provides: "Issue detail page with action button bar wired"
      contains: "ActionButtonBar"
  key_links:
    - from: "action-button-bar.tsx"
      to: "PilotSpaceStore.ts"
      via: "clearConversation + setIssueContext + setActiveSkill + sendMessage"
      pattern: "clearConversation|setIssueContext|setActiveSkill|sendMessage"
    - from: "action-buttons-tab-content.tsx"
      to: "skill-action-buttons.ts"
      via: "TanStack Query hooks for CRUD"
      pattern: "useAdminActionButtons|useCreateActionButton"
    - from: "page.tsx"
      to: "action-button-bar.tsx"
      via: "component render with onButtonClick handler"
      pattern: "ActionButtonBar"
---

<objective>
Frontend UI for skill action buttons — API client with TanStack hooks, admin configuration tab in Settings Skills page, action button bar on issue detail page, and chat activation wiring.

Purpose: Gives workspace admins a UI to manage action buttons and gives all members one-click access to skills/MCP tools from the issue detail page.
Output: Working end-to-end flow: admin creates button in Settings, button appears on issue page, clicking it opens chat with context.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-skill-action-buttons/17-CONTEXT.md
@.planning/phases/17-skill-action-buttons/17-RESEARCH.md
@.planning/phases/17-skill-action-buttons/17-01-SUMMARY.md

@frontend/src/features/settings/pages/skills-settings-page.tsx
@frontend/src/features/settings/components/plugins-tab-content.tsx
@frontend/src/services/api/workspace-role-skills.ts
@frontend/src/services/api/workspace-plugins.ts
@frontend/src/app/(workspace)/[workspaceSlug]/issues/[issueId]/page.tsx
@frontend/src/stores/ai/PilotSpaceStore.ts
@frontend/src/features/ai/ChatView/ChatView.tsx

<interfaces>
<!-- Key types and contracts from Plan 01 backend that this plan consumes. -->

From backend schemas (Plan 01 output):
```typescript
// TypeScript equivalent of backend Pydantic schemas
interface SkillActionButton {
  id: string;
  name: string;
  icon: string | null;
  binding_type: 'skill' | 'mcp_tool';
  binding_id: string | null;
  binding_metadata: Record<string, unknown>;
  sort_order: number;
  is_active: boolean;
  created_at: string;
  updated_at: string;
}

interface SkillActionButtonCreate {
  name: string;
  icon?: string | null;
  binding_type: 'skill' | 'mcp_tool';
  binding_id?: string | null;
  binding_metadata?: Record<string, unknown>;
}

interface SkillActionButtonUpdate {
  name?: string;
  icon?: string | null;
  binding_type?: 'skill' | 'mcp_tool';
  binding_id?: string | null;
  binding_metadata?: Record<string, unknown>;
  sort_order?: number;
  is_active?: boolean;
}

interface SkillActionButtonReorder {
  button_ids: string[];
}
```

API Endpoints (from Plan 01):
- GET    /api/v1/workspaces/{slug}/action-buttons          → SkillActionButton[] (all members)
- GET    /api/v1/workspaces/{slug}/action-buttons/admin     → SkillActionButton[] (admin only)
- POST   /api/v1/workspaces/{slug}/action-buttons           → SkillActionButton (admin, 201)
- PATCH  /api/v1/workspaces/{slug}/action-buttons/{id}      → SkillActionButton (admin)
- PUT    /api/v1/workspaces/{slug}/action-buttons/reorder   → 204 (admin)
- DELETE /api/v1/workspaces/{slug}/action-buttons/{id}      → 204 (admin)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API client, TanStack hooks, and ActionButtonsTabContent admin UI</name>
  <files>
    frontend/src/services/api/skill-action-buttons.ts
    frontend/src/features/settings/components/action-buttons-tab-content.tsx
    frontend/src/features/settings/pages/skills-settings-page.tsx
    frontend/src/services/api/__tests__/skill-action-buttons.test.ts
    frontend/src/features/settings/components/__tests__/action-buttons-tab-content.test.tsx
  </files>
  <behavior>
    - actionButtonsApi.getButtons fetches from correct endpoint
    - actionButtonsApi.getAdminButtons fetches from /admin endpoint
    - useActionButtons returns query result with enabled guard on workspaceId
    - useCreateActionButton invalidates action-buttons query on success
    - ActionButtonsTabContent renders list of buttons with name, binding info, toggle, edit, delete
    - ActionButtonsTabContent shows "Add Button" form with name, icon (text input), binding type select, target select
    - Inactive buttons show greyed/disabled visual in admin list
  </behavior>
  <action>
    1. Create `skill-action-buttons.ts` API client following `workspace-role-skills.ts` pattern:
       - Define `SkillActionButton` interface matching backend response schema (id, name, icon, binding_type, binding_id, binding_metadata, sort_order, is_active, created_at, updated_at)
       - Define `SkillActionButtonCreate` and `SkillActionButtonUpdate` types
       - `actionButtonsApi` object with methods:
         - `getButtons(workspaceId: string)` — GET `/workspaces/${workspaceId}/action-buttons`
         - `getAdminButtons(workspaceId: string)` — GET `/workspaces/${workspaceId}/action-buttons/admin`
         - `createButton(workspaceId: string, data: SkillActionButtonCreate)` — POST
         - `updateButton(workspaceId: string, buttonId: string, data: SkillActionButtonUpdate)` — PATCH
         - `reorderButtons(workspaceId: string, buttonIds: string[])` — PUT `/reorder`
         - `deleteButton(workspaceId: string, buttonId: string)` — DELETE
       - Export query key constant: `ACTION_BUTTONS_KEY = 'action-buttons'`
       - TanStack hooks:
         - `useActionButtons(workspaceId)` — useQuery, enabled: !!workspaceId
         - `useAdminActionButtons(workspaceId)` — useQuery for admin endpoint
         - `useCreateActionButton(workspaceId)` — useMutation, invalidates ACTION_BUTTONS_KEY on success
         - `useUpdateActionButton(workspaceId)` — useMutation, invalidates on success
         - `useReorderActionButtons(workspaceId)` — useMutation, invalidates on success
         - `useDeleteActionButton(workspaceId)` — useMutation, invalidates on success

    2. Create `action-buttons-tab-content.tsx` as a separate `observer()` component (same extraction pattern as `PluginsTabContent` from Phase 19):
       - Props: `workspaceId: string`
       - Uses `useAdminActionButtons(workspaceId)` for data
       - Renders a list of button cards, each showing:
         - Name + icon (if set)
         - Binding type badge ("Skill" or "MCP Tool") + target name from binding_metadata
         - Toggle switch for is_active (calls useUpdateActionButton)
         - Edit button (opens inline edit form or dialog)
         - Delete button with confirmation (calls useDeleteActionButton)
         - Up/down arrow buttons for reordering (calls useReorderActionButtons) — per user discretion, use simple arrows not drag-and-drop
       - "Add Button" button at top opens a form (inline or Dialog):
         - Name input (required, max 100 chars)
         - Icon input (text field for Lucide icon name, e.g. "Zap", "Send") — per user discretion, use simple text input
         - Binding type select: "Skill" or "MCP Tool"
         - Target dropdown:
           - When "Skill": fetch workspace role skills (`useWorkspaceRoleSkills`) + installed plugins (`useWorkspacePlugins`) and list active ones
           - When "MCP Tool": fetch MCP servers and list as "ServerName / ToolName" (use existing MCP API if available, or show server-level binding)
         - Submit calls useCreateActionButton
       - Inactive buttons shown with reduced opacity and "Inactive" badge
       - Empty state: "No action buttons configured. Add buttons to give your team quick actions on issues."
       - Toast notifications on success/error (sonner)

    3. Add "Action Buttons" tab to `skills-settings-page.tsx`:
       - Add new `TabsTrigger` with value "action-buttons" and label "Action Buttons"
       - Add new `TabsContent` rendering `<ActionButtonsTabContent workspaceId={workspaceId} />`
       - Only show tab for admin users (same isAdmin check as Plugins tab)
       - Import ActionButtonsTabContent from the new component file
       - Keep changes minimal — just the tab trigger + content wrapper (all logic in extracted component)

    4. Create test files:
       - `skill-action-buttons.test.ts`: Test API client method URLs and TanStack hook query keys
       - `action-buttons-tab-content.test.tsx`: Test renders button list, shows add form, handles toggle

    Note: For the binding target dropdown, if MCP tool listing is not yet available as a frontend API, show MCP servers (from existing MCPServersStore) as binding targets at the server level. The binding_metadata can store `{ tool_name: "all" }` for server-level bindings — individual tool selection can be enhanced later if MCP tool discovery (MCP-07, v2) is implemented.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm test -- --run src/services/api/__tests__/skill-action-buttons.test.ts src/features/settings/components/__tests__/action-buttons-tab-content.test.tsx && pnpm type-check</automated>
  </verify>
  <done>
    - API client with 6 methods and query key exported
    - 6 TanStack hooks for CRUD + reorder
    - ActionButtonsTabContent renders admin config with add/edit/toggle/delete/reorder
    - "Action Buttons" tab visible in Settings Skills page for admins
    - Tests pass, type-check clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActionButtonBar on issue detail page with chat activation</name>
  <files>
    frontend/src/features/issues/components/action-button-bar.tsx
    frontend/src/features/issues/components/index.ts
    frontend/src/app/(workspace)/[workspaceSlug]/issues/[issueId]/page.tsx
    frontend/src/features/issues/components/__tests__/action-button-bar.test.tsx
  </files>
  <behavior>
    - ActionButtonBar renders nothing when no buttons
    - ActionButtonBar shows max 3 buttons with secondary/sm variant
    - ActionButtonBar shows "More" dropdown for overflow buttons (4th+)
    - Button with icon renders Lucide icon + text label
    - Button without icon renders text label only
    - Clicking button calls onButtonClick with the button object
    - Disabled button (stale binding) shows tooltip explaining why
    - Chat activation sequence: clearConversation -> setIssueContext -> setActiveSkill -> sendMessage -> open chat panel
  </behavior>
  <action>
    1. Create `action-button-bar.tsx` component:
       - NOT an observer — receives buttons as props (parent fetches via hook)
       - Props: `buttons: SkillActionButton[]`, `onButtonClick: (button: SkillActionButton) => void`
       - Return `null` if `buttons.length === 0` (no empty container per locked decision)
       - Render horizontal bar: `flex items-center gap-2 px-4 py-2 border-b`
       - First 3 buttons: shadcn/ui `Button` with `variant="secondary"` `size="sm"`
       - Each button: optional Lucide icon (lookup by name string from `lucide-react`) + text label
       - For icon rendering: create a small `DynamicLucideIcon` helper that imports from a curated map of ~20-30 common Lucide icons (Zap, Send, RefreshCw, GitPullRequest, Bug, Code, FileText, MessageSquare, Rocket, Settings, Shield, Star, Target, Wand2, etc.) with fallback to a generic `Sparkles` icon if name not found
       - Overflow (4th+ buttons): shadcn/ui `DropdownMenu` with `MoreHorizontal` trigger icon
       - Each overflow item: `DropdownMenuItem` with `onClick={() => onButtonClick(btn)}`
       - For buttons whose bound skill/tool no longer exists: check `binding_id` presence + `binding_metadata.skill_name` or `binding_metadata.tool_name` presence. If binding appears stale, render button as disabled with a Tooltip: "Bound skill/tool is no longer available"

    2. Export from `features/issues/components/index.ts` barrel.

    3. Wire into issue detail page (`page.tsx`):
       - Import `ActionButtonBar` and `useActionButtons` from API client
       - Inside the `IssueDetailPage` observer component, call `useActionButtons(workspaceId)` to fetch active buttons
       - Create `handleActionButtonClick` callback that orchestrates the chat activation sequence:
         ```
         1. const store = aiStore.pilotSpace
         2. store.clearConversation()           // Start fresh session
         3. store.setIssueContext({ issueId, projectId, title, description })
         4. const skillName = button.binding_metadata.skill_name || button.binding_metadata.tool_name || button.name
         5. store.setActiveSkill(skillName)
         6. void store.sendMessage(`Run ${button.name} on this issue`)
         7. Set chat panel open (setActiveRightPanelTab('chat') or equivalent)
         ```
       - Place `<ActionButtonBar>` between the issue header and the editor content area:
         - Find the `IssueNoteLayout` or equivalent layout wrapper
         - Insert ActionButtonBar after the header/title section, before `IssueEditorContent`
         - Pass `buttons={actionButtons ?? []}` and `onButtonClick={handleActionButtonClick}`
       - SKBTN-04 (approval gate): No new code needed — the existing DD-003 approval gate in PilotSpaceAgent fires automatically when the bound skill/tool triggers a destructive action. The chat activation just sends a prompt; the agent handles approval flow.

    4. Create `action-button-bar.test.tsx`:
       - Test renders nothing when buttons array is empty
       - Test renders up to 3 buttons with correct labels
       - Test renders "More" dropdown when >3 buttons
       - Test calls onButtonClick with correct button object on click
       - Test renders icon when button has icon property
       - Test renders disabled state for stale binding

    Note on page.tsx modification: The issue detail page is a large file. Add the ActionButtonBar import and hook call near the top of the component. Insert the `<ActionButtonBar>` JSX in the layout between header and editor. Minimize changes — do not refactor existing code.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm test -- --run src/features/issues/components/__tests__/action-button-bar.test.tsx && pnpm type-check && pnpm lint</automated>
  </verify>
  <done>
    - ActionButtonBar renders active buttons with correct layout (max 3 visible, overflow in dropdown)
    - Clicking a button starts a new chat session with issue context and bound skill activated
    - Action bar hidden when no buttons defined
    - Icons rendered from curated Lucide icon map
    - Stale bindings show disabled state with tooltip
    - Tests pass, type-check and lint clean
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm test -- --run src/services/api/__tests__/skill-action-buttons.test.ts src/features/settings/components/__tests__/action-buttons-tab-content.test.tsx src/features/issues/components/__tests__/action-button-bar.test.tsx && pnpm type-check && pnpm lint
```
</verification>

<success_criteria>
- Admin can manage action buttons from Settings > Skills > Action Buttons tab
- Issue detail page shows active buttons in horizontal bar below header
- Max 3 visible, overflow in "More" dropdown
- Clicking button starts new chat session with issue context + skill/tool activated
- Empty state: no action bar rendered
- All tests pass, lint and type-check clean
</success_criteria>

<output>
After completion, create `.planning/phases/17-skill-action-buttons/17-02-SUMMARY.md`
</output>
