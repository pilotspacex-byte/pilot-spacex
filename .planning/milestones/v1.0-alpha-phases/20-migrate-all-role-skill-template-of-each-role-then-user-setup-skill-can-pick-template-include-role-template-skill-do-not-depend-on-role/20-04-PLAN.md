---
phase: 20-skill-template-catalog
plan: 04
type: execute
wave: 3
depends_on: ["20-02", "20-03"]
files_modified:
  - frontend/src/services/api/skill-templates.ts
  - frontend/src/services/api/user-skills.ts
  - frontend/src/features/settings/components/template-catalog.tsx
  - frontend/src/features/settings/components/template-card.tsx
  - frontend/src/features/settings/components/my-skill-card.tsx
  - frontend/src/features/settings/components/create-template-modal.tsx
  - frontend/src/features/settings/pages/skills-settings-page.tsx
  - frontend/src/services/api/user-skills.test.ts
autonomous: false
requirements:
  - P20-09
  - P20-10

must_haves:
  truths:
    - "User sees 'My Skills' section listing their active personalized skills"
    - "User sees 'Skill Templates' section with browsable template catalog"
    - "User can click 'Use This' on a template to trigger AI personalization flow"
    - "Admin sees 'Create Template' button to add workspace templates"
    - "Admin can deactivate built-in templates but not edit their content"
    - "Admin can edit/delete workspace templates"
    - "Source badges distinguish built-in vs workspace templates"
  artifacts:
    - path: "frontend/src/services/api/skill-templates.ts"
      provides: "TanStack Query hooks for skill templates API"
      exports: ["useSkillTemplates", "useCreateSkillTemplate", "useUpdateSkillTemplate", "useDeleteSkillTemplate"]
    - path: "frontend/src/services/api/user-skills.ts"
      provides: "TanStack Query hooks for user skills API"
      exports: ["useUserSkills", "useCreateUserSkill", "useUpdateUserSkill", "useDeleteUserSkill"]
    - path: "frontend/src/features/settings/pages/skills-settings-page.tsx"
      provides: "Restructured skills page with My Skills + Template Catalog"
      contains: "TemplateCatalog"
    - path: "frontend/src/features/settings/components/template-card.tsx"
      provides: "Template card with source badge and Use This button"
      contains: "TemplateCard"
  key_links:
    - from: "skill-templates.ts"
      to: "/api/v1/workspaces/{slug}/skill-templates"
      via: "apiClient.get/post/patch/delete"
      pattern: "skill-templates"
    - from: "user-skills.ts"
      to: "/api/v1/workspaces/{slug}/user-skills"
      via: "apiClient.get/post/patch/delete"
      pattern: "user-skills"
    - from: "skills-settings-page.tsx"
      to: "template-catalog.tsx"
      via: "component composition"
      pattern: "<TemplateCatalog"
---

<objective>
Build the frontend template catalog UI: API hooks, template browsing cards, user skill management, and restructured Skills settings page.

Purpose: This is the user-facing surface where members browse templates, create personalized skills, and admins manage the template catalog.
Output: Restructured Skills settings page with My Skills + Template Catalog sections, template cards, create template modal.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-migrate-all-role-skill-template-of-each-role-then-user-setup-skill-can-pick-template-include-role-template-skill-do-not-depend-on-role/20-RESEARCH.md
@.planning/phases/20-migrate-all-role-skill-template-of-each-role-then-user-setup-skill-can-pick-template-include-role-template-skill-do-not-depend-on-role/20-02-SUMMARY.md
@.planning/phases/20-migrate-all-role-skill-template-of-each-role-then-user-setup-skill-can-pick-template-include-role-template-skill-do-not-depend-on-role/20-03-SUMMARY.md

@frontend/src/features/settings/pages/skills-settings-page.tsx
@frontend/src/services/api/workspace-role-skills.ts
@frontend/src/features/settings/components/skill-generator-modal.tsx
@frontend/src/features/settings/components/plugins-tab-content.tsx

<interfaces>
<!-- Backend API contract from Plan 03 -->

Skill Templates API:
```
GET    /api/v1/workspaces/{slug}/skill-templates     -> SkillTemplateSchema[]
POST   /api/v1/workspaces/{slug}/skill-templates     -> SkillTemplateSchema (admin)
PATCH  /api/v1/workspaces/{slug}/skill-templates/{id} -> SkillTemplateSchema (admin)
DELETE /api/v1/workspaces/{slug}/skill-templates/{id} -> 204 (admin)
```

User Skills API:
```
GET    /api/v1/workspaces/{slug}/user-skills          -> UserSkillSchema[]
POST   /api/v1/workspaces/{slug}/user-skills          -> UserSkillSchema (201)
PATCH  /api/v1/workspaces/{slug}/user-skills/{id}     -> UserSkillSchema
DELETE /api/v1/workspaces/{slug}/user-skills/{id}      -> 204
```

SkillTemplateSchema:
```typescript
interface SkillTemplate {
  id: string;
  workspace_id: string;
  name: string;
  description: string;
  skill_content: string;
  icon: string;
  sort_order: number;
  source: 'built_in' | 'workspace' | 'custom';
  role_type: string | null;
  is_active: boolean;
  created_by: string | null;
  created_at: string;
  updated_at: string;
}
```

UserSkillSchema:
```typescript
interface UserSkill {
  id: string;
  user_id: string;
  workspace_id: string;
  template_id: string | null;
  skill_content: string;
  experience_description: string | null;
  is_active: boolean;
  created_at: string;
  updated_at: string;
  template_name: string | null;
}
```

From workspace-role-skills.ts (pattern reference):
```typescript
// TanStack Query hooks with apiClient.get/post/patch/delete
// Query keys: ['workspace-role-skills', workspaceSlug]
```

From plugins-tab-content.tsx (card layout pattern reference):
```typescript
// observer() component with grid layout, PluginCard sub-components
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API hooks for skill templates and user skills</name>
  <files>
    frontend/src/services/api/skill-templates.ts,
    frontend/src/services/api/user-skills.ts,
    frontend/src/services/api/user-skills.test.ts
  </files>
  <behavior>
    - useSkillTemplates(workspaceSlug): returns { data: SkillTemplate[], isLoading, error }
    - useCreateSkillTemplate(workspaceSlug): mutation, invalidates ['skill-templates', slug]
    - useUpdateSkillTemplate(workspaceSlug): mutation, invalidates query
    - useDeleteSkillTemplate(workspaceSlug): mutation, invalidates query
    - useUserSkills(workspaceSlug): returns { data: UserSkill[], isLoading, error }
    - useCreateUserSkill(workspaceSlug): mutation, invalidates ['user-skills', slug]
    - useUpdateUserSkill(workspaceSlug): mutation, invalidates query
    - useDeleteUserSkill(workspaceSlug): mutation, invalidates query
    - Query keys: ['skill-templates', workspaceSlug] and ['user-skills', workspaceSlug]
  </behavior>
  <action>
    1. Create `skill-templates.ts` following workspace-role-skills.ts pattern:
       - Define SkillTemplate interface with all fields from schema.
       - Define SkillTemplateCreate: { name, description, skill_content, icon?, sort_order?, role_type? }
       - Define SkillTemplateUpdate: { name?, description?, skill_content?, icon?, sort_order?, is_active? }
       - useSkillTemplates: `apiClient.get(\`/workspaces/\${slug}/skill-templates\`)`, queryKey: ['skill-templates', slug]
       - useCreateSkillTemplate: `apiClient.post(...)`, invalidates ['skill-templates', slug]
       - useUpdateSkillTemplate: `apiClient.patch(\`/workspaces/\${slug}/skill-templates/\${id}\`, data)`, invalidates
       - useDeleteSkillTemplate: `apiClient.delete(...)`, invalidates
    2. Create `user-skills.ts` similarly:
       - Define UserSkill interface.
       - Define UserSkillCreate: { template_id, experience_description? }
       - Define UserSkillUpdate: { is_active?, experience_description? }
       - useUserSkills, useCreateUserSkill, useUpdateUserSkill, useDeleteUserSkill
    3. Create `user-skills.test.ts`: test hook creation, mock apiClient, verify query keys and mutation invalidation.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm vitest run src/services/api/user-skills.test.ts --reporter=verbose</automated>
  </verify>
  <done>Both API hook files export typed TanStack Query hooks with correct endpoints and cache invalidation. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Template catalog components + restructured Skills settings page</name>
  <files>
    frontend/src/features/settings/components/template-catalog.tsx,
    frontend/src/features/settings/components/template-card.tsx,
    frontend/src/features/settings/components/my-skill-card.tsx,
    frontend/src/features/settings/components/create-template-modal.tsx,
    frontend/src/features/settings/pages/skills-settings-page.tsx
  </files>
  <action>
    1. Create `template-card.tsx` (plain component, NOT observer — receives props):
       - Props: template (SkillTemplate), onUseThis, onEdit?, onDeactivate?, onDelete?, isAdmin
       - Card layout consistent with PluginCard from Phase 19: name, description, icon, source badge (built-in = blue, workspace = green)
       - "Use This" button for all members (calls onUseThis with template)
       - Admin actions: Edit (workspace only), Deactivate toggle, Delete (workspace only)
       - Built-in templates show lock icon, no edit/delete buttons (only deactivate toggle)

    2. Create `my-skill-card.tsx` (plain component):
       - Props: skill (UserSkill), onToggleActive, onDelete
       - Card showing: skill name (from template_name or "Custom Skill"), experience snippet, active/inactive badge
       - Toggle active button, delete button

    3. Create `template-catalog.tsx` (observer component):
       - Uses useSkillTemplates(workspaceSlug) to load templates
       - Grid layout (3 cols on lg, 2 on md, 1 on sm) — consistent with plugin cards
       - Renders TemplateCard for each template
       - "Use This" opens the SkillGeneratorModal in template mode (pass template content as seed)
       - Loading skeleton while fetching

    4. Create `create-template-modal.tsx`:
       - Dialog with form: name, description, skill content (textarea), icon selector (optional), role type (optional)
       - Uses useCreateSkillTemplate mutation
       - Admin-only action (parent gates visibility)
       - On success: toast notification, close modal, query invalidated automatically

    5. Restructure `skills-settings-page.tsx`:
       - Keep Tabs structure (Skills, Plugins, Action Buttons)
       - Replace the "Skills" tab content with:
         a. **"My Skills"** section at top: useUserSkills hook, grid of MySkillCard. Empty state: "No skills yet. Browse templates below to get started."
         b. **"Skill Templates"** section below: TemplateCatalog component
         c. **Admin section**: "Create Template" button (visible to admins only) opens CreateTemplateModal
       - Remove references to old role-based skill wizard where redundant with new template flow
       - Keep SkillGeneratorModal but adapt its usage: when triggered from "Use This", pre-fill with template content
       - Remove MAX_SKILLS = 3 constant (no cap per user decision — multiple active skills)
       - Keep Plugins tab and Action Buttons tab unchanged
       - File must stay under 700 lines — extract sections into sub-components if needed

    CRITICAL: Follow MobX + TanStack Query pattern. TemplateCatalog and SkillsSettingsPage are observer. TemplateCard and MySkillCard are plain components receiving props.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm vitest run src/features/settings --reporter=verbose</automated>
  </verify>
  <done>Skills settings page shows My Skills + Template Catalog sections. Template cards with source badges. Admin can create/manage workspace templates. Built-in templates read-only. All existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of skill template catalog</name>
  <action>Human verifies the complete skill template catalog UI and flows.</action>
  <what-built>Complete skill template catalog: browsable templates, "Use This" flow, admin create template, my skills section. Backend migration + API endpoints. Materializer refactored.</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000/{workspace}/skills
    2. Verify "My Skills" section shows existing skills (migrated from old role skills)
    3. Verify "Skill Templates" section shows template cards with source badges (built-in vs workspace)
    4. Click "Use This" on a template — verify SkillGeneratorModal opens with template content pre-filled
    5. As admin: verify "Create Template" button visible, opens modal, can create workspace template
    6. As admin: verify built-in templates show lock icon, only deactivate toggle (no edit/delete)
    7. As admin: verify workspace templates have edit + delete actions
    8. Verify Plugins and Action Buttons tabs still work correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && pnpm vitest run src/features/settings --reporter=verbose` all green
- `cd frontend && pnpm vitest run src/services/api/user-skills.test.ts` green
- `cd frontend && pnpm lint` no errors
- `cd frontend && pnpm type-check` no errors
- `make quality-gates-frontend` passes
</verification>

<success_criteria>
- Skills settings page restructured: My Skills + Template Catalog + admin Create Template
- Template cards show source badge, "Use This" button, admin actions
- Built-in templates read-only (only deactivate toggle)
- API hooks with correct query keys and cache invalidation
- All frontend tests pass, type-check clean
</success_criteria>

<output>
After completion, create `.planning/phases/20-migrate-all-role-skill-template-of-each-role-then-user-setup-skill-can-pick-template-include-role-template-skill-do-not-depend-on-role/20-04-SUMMARY.md`
</output>
