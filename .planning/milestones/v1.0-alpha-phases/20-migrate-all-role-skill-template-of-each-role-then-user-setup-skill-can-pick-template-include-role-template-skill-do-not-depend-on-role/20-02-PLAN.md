---
phase: 20-skill-template-catalog
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - backend/src/pilot_space/ai/agents/role_skill_materializer.py
  - backend/src/pilot_space/application/services/skill_template/__init__.py
  - backend/src/pilot_space/application/services/skill_template/seed_templates_service.py
  - backend/src/pilot_space/application/services/user_skill/__init__.py
  - backend/src/pilot_space/application/services/user_skill/create_user_skill_service.py
  - backend/tests/unit/ai/agents/test_role_skill_materializer.py
  - backend/tests/unit/services/test_seed_templates_service.py
  - backend/tests/unit/services/test_create_user_skill_service.py
autonomous: true
requirements:
  - P20-04
  - P20-07
  - P20-08

must_haves:
  truths:
    - "Materializer reads from user_skills + skill_templates (new tables) with OperationalError fallback to legacy"
    - "User skills are materialized as skill-{sanitized-name}-{id[:6]} directories"
    - "Workspace template fallback: templates without user personalization still get materialized"
    - "Built-in templates are seeded per workspace at creation time (non-fatal fire-and-forget)"
    - "AI personalization from template: user picks template, provides experience, AI generates skill content"
  artifacts:
    - path: "backend/src/pilot_space/ai/agents/role_skill_materializer.py"
      provides: "Refactored materialize_role_skills reading from new tables"
      contains: "user_skills"
    - path: "backend/src/pilot_space/application/services/skill_template/seed_templates_service.py"
      provides: "Workspace template seeding service"
      contains: "class SeedTemplatesService"
    - path: "backend/src/pilot_space/application/services/user_skill/create_user_skill_service.py"
      provides: "AI personalization service for user skill creation"
      contains: "class CreateUserSkillService"
  key_links:
    - from: "role_skill_materializer.py"
      to: "user_skill_repository.py"
      via: "import and query"
      pattern: "UserSkillRepository"
    - from: "role_skill_materializer.py"
      to: "skill_template_repository.py"
      via: "import and fallback query"
      pattern: "SkillTemplateRepository"
    - from: "seed_templates_service.py"
      to: "RoleTemplateRepository"
      via: "reads built-in templates"
      pattern: "get_all_ordered"
---

<objective>
Refactor the materializer to read from new tables and create the core backend services: template seeding and AI-based user skill creation.

Purpose: The materializer is the critical integration point where skills get injected into PilotSpaceAgent. Seeding ensures new workspaces get templates. User skill creation enables the template-based flow.
Output: Refactored materializer, SeedTemplatesService, CreateUserSkillService, all with passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-migrate-all-role-skill-template-of-each-role-then-user-setup-skill-can-pick-template-include-role-template-skill-do-not-depend-on-role/20-RESEARCH.md
@.planning/phases/20-migrate-all-role-skill-template-of-each-role-then-user-setup-skill-can-pick-template-include-role-template-skill-do-not-depend-on-role/20-01-SUMMARY.md

@backend/src/pilot_space/ai/agents/role_skill_materializer.py
@backend/src/pilot_space/application/services/workspace_plugin/seed_plugins_service.py
@backend/src/pilot_space/application/services/role_skill/generate_role_skill_service.py
@backend/tests/unit/ai/agents/test_role_skill_materializer.py

<interfaces>
<!-- From Plan 01 outputs -->

From skill_template_repository.py:
```python
class SkillTemplateRepository:
    def __init__(self, session: AsyncSession): ...
    async def get_active_by_workspace(self, workspace_id: UUID) -> list[SkillTemplate]: ...
    async def get_by_workspace(self, workspace_id: UUID) -> list[SkillTemplate]: ...
    async def create(self, **kwargs) -> SkillTemplate: ...
```

From user_skill_repository.py:
```python
class UserSkillRepository:
    def __init__(self, session: AsyncSession): ...
    async def get_by_user_workspace(self, user_id: UUID, workspace_id: UUID) -> list[UserSkill]: ...
    async def get_by_user_workspace_template(self, user_id: UUID, workspace_id: UUID, template_id: UUID) -> UserSkill | None: ...
    async def create(self, **kwargs) -> UserSkill: ...
```

From role_skill_materializer.py (current, being refactored):
```python
async def materialize_role_skills(db_session, user_id, workspace_id, skills_dir) -> int:
    # Called from pilotspace_agent.py line ~289
    # MUST keep this function name (wrapper) to avoid touching 698-line agent file
```

From seed_plugins_service.py (pattern reference):
```python
class SeedPluginsService:
    def __init__(self, db_session: AsyncSession): ...
    async def seed_workspace(self, workspace_id: UUID) -> None: ...
    # Called via asyncio.create_task in workspace creation — fire-and-forget, non-fatal
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor materializer to read from new tables</name>
  <files>
    backend/src/pilot_space/ai/agents/role_skill_materializer.py,
    backend/tests/unit/ai/agents/test_role_skill_materializer.py
  </files>
  <behavior>
    - materialize_role_skills() still exists as entry point (wrapper) — do NOT rename, pilotspace_agent.py calls it
    - New internal _materialize_from_new_tables() reads UserSkill rows via UserSkillRepository.get_by_user_workspace()
    - For each user_skill: dir_name = skill-{sanitized_name}-{id[:6]} where sanitized_name comes from template.name or fallback to id[:8]
    - _sanitize_skill_dir_name(name, fallback_id) -> str: lowercases, replaces non-alphanumeric with hyphens, strips leading/trailing hyphens
    - After user skills: load active skill_templates via SkillTemplateRepository.get_active_by_workspace() for templates where user has NO matching user_skill (fallback)
    - Workspace template fallback dirs use skill-{sanitized_name}-{template.id[:6]}
    - OperationalError wraps new table queries — falls back to legacy _materialize_from_legacy_tables()
    - Legacy function is current materialize_role_skills logic (extracted, not deleted)
    - Plugin materialization (materialize_plugin_skills) unchanged — still called at end
    - Cleanup removes stale skill-* dirs not in expected_dirs set
    - Total file stays under 700 lines
  </behavior>
  <action>
    1. Extract current materialize_role_skills body into `_materialize_from_legacy_tables(db_session, user_id, workspace_id, skills_dir) -> int`.
    2. Create `_sanitize_skill_dir_name(name: str, fallback_id: str) -> str` using `re.sub(r"[^a-z0-9]+", "-", name.lower()).strip("-")`, fallback to `fallback_id[:8]` if empty.
    3. Create `_materialize_from_new_tables(db_session, user_id, workspace_id, skills_dir) -> int`:
       a. Import UserSkillRepository, SkillTemplateRepository (lazy imports inside function).
       b. Load user skills: `user_skills = await user_repo.get_by_user_workspace(user_id, workspace_id)`.
       c. For each user_skill: build dir_name using template.name (if template exists) or skill id. Write SKILL.md with frontmatter (name, description, origin: personal).
       d. Track template_ids covered by user skills.
       e. Load active workspace templates: `templates = await template_repo.get_active_by_workspace(workspace_id)`.
       f. For templates not covered: write SKILL.md with frontmatter (name, description, origin: workspace).
       g. Cleanup stale skill-* dirs.
       h. Return total count.
    4. Rewrite `materialize_role_skills()` as: try _materialize_from_new_tables, except OperationalError fall back to _materialize_from_legacy_tables. Always call materialize_plugin_skills at the end.
    5. Update `_build_frontmatter` and `_build_workspace_frontmatter` to accept name/id instead of role_type/role_name.
    6. Update existing tests + add new tests for new table path.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/ai/agents/test_role_skill_materializer.py -x -v</automated>
  </verify>
  <done>Materializer reads from user_skills + skill_templates with OperationalError fallback. Directory naming uses sanitized template names. Plugin materialization unchanged. File under 700 lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SeedTemplatesService and CreateUserSkillService</name>
  <files>
    backend/src/pilot_space/application/services/skill_template/__init__.py,
    backend/src/pilot_space/application/services/skill_template/seed_templates_service.py,
    backend/src/pilot_space/application/services/user_skill/__init__.py,
    backend/src/pilot_space/application/services/user_skill/create_user_skill_service.py,
    backend/tests/unit/services/test_seed_templates_service.py,
    backend/tests/unit/services/test_create_user_skill_service.py
  </files>
  <behavior>
    - SeedTemplatesService.seed_workspace(workspace_id) copies all RoleTemplate rows into skill_templates (source=built_in) for the workspace
    - SeedTemplatesService is non-fatal: wraps entire body in try/except, logs errors, never raises
    - SeedTemplatesService skips if workspace already has built_in templates (idempotent)
    - CreateUserSkillService.create(user_id, workspace_id, template_id, experience_description) -> UserSkill:
      a. Loads template from SkillTemplateRepository
      b. Calls AI generation (reuse GenerateRoleSkillService pattern) with template.skill_content + experience
      c. Creates UserSkill row with generated content
    - CreateUserSkillService raises ValueError if template not found or inactive
    - CreateUserSkillService raises ValueError if user already has a skill from this template (duplicate check)
  </behavior>
  <action>
    1. Create `skill_template/` service package with `__init__.py`.
    2. Create `SeedTemplatesService` following `SeedPluginsService` pattern:
       - Constructor takes `AsyncSession`.
       - `seed_workspace(workspace_id)`: check if workspace already has built_in templates (idempotent guard). Load RoleTemplates via RoleTemplateRepository.get_all_ordered(). For each, create SkillTemplate via SkillTemplateRepository.create(source='built_in', ...). Wrap in try/except with logger.exception.
    3. Create `user_skill/` service package with `__init__.py`.
    4. Create `CreateUserSkillService`:
       - Constructor takes `AsyncSession`.
       - `create(user_id, workspace_id, template_id, experience_description) -> UserSkill`:
         a. Load template via SkillTemplateRepository.get_by_id (verify active, same workspace).
         b. Check duplicate via UserSkillRepository.get_by_user_workspace_template.
         c. Use GenerateRoleSkillService._generate_skill_content pattern (or call it) to produce personalized skill_content from template.skill_content + experience_description.
         d. Create UserSkill via repository.
         e. Return the created UserSkill.
    5. Write tests: mock RoleTemplateRepository for seed tests, mock AI generation for create tests.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/services/test_seed_templates_service.py tests/unit/services/test_create_user_skill_service.py -x -v</automated>
  </verify>
  <done>SeedTemplatesService seeds built-in templates idempotently. CreateUserSkillService generates personalized skill from template via AI. Both have passing tests.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run pytest tests/unit/ai/agents/ tests/unit/services/test_seed_templates_service.py tests/unit/services/test_create_user_skill_service.py -x -v` all green
- `cd backend && uv run ruff check src/pilot_space/ai/agents/role_skill_materializer.py src/pilot_space/application/services/skill_template/ src/pilot_space/application/services/user_skill/` no errors
- `cd backend && uv run pyright src/pilot_space/ai/agents/role_skill_materializer.py` no errors
- `wc -l backend/src/pilot_space/ai/agents/role_skill_materializer.py` under 700 lines
</verification>

<success_criteria>
- Materializer reads user_skills first, falls back to skill_templates for uncovered templates, with OperationalError guard for legacy
- SeedTemplatesService copies RoleTemplate rows per workspace, idempotent, non-fatal
- CreateUserSkillService: template lookup + AI generation + user_skill creation
- pilotspace_agent.py NOT modified (materialize_role_skills wrapper name preserved)
- All tests pass, materializer under 700 lines
</success_criteria>

<output>
After completion, create `.planning/phases/20-migrate-all-role-skill-template-of-each-role-then-user-setup-skill-can-pick-template-include-role-template-skill-do-not-depend-on-role/20-02-SUMMARY.md`
</output>
