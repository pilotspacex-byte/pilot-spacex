---
phase: 018-tech-debt-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/oidc-login.spec.ts
  - frontend/e2e/fixtures/oidc-mock.ts
  - infra/oidc-mock/docker-compose.yml
  - infra/oidc-mock/oidc-mock-config.json
autonomous: false
requirements:
  - DEBT-01

must_haves:
  truths:
    - "Playwright E2E test verifies full OIDC flow: SSO settings -> initiate login -> IdP redirect -> callback -> authenticated session"
    - "Mock OIDC IdP runs in Docker for CI reproducibility"
    - "Test covers all 3 provider types: Okta, Azure AD, Google Workspace (parameterized or sequential)"
  artifacts:
    - path: "frontend/e2e/oidc-login.spec.ts"
      provides: "Playwright E2E test for OIDC login flow"
      contains: "oidc"
    - path: "infra/oidc-mock/docker-compose.yml"
      provides: "Docker compose for mock OIDC IdP"
      contains: "oidc-server-mock"
  key_links:
    - from: "frontend/e2e/oidc-login.spec.ts"
      to: "infra/oidc-mock/docker-compose.yml"
      via: "test setup starts Docker mock IdP"
      pattern: "docker compose"
    - from: "frontend/e2e/oidc-login.spec.ts"
      to: "backend/src/pilot_space/api/v1/routers/auth_sso.py"
      via: "SSO settings page -> OIDC login initiation -> callback"
      pattern: "sso|oidc"
---

<objective>
Create Playwright E2E test for OIDC login flow using mock IdP

Purpose: Close DEBT-01 — verify the OIDC login flow (Okta/Azure AD/Google Workspace) works end-to-end in a real browser. The SSO backend and frontend exist but have never been E2E tested. A mock OIDC IdP in Docker enables CI regression testing.

Output: Playwright E2E test file, Docker compose for mock OIDC IdP, manual verification checklist for real provider test tenants.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/018-tech-debt-closure/18-CONTEXT.md
@.planning/phases/018-tech-debt-closure/18-RESEARCH.md

@backend/src/pilot_space/api/v1/routers/auth_sso.py
@backend/src/pilot_space/services/sso_service.py
@frontend/src/features/settings/hooks/use-sso-login.ts
@frontend/src/features/settings/components/sso-settings-page.tsx
@frontend/playwright.config.ts
@frontend/e2e/ (existing E2E test structure)

<interfaces>
<!-- SSO backend endpoints (auth_sso.py) -->
# GET /api/v1/auth/sso/providers — list configured OIDC providers
# POST /api/v1/auth/sso/login — initiate OIDC login (returns redirect URL)
# GET /api/v1/auth/sso/callback — handle IdP callback, create session

<!-- SSO frontend hooks -->
# use-sso-login.ts: useSsoLogin() -> { loginWithSso(provider), isLoading, error }
# sso-settings-page.tsx: SSO provider configuration UI
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mock OIDC IdP Docker setup + Playwright E2E test</name>
  <files>
    infra/oidc-mock/docker-compose.yml
    infra/oidc-mock/oidc-mock-config.json
    frontend/e2e/oidc-login.spec.ts
    frontend/e2e/fixtures/oidc-mock.ts
  </files>
  <action>
1. Read existing Playwright config (`frontend/playwright.config.ts`) and E2E test structure (`frontend/e2e/`) to understand the project's E2E patterns.

2. Read `auth_sso.py` and `sso_service.py` to understand the OIDC endpoints: how login is initiated, what the callback expects, what Supabase Auth configuration is needed.

3. Create `infra/oidc-mock/docker-compose.yml`:
   - Use `ghcr.io/soluto/oidc-server-mock` (or similar lightweight OIDC mock)
   - Configure with test users, client ID/secret
   - Expose on port 9090 (avoid conflicts with existing services on 8000, 3000, 18000)
   - Mount `oidc-mock-config.json` for user/client configuration

4. Create `infra/oidc-mock/oidc-mock-config.json`:
   - Define 3 provider configurations mimicking Okta, Azure AD, Google Workspace
   - Each with: issuer URL, client_id, client_secret, test user claims (email, name, sub)
   - Use different scopes/claim structures to match each real provider

5. Create `frontend/e2e/fixtures/oidc-mock.ts`:
   - Helper to start/stop the mock IdP Docker container before/after tests
   - Health check: wait for `http://localhost:9090/.well-known/openid-configuration` to be available
   - Cleanup: stop container after test suite

6. Create `frontend/e2e/oidc-login.spec.ts`:
   - Use Playwright test with the oidc-mock fixture
   - Test flow for each provider type (parameterized):
     a. Navigate to workspace SSO settings page
     b. Configure OIDC provider (issuer URL pointing to mock, client ID/secret)
     c. Initiate SSO login
     d. Handle IdP redirect (Playwright follows redirects to mock IdP)
     e. Enter test user credentials on mock IdP login page
     f. Verify callback redirects back to app with authenticated session
     g. Verify user is logged in (check for user-specific UI element)
   - Mark tests with `@slow` tag if Playwright config supports it (Docker startup time)
   - Add `test.describe.configure({ timeout: 60_000 })` for Docker startup

NOTE: The mock IdP approach depends on Supabase Auth supporting custom OIDC providers. Read the SSO service to understand if Supabase handles the OIDC flow or if the backend does it directly. Adjust the test accordingly.

Per user decision: Mock IdP library choice is Claude's discretion — use oidc-server-mock (purpose-built for testing, per research recommendation).
  </action>
  <verify>
    <automated>cd infra/oidc-mock && docker compose config --quiet && echo "Docker compose valid" && cd ../../frontend && npx playwright test e2e/oidc-login.spec.ts --reporter=list 2>&1 | head -50</automated>
  </verify>
  <done>
    - infra/oidc-mock/docker-compose.yml is valid and starts mock OIDC IdP
    - Playwright E2E test covers full OIDC flow for 3 provider types
    - Test is self-contained (starts/stops Docker mock IdP)
    - Test passes in CI-like environment (no external dependencies beyond Docker)
  </done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Manual OIDC verification with real providers (optional)</name>
  <action>
OIDC E2E test with mock IdP has been completed. This checkpoint is for optional manual verification against real provider test tenants (Okta dev, Azure AD test, Google Workspace test) if available.

Steps to verify manually:
1. If you have real OIDC test tenants available:
   - Start the app: `cd backend && uv run uvicorn pilot_space.main:app --reload --port 8000` and `cd frontend && pnpm dev`
   - Navigate to workspace SSO settings (http://localhost:3000/workspace/settings/sso)
   - Configure each provider with real test tenant credentials
   - Test login flow for each: click SSO login -> redirect to real IdP -> authenticate -> callback -> verify session
2. If no real test tenants available:
   - Confirm the mock IdP test passes: `cd frontend && pnpm test:e2e --grep "oidc"`
   - Type "approved" to continue
3. Verify no console errors during the flow
  </action>
  <verify>Manual verification by user</verify>
  <done>User confirms OIDC flow works with real or mock providers</done>
</task>

</tasks>

<verification>
cd infra/oidc-mock && docker compose config --quiet
cd frontend && npx playwright test e2e/oidc-login.spec.ts --reporter=list
</verification>

<success_criteria>
- Mock OIDC IdP Docker setup is functional and reproducible
- Playwright E2E test verifies full OIDC flow (settings -> login -> IdP -> callback -> session)
- Test covers 3 provider types (Okta, Azure AD, Google Workspace) via parameterization
- Test is CI-ready (no manual steps required for automated run)
</success_criteria>

<output>
After completion, create `.planning/phases/018-tech-debt-closure/18-03-SUMMARY.md`
</output>
