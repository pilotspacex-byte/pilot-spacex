---
phase: 018-tech-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/pilot_space/ai/mcp/note_content_server.py
  - backend/src/pilot_space/ai/mcp/issue_relation_server.py
  - backend/tests/unit/ai/test_note_content_server_approval.py
  - backend/tests/unit/ai/test_issue_relation_server_approval.py
  - backend/tests/audit/conftest.py
  - backend/tests/audit/test_audit_api.py
  - backend/tests/audit/test_audit_export.py
  - backend/tests/audit/test_immutability.py
  - backend/tests/audit/test_retention.py
autonomous: true
requirements:
  - DEBT-02
  - DEBT-03

must_haves:
  truths:
    - "note_content_server.py calls check_approval_from_db() for every mutating tool (6 of 7 tools; search_note_content is read-only auto-execute)"
    - "issue_relation_server.py calls check_approval_from_db() for all 4 tools"
    - "Neither file imports or calls get_tool_approval_level()"
    - "All audit API tests in test_audit_api.py pass without xfail markers"
    - "All audit export tests in test_audit_export.py pass without xfail markers"
    - "Immutability router tests (2 of 5) pass without xfail; 3 PostgreSQL trigger tests remain @pytest.mark.integration"
    - "All retention tests in test_retention.py pass without xfail markers"
  artifacts:
    - path: "backend/src/pilot_space/ai/mcp/note_content_server.py"
      provides: "DB-backed approval for all mutating tools"
      contains: "check_approval_from_db"
    - path: "backend/src/pilot_space/ai/mcp/issue_relation_server.py"
      provides: "DB-backed approval for all 4 tools"
      contains: "check_approval_from_db"
    - path: "backend/tests/unit/ai/test_note_content_server_approval.py"
      provides: "Unit tests for note_content_server approval wiring"
    - path: "backend/tests/unit/ai/test_issue_relation_server_approval.py"
      provides: "Unit tests for issue_relation_server approval wiring"
    - path: "backend/tests/audit/conftest.py"
      provides: "AsyncClient fixture with auth and workspace context for audit tests"
      contains: "AsyncClient"
  key_links:
    - from: "backend/src/pilot_space/ai/mcp/note_content_server.py"
      to: "pilot_space.ai.tools.mcp_server.check_approval_from_db"
      via: "import and call in each tool handler"
      pattern: "check_approval_from_db"
    - from: "backend/src/pilot_space/ai/mcp/issue_relation_server.py"
      to: "pilot_space.ai.tools.mcp_server.check_approval_from_db"
      via: "import and call in each tool handler"
      pattern: "check_approval_from_db"
    - from: "backend/tests/audit/conftest.py"
      to: "backend/tests/conftest.py"
      via: "reuse app fixture, add AsyncClient with auth headers"
      pattern: "ASGITransport"
---

<objective>
Fix MCP approval wiring gaps and audit test infrastructure

Purpose: Close DEBT-02 (MCP servers must use DB-backed approval, not static map) and DEBT-03 (26 xfail audit tests need proper AsyncClient fixture to pass). Both are backend test/code correctness fixes with no new features.

Output: 2 MCP server files using correct approval pattern, 4 audit test files passing without xfail markers (except 3 PostgreSQL-only immutability trigger tests which become @pytest.mark.integration), 2 new unit test files for MCP approval verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/018-tech-debt-closure/18-CONTEXT.md
@.planning/phases/018-tech-debt-closure/18-RESEARCH.md

@backend/src/pilot_space/ai/mcp/note_content_server.py
@backend/src/pilot_space/ai/mcp/issue_relation_server.py
@backend/src/pilot_space/ai/mcp/note_server.py (correct approval pattern reference)
@backend/src/pilot_space/ai/tools/mcp_server.py (check_approval_from_db source)
@backend/src/pilot_space/ai/infrastructure/approval.py (ActionType enum)
@backend/tests/audit/conftest.py
@backend/tests/audit/test_audit_api.py
@backend/tests/audit/test_audit_export.py
@backend/tests/audit/test_immutability.py
@backend/tests/audit/test_retention.py
@backend/tests/conftest.py (lines 560-670 for client fixture patterns)

<interfaces>
<!-- Correct approval pattern from note_server.py / issue_server.py -->
from pilot_space.ai.infrastructure.approval import ActionType as AT
from pilot_space.ai.tools.mcp_server import ToolContext, check_approval_from_db

# Alias inside create_*_server():
_chk = check_approval_from_db

# Each tool handler:
approval_level = await _chk("tool_name", AT.ACTION_TYPE, tool_context)
status = "approval_required" if approval_level.value != "auto_execute" else "pending_apply"

<!-- Tool-to-ActionType mapping for note_content_server.py -->
# search_note_content → None (read-only, auto-execute — no approval needed)
# insert_block → AT.INSERT_BLOCK
# remove_block → AT.REMOVE_BLOCK
# remove_content → AT.REMOVE_CONTENT
# replace_content → AT.REPLACE_CONTENT
# create_pm_block → AT.INSERT_BLOCK
# update_pm_block → AT.REPLACE_CONTENT

<!-- Tool-to-ActionType mapping for issue_relation_server.py -->
# link_issue_to_note → AT.LINK_ISSUE_TO_NOTE
# link_issues → AT.LINK_ISSUES
# add_sub_issue → AT.ADD_SUB_ISSUE
# transition_issue_state → AT.TRANSITION_ISSUE_STATE

<!-- Root conftest client fixture pattern (lines 605-615) -->
@pytest.fixture
async def client(app: Any) -> AsyncGenerator[AsyncClient, None]:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        yield ac
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix MCP approval wiring in note_content_server.py and issue_relation_server.py</name>
  <files>
    backend/src/pilot_space/ai/mcp/note_content_server.py
    backend/src/pilot_space/ai/mcp/issue_relation_server.py
    backend/tests/unit/ai/test_note_content_server_approval.py
    backend/tests/unit/ai/test_issue_relation_server_approval.py
  </files>
  <behavior>
    - note_content_server: insert_block calls check_approval_from_db("insert_block", AT.INSERT_BLOCK, ctx)
    - note_content_server: remove_block calls check_approval_from_db("remove_block", AT.REMOVE_BLOCK, ctx)
    - note_content_server: remove_content calls check_approval_from_db("remove_content", AT.REMOVE_CONTENT, ctx)
    - note_content_server: replace_content calls check_approval_from_db("replace_content", AT.REPLACE_CONTENT, ctx)
    - note_content_server: create_pm_block calls check_approval_from_db("create_pm_block", AT.INSERT_BLOCK, ctx)
    - note_content_server: update_pm_block calls check_approval_from_db("update_pm_block", AT.REPLACE_CONTENT, ctx)
    - note_content_server: search_note_content does NOT call approval (read-only)
    - issue_relation_server: link_issue_to_note calls check_approval_from_db("link_issue_to_note", AT.LINK_ISSUE_TO_NOTE, ctx)
    - issue_relation_server: link_issues calls check_approval_from_db("link_issues", AT.LINK_ISSUES, ctx)
    - issue_relation_server: add_sub_issue calls check_approval_from_db("add_sub_issue", AT.ADD_SUB_ISSUE, ctx)
    - issue_relation_server: transition_issue_state calls check_approval_from_db("transition_issue_state", AT.TRANSITION_ISSUE_STATE, ctx)
    - Neither file imports get_tool_approval_level after the change
  </behavior>
  <action>
1. Create unit test files first (RED phase):
   - `test_note_content_server_approval.py`: Test that each of the 6 mutating tools calls `check_approval_from_db` with the correct tool_name and ActionType. Mock `check_approval_from_db` to return a mock approval level. Verify search_note_content does NOT call it.
   - `test_issue_relation_server_approval.py`: Test that each of the 4 tools calls `check_approval_from_db` with correct args.
   - Follow the pattern from `test_mcp_server_approval.py` (the existing correct test).

2. Fix `note_content_server.py` (GREEN phase):
   - Change import: replace `from pilot_space.ai.tools.mcp_server import ToolContext, get_tool_approval_level` with `from pilot_space.ai.infrastructure.approval import ActionType as AT` and `from pilot_space.ai.tools.mcp_server import ToolContext, check_approval_from_db`
   - Inside `create_note_content_server()`, add `_chk = check_approval_from_db`
   - In each mutating tool handler, replace `approval_level = get_tool_approval_level("tool_name")` with `approval_level = await _chk("tool_name", AT.ACTION_TYPE, tool_context)` using the mapping from the interfaces block above
   - search_note_content is read-only — leave it unchanged (no approval check needed)
   - Remove any remaining `get_tool_approval_level` usage

3. Fix `issue_relation_server.py` (GREEN phase):
   - Same import change pattern as note_content_server
   - Add `_chk = check_approval_from_db` inside `create_issue_relation_server()`
   - Replace all 4 tool handlers' `get_tool_approval_level()` calls with `await _chk()` using the mapping above
   - Remove any remaining `get_tool_approval_level` usage

PITFALL: tool_context can be None — check_approval_from_db already handles this gracefully (falls back to static map). No special handling needed.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/ai/test_note_content_server_approval.py tests/unit/ai/test_issue_relation_server_approval.py -x -q</automated>
  </verify>
  <done>
    - note_content_server.py: 6 mutating tools use check_approval_from_db with correct ActionType; search_note_content unchanged
    - issue_relation_server.py: 4 tools use check_approval_from_db with correct ActionType
    - Neither file imports get_tool_approval_level
    - All new unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix audit test infrastructure and remove xfail markers</name>
  <files>
    backend/tests/audit/conftest.py
    backend/tests/audit/test_audit_api.py
    backend/tests/audit/test_audit_export.py
    backend/tests/audit/test_immutability.py
    backend/tests/audit/test_retention.py
  </files>
  <action>
1. Update `tests/audit/conftest.py`:
   - Add an `audit_client` fixture: authenticated AsyncClient with ADMIN role and workspace context. Follow the `client_with_workspace` pattern from root conftest (lines 645-670). The fixture needs: `app` fixture, auth headers with Bearer token, workspace context set via mock or header.
   - Also add supporting fixtures if needed: `test_workspace_id` (UUID factory), `mock_auth` (patches Supabase auth verification to return test user with ADMIN role).
   - Ensure the `audit_log_factory` fixture still works (it depends on `db_session` from parent conftest).

2. Update `test_audit_api.py` (9 xfail tests):
   - Remove all `@pytest.mark.xfail(strict=False, reason="audit router implementation pending")` markers
   - Update test function signatures to accept the new `audit_client` fixture instead of bare `client`
   - Ensure each test calls the correct API endpoint with auth headers and workspace context
   - If any test requires seeded audit data, use `audit_log_factory`

3. Update `test_audit_export.py` (8 xfail tests):
   - Same pattern: remove xfail markers, use `audit_client` fixture

4. Update `test_retention.py` (4 xfail tests):
   - Same pattern: remove xfail markers, use `audit_client` fixture

5. Update `test_immutability.py` (5 xfail tests):
   - For the 3 PostgreSQL trigger tests: replace `@pytest.mark.xfail` with `@pytest.mark.integration` (they test DB triggers, only pass with TEST_DATABASE_URL set to PostgreSQL)
   - For the 2 router tests: remove xfail, use `audit_client` fixture

IMPORTANT: Read each test file fully before editing to understand what each test expects. The tests already have test bodies — they just lack the proper client fixture. The fix is infrastructure (conftest), not test logic.

PITFALL: Audit endpoints call `_resolve_workspace()` and `set_rls_context()` — the client fixture must provide workspace context. Follow `client_with_workspace` pattern from root conftest.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/audit/test_audit_api.py tests/audit/test_audit_export.py tests/audit/test_retention.py -x -q && echo "--- immutability router tests ---" && uv run pytest tests/audit/test_immutability.py -k "not trigger" -x -q</automated>
  </verify>
  <done>
    - tests/audit/conftest.py has audit_client fixture with AsyncClient + auth + workspace context
    - test_audit_api.py: 9 tests pass, no xfail markers
    - test_audit_export.py: 8 tests pass, no xfail markers
    - test_retention.py: 4 tests pass, no xfail markers
    - test_immutability.py: 2 router tests pass without xfail; 3 trigger tests marked @pytest.mark.integration
    - Zero xfail markers remain in audit test directory (except integration-marked PostgreSQL tests)
  </done>
</task>

</tasks>

<verification>
cd backend && uv run pytest tests/unit/ai/test_note_content_server_approval.py tests/unit/ai/test_issue_relation_server_approval.py tests/audit/ -x -q
cd backend && uv run ruff check src/pilot_space/ai/mcp/note_content_server.py src/pilot_space/ai/mcp/issue_relation_server.py
cd backend && uv run pyright src/pilot_space/ai/mcp/note_content_server.py src/pilot_space/ai/mcp/issue_relation_server.py
</verification>

<success_criteria>
- get_tool_approval_level() no longer called in any MCP tool handler (note_content_server, issue_relation_server)
- check_approval_from_db() used for all mutating tools with correct ActionType
- 23+ audit tests pass without xfail markers (9 API + 8 export + 4 retention + 2 immutability router)
- 3 immutability trigger tests properly marked @pytest.mark.integration
- All quality gates pass (ruff + pyright + pytest)
</success_criteria>

<output>
After completion, create `.planning/phases/018-tech-debt-closure/18-01-SUMMARY.md`
</output>
