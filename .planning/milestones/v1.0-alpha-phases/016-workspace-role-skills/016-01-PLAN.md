---
phase: 016-workspace-role-skills
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/repositories/test_workspace_role_skill_repository.py
  - backend/tests/unit/services/test_workspace_role_skill_service.py
  - backend/tests/unit/api/test_workspace_role_skills_router.py
  - backend/tests/unit/ai/agents/test_role_skill_materializer.py
  - frontend/src/features/settings/components/__tests__/workspace-skill-card.test.tsx
  - frontend/src/services/api/__tests__/workspace-role-skills.test.ts
autonomous: true
requirements:
  - WRSKL-01
  - WRSKL-02
  - WRSKL-03
  - WRSKL-04

must_haves:
  truths:
    - "All 6 xfail backend stubs exist and are collected by pytest without import errors"
    - "Frontend it.todo() stubs exist and are collected by vitest without errors"
    - "Materializer test file is extended with 2 new xfail stubs for workspace skill injection"
    - "API client test stub file exists and is collected by vitest without errors"
  artifacts:
    - path: "backend/tests/unit/repositories/test_workspace_role_skill_repository.py"
      provides: "Repository CRUD + UNIQUE constraint xfail stubs"
    - path: "backend/tests/unit/services/test_workspace_role_skill_service.py"
      provides: "Service generate/activate/list/delete xfail stubs (WRSKL-01, WRSKL-02)"
    - path: "backend/tests/unit/api/test_workspace_role_skills_router.py"
      provides: "Admin-only 403 guard xfail stubs (WRSKL-01, WRSKL-02)"
    - path: "backend/tests/unit/ai/agents/test_role_skill_materializer.py"
      provides: "Materializer inheritance + override xfail stubs (WRSKL-03, WRSKL-04)"
    - path: "frontend/src/features/settings/components/__tests__/workspace-skill-card.test.tsx"
      provides: "Frontend it.todo() stubs (WRSKL-01, WRSKL-02)"
    - path: "frontend/src/services/api/__tests__/workspace-role-skills.test.ts"
      provides: "API client it.todo() stubs (WRSKL-01, WRSKL-02)"
  key_links:
    - from: "backend/tests/unit/api/test_workspace_role_skills_router.py"
      to: "pilot_space.api.v1.routers.workspace_role_skills"
      via: "xfail import"
      pattern: "pytest.importorskip"
    - from: "backend/tests/unit/ai/agents/test_role_skill_materializer.py"
      to: "pilot_space.ai.agents.role_skill_materializer"
      via: "import extension"
      pattern: "test_workspace_skill_inherited"
---

<objective>
Create Wave 0 test stubs for all Phase 16 requirements so automated verification can run from the first implementation task.

Purpose: Nyquist compliance — every implementation task must have an automated test command. Without stubs, verify commands would fail on import before any code is written.
Output: 6 stub files (4 backend xfail, 2 frontend it.todo()) that fail correctly and are collected by test runners.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/016-workspace-role-skills/016-RESEARCH.md

<!-- Stub patterns from previous phases -->
<!-- From Phase 15 (015-01): xfail stubs use pytest.fail() (not assert False) to satisfy PT015/B011 ruff rules -->
<!-- From Phase 14 (014-01): frontend stubs use it.todo() for Vitest pending state -->
<!-- From STATE.md decisions: xfail(strict=False) — runs body, reports XFAIL/XPASS -->
</context>

<interfaces>
<!-- Existing test patterns this plan must follow exactly -->

Backend xfail stub pattern (from test_workspace_tasks.py / Phase 15):
```python
import pytest

@pytest.mark.xfail(strict=False, reason="implementation pending — WRSKL-01")
async def test_something() -> None:
    pytest.fail("not implemented")
```

Frontend it.todo() pattern (from Phase 14 frontend stubs):
```typescript
import { describe, it } from 'vitest';

describe('WorkspaceSkillCard', () => {
  it.todo('renders pending badge when is_active is false');
  it.todo('renders active badge when is_active is true');
  it.todo('calls onActivate when Activate button clicked');
  it.todo('calls onRemove when Remove button clicked');
});
```

Role skill test utilities (from existing test_role_skill_repository.py if it exists):
- Tests use SQLite in-memory (db_session fixture)
- async tests use @pytest.mark.asyncio
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend xfail stubs — repository, service, and router tests</name>
  <files>
    backend/tests/unit/repositories/test_workspace_role_skill_repository.py,
    backend/tests/unit/services/test_workspace_role_skill_service.py,
    backend/tests/unit/api/test_workspace_role_skills_router.py
  </files>
  <action>
Create three xfail stub files. All use `@pytest.mark.xfail(strict=False, reason="...")` with `pytest.fail("not implemented")` body to satisfy ruff PT015/B011 rules.

**test_workspace_role_skill_repository.py** — stubs covering:
- `test_create_workspace_role_skill` (WRSKL-01)
- `test_get_active_by_workspace` (WRSKL-03)
- `test_unique_constraint_workspace_role` (WRSKL-01)
- `test_soft_delete_skill` (WRSKL-02)

**test_workspace_role_skill_service.py** — stubs covering:
- `test_generate_workspace_skill_creates_inactive` (WRSKL-01 — is_active=False after generate)
- `test_activate_workspace_skill_sets_active` (WRSKL-02)
- `test_list_workspace_skills` (WRSKL-01)
- `test_delete_workspace_skill` (WRSKL-01)
- `test_generate_rate_limit_enforced` (WRSKL-01)

**test_workspace_role_skills_router.py** — stubs covering:
- `test_create_workspace_skill_forbidden_for_member` (WRSKL-01 — 403 for non-admin)
- `test_activate_workspace_skill_forbidden_for_member` (WRSKL-02 — 403 for non-admin)
- `test_create_workspace_skill_as_admin` (WRSKL-01 — 201 success)
- `test_activate_workspace_skill_as_admin` (WRSKL-02 — 200 success)

Use `import pytest` only — no module imports for not-yet-existing modules (router/service don't exist yet; just stub signatures).

Add standard conftest imports at top: `import pytest` only. Add module-level docstring: `"""Xfail stubs for WRSKL-01..02 — workspace role skill router tests."""`
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/repositories/test_workspace_role_skill_repository.py tests/unit/services/test_workspace_role_skill_service.py tests/unit/api/test_workspace_role_skills_router.py --co -q 2>&1 | tail -20</automated>
  </verify>
  <done>All 13 stubs collected, all reported as xfail, zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extend materializer tests + frontend stubs (component + API client)</name>
  <files>
    backend/tests/unit/ai/agents/test_role_skill_materializer.py,
    frontend/src/features/settings/components/__tests__/workspace-skill-card.test.tsx,
    frontend/src/services/api/__tests__/workspace-role-skills.test.ts
  </files>
  <action>
**Materializer test extension:**

First read `backend/tests/unit/ai/agents/test_role_skill_materializer.py` to see current test structure. Append two new xfail tests at the end of the file (do NOT remove existing tests):

```python
@pytest.mark.asyncio
@pytest.mark.xfail(strict=False, reason="WRSKL-03: workspace skill injected when no personal skill exists")
async def test_workspace_skill_inherited(tmp_path: Path) -> None:
    """WRSKL-03: active workspace skill appears when user has no personal skill for same role."""
    pytest.fail("not implemented")


@pytest.mark.asyncio
@pytest.mark.xfail(strict=False, reason="WRSKL-04: personal skill takes precedence over workspace skill")
async def test_personal_skill_overrides_workspace(tmp_path: Path) -> None:
    """WRSKL-04: when both personal and workspace skill exist for same role_type, personal wins."""
    pytest.fail("not implemented")
```

If the test file does not exist yet, create it with only these two xfail tests plus necessary imports (`import pytest`, `from pathlib import Path`).

**Frontend component stubs:**

Create `frontend/src/features/settings/components/__tests__/workspace-skill-card.test.tsx` with `it.todo()` stubs:

```typescript
/**
 * Workspace skill card stubs — WRSKL-01, WRSKL-02
 * it.todo() stubs: implement after WorkspaceSkillCard component is created in Plan 04.
 */
import { describe, it } from 'vitest';

describe('WorkspaceSkillCard', () => {
  it.todo('renders Pending Review badge when is_active is false (WRSKL-02)');
  it.todo('renders Active badge when is_active is true (WRSKL-02)');
  it.todo('shows Activate button for pending skills (WRSKL-02)');
  it.todo('calls onActivate with skill id when Activate clicked (WRSKL-02)');
  it.todo('calls onRemove when Remove button clicked (WRSKL-01)');
});
```

**Frontend API client stubs:**

Create `frontend/src/services/api/__tests__/workspace-role-skills.test.ts` with `it.todo()` stubs:

```typescript
/**
 * Workspace role skills API client stubs — WRSKL-01, WRSKL-02
 * it.todo() stubs: implement after workspace-role-skills.ts is created in Plan 04.
 */
import { describe, it } from 'vitest';

describe('workspaceRoleSkillsApi', () => {
  it.todo('getWorkspaceSkills calls GET /workspaces/{id}/workspace-role-skills (WRSKL-01)');
  it.todo('generateWorkspaceSkill calls POST /workspaces/{id}/workspace-role-skills (WRSKL-01)');
  it.todo('activateWorkspaceSkill calls POST /workspaces/{id}/workspace-role-skills/{skillId}/activate (WRSKL-02)');
  it.todo('deleteWorkspaceSkill calls DELETE /workspaces/{id}/workspace-role-skills/{skillId} (WRSKL-01)');
});
```

No imports from not-yet-existing modules.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/ai/agents/test_role_skill_materializer.py --co -q 2>&1 | tail -10 && cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm test --reporter=verbose --run src/features/settings/components/__tests__/workspace-skill-card.test.tsx src/services/api/__tests__/workspace-role-skills.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>2 new xfail materializer stubs collected. Frontend 5 component it.todo() stubs + 4 API client it.todo() stubs collected. Zero import errors.</done>
</task>

</tasks>

<verification>
cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/repositories/test_workspace_role_skill_repository.py tests/unit/services/test_workspace_role_skill_service.py tests/unit/api/test_workspace_role_skills_router.py tests/unit/ai/agents/test_role_skill_materializer.py -q 2>&1 | tail -20

All stubs: xfail status, zero failures, zero errors.
</verification>

<success_criteria>
- 13 backend xfail stubs collected across 3 new files
- 2 materializer xfail stubs appended to existing file
- 5 frontend component it.todo() stubs collected
- 4 frontend API client it.todo() stubs collected
- `pnpm test` and `pytest --co` both succeed without errors
- No stub imports from modules that don't exist yet
</success_criteria>

<output>
After completion, create `.planning/phases/016-workspace-role-skills/016-01-SUMMARY.md`
</output>
