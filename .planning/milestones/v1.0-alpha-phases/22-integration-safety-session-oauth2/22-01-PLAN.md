---
phase: 22-integration-safety-session-oauth2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/pilot_space/api/v1/routers/workspaces.py
  - backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py
  - backend/tests/unit/services/test_seed_plugins_service.py
  - backend/tests/api/test_workspace_mcp_servers.py
autonomous: true
requirements:
  - SKRG-05
  - MCP-03

must_haves:
  truths:
    - "SeedPluginsService background task uses its own DB session, not the request-scoped session"
    - "Background plugin seeding is non-fatal: exceptions are caught and logged, not propagated"
    - "OAuth callback redirect includes workspace slug prefix so Next.js App Router resolves the route"
  artifacts:
    - path: "backend/src/pilot_space/api/v1/routers/workspaces.py"
      provides: "Independent session background task for SeedPluginsService"
      contains: "get_db_session"
    - path: "backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py"
      provides: "workspace_slug in Redis state data and redirect URL"
      contains: "workspace_slug"
    - path: "backend/tests/unit/services/test_seed_plugins_service.py"
      provides: "Tests verifying independent session usage"
    - path: "backend/tests/api/test_workspace_mcp_servers.py"
      provides: "Tests verifying OAuth callback redirect includes workspace slug"
  key_links:
    - from: "backend/src/pilot_space/api/v1/routers/workspaces.py"
      to: "pilot_space.infrastructure.database.get_db_session"
      via: "async context manager in background task"
      pattern: "async with get_db_session\\(\\) as"
    - from: "backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py (get_mcp_oauth_url)"
      to: "backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py (mcp_oauth_callback)"
      via: "workspace_slug stored in Redis state_data, read back in callback"
      pattern: "workspace_slug"
---

<objective>
Fix the SeedPluginsService session-sharing race condition and the OAuth callback redirect missing workspace slug.

Purpose: Close SKRG-05 (session safety) and the backend half of MCP-03 (OAuth redirect correctness).
Output: Safe background task pattern in workspaces.py, workspace-slug-aware OAuth redirect in workspace_mcp_servers.py, tests for both.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-integration-safety-session-oauth2/22-RESEARCH.md

@backend/src/pilot_space/api/v1/routers/workspaces.py
@backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py
@backend/src/pilot_space/infrastructure/database/engine.py
@backend/tests/unit/services/test_seed_plugins_service.py
@backend/tests/api/test_workspace_mcp_servers.py

<interfaces>
<!-- Key contracts the executor needs. -->

From backend/src/pilot_space/infrastructure/database/engine.py (lines 112-129):
```python
@asynccontextmanager
async def get_db_session() -> AsyncGenerator[AsyncSession, None]:
    """Independent session lifecycle — auto-commits on success, rollbacks on exception."""
```

From backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py:
```python
async def _get_admin_workspace(workspace_id, current_user, session) -> Workspace:
    # Returns workspace model with .slug attribute

class McpOAuthUrlResponse(BaseModel):
    auth_url: str
    state: str

# state_data stored in Redis:
state_data = {"server_id": str, "workspace_id": str, "nonce": str}
# Missing: "workspace_slug" — this plan adds it

# redirect_base in mcp_oauth_callback:
redirect_base = "/settings/mcp-servers"  # BUG: missing /{workspace_slug} prefix
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix SeedPluginsService session-sharing race condition</name>
  <files>backend/src/pilot_space/api/v1/routers/workspaces.py, backend/tests/unit/services/test_seed_plugins_service.py</files>
  <behavior>
    - Test: _seed_workspace_background calls get_db_session() to create an independent session (not the request session)
    - Test: _seed_workspace_background catches all exceptions and logs them (non-fatal contract)
    - Test: The background task function is called via asyncio.create_task with only workspace_id (no session arg)
  </behavior>
  <action>
1. In `workspaces.py`, replace lines 145-156 with:
   - Define `_seed_workspace_background(workspace_id: UUID)` as a module-level async function
   - Inside: `from pilot_space.infrastructure.database import get_db_session` (lazy import)
   - Use `async with get_db_session() as bg_session:` for independent session lifecycle
   - Create `SeedPluginsService(db_session=bg_session)` and call `.seed_workspace(workspace_id=workspace_id)`
   - Wrap the entire body (including session creation) in `try/except Exception` with `_logger.exception(...)` for non-fatal error handling
   - Call `asyncio.create_task(_seed_workspace_background(workspace.id))` with `# noqa: RUF006`
   - Add `from pilot_space.infrastructure.logging import get_logger` and `_logger = get_logger(__name__)` at module level (check if already present; if so, reuse existing logger)

2. In `test_seed_plugins_service.py`, add new test cases:
   - `test_seed_workspace_background_uses_independent_session`: Mock `get_db_session` to verify it is called (proving independent session, not request session)
   - `test_seed_workspace_background_non_fatal_on_exception`: Mock `get_db_session` to raise, verify no exception propagates out of the background task, verify logger.exception was called

Pattern reference: `workspace_mcp_servers.py` lines 566-573 (mcp_oauth_callback) already uses this exact `get_db_session()` pattern for background work.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/services/test_seed_plugins_service.py -x -v</automated>
  </verify>
  <done>SeedPluginsService background task uses get_db_session() for independent session. All exceptions are caught and logged. No request-scoped session is passed to create_task. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix OAuth callback redirect to include workspace slug</name>
  <files>backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py, backend/tests/api/test_workspace_mcp_servers.py</files>
  <behavior>
    - Test: get_mcp_oauth_url stores workspace_slug in Redis state_data alongside server_id and workspace_id
    - Test: mcp_oauth_callback reads workspace_slug from state_data and uses it in redirect_base as /{slug}/settings/mcp-servers
    - Test: mcp_oauth_callback falls back to /settings/mcp-servers if workspace_slug is missing from state_data (backward compat)
  </behavior>
  <action>
1. In `get_mcp_oauth_url` (line 438):
   - Capture return value: `workspace = await _get_admin_workspace(workspace_id, current_user, session)` (currently discards return)
   - Add `"workspace_slug": workspace.slug` to `state_data` dict (line 468-472)

2. In `mcp_oauth_callback` (line 534):
   - After parsing `state_data` (line 556-558), extract: `workspace_slug = state_data.get("workspace_slug", "")`
   - Replace hardcoded `redirect_base = "/settings/mcp-servers"` with:
     ```python
     workspace_slug = state_data.get("workspace_slug", "")
     redirect_base = f"/{workspace_slug}/settings/mcp-servers" if workspace_slug else "/settings/mcp-servers"
     ```
   - Move `redirect_base` assignment to AFTER state_data parsing (currently it's at line 534, before parsing). For error paths before state parsing (lines 536-547), keep using `"/settings/mcp-servers"` as the fallback.

3. In `test_workspace_mcp_servers.py`:
   - Find existing xfail OAuth tests and update them: remove xfail markers, add assertions that redirect URL contains workspace slug
   - Add test: `test_oauth_callback_redirect_includes_workspace_slug` — mock Redis to return state_data with workspace_slug, verify RedirectResponse URL starts with `/{slug}/settings/mcp-servers`
   - Add test: `test_oauth_callback_redirect_fallback_without_slug` — mock Redis state_data without workspace_slug, verify redirect falls back to `/settings/mcp-servers`
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/api/test_workspace_mcp_servers.py -x -v -k oauth</automated>
  </verify>
  <done>OAuth state_data includes workspace_slug. Callback redirect URL uses /{slug}/settings/mcp-servers. Fallback works for legacy state without slug. All OAuth tests pass (no xfail stubs).</done>
</task>

</tasks>

<verification>
```bash
cd /Users/tindang/workspaces/tind-repo/pilot-space && make quality-gates-backend
```
</verification>

<success_criteria>
- SeedPluginsService background task uses independent session (grep confirms `get_db_session` in workspaces.py background function)
- OAuth callback redirects to `/{workspace_slug}/settings/mcp-servers?status=connected` (not `/settings/mcp-servers`)
- All backend tests pass including new test cases
- Quality gates pass (pyright + ruff + pytest --cov)
</success_criteria>

<output>
After completion, create `.planning/phases/22-integration-safety-session-oauth2/22-01-SUMMARY.md`
</output>
