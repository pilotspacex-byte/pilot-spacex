---
phase: 04-ai-governance
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - backend/src/pilot_space/infrastructure/database/repositories/audit_log_repository.py
  - backend/src/pilot_space/api/v1/routers/audit.py
autonomous: true
requirements:
  - AIGOV-03

must_haves:
  truths:
    - "AuditLogRepository.list_filtered() accepts actor_type parameter and filters correctly"
    - "AuditLogRepository.list_for_export() accepts actor_type parameter for consistency"
    - "GET /workspaces/{slug}/audit?actor_type=AI returns only AI actor rows"
    - "actor_type query param is plumbed from router → repository WHERE clause (not silently ignored)"
  artifacts:
    - path: "backend/src/pilot_space/infrastructure/database/repositories/audit_log_repository.py"
      provides: "list_filtered() + list_for_export() with actor_type param"
      contains: "actor_type"
    - path: "backend/src/pilot_space/api/v1/routers/audit.py"
      provides: "actor_type query param on GET /audit and GET /audit/export"
      contains: "actor_type"
  key_links:
    - from: "audit.py GET /audit"
      to: "audit_log_repository.list_filtered()"
      via: "actor_type=actor_type kwarg"
      pattern: "actor_type=actor_type"
    - from: "audit.py GET /audit/export"
      to: "audit_log_repository.list_for_export()"
      via: "actor_type=actor_type kwarg"
      pattern: "actor_type=actor_type"
---

<objective>
Extend the audit log repository and router to support actor_type filtering — the backend half of the AI audit trail (AIGOV-03). This is a small, focused plan: add one parameter to two repository methods and thread it through the router.

Purpose: Enable the frontend to filter `actor_type = AI` in the audit log. The AI fields (ai_rationale, ai_model, ai_token_cost) are already in the audit_log table from Phase 2 — we just need the filter parameter.
Output: actor_type parameter live in repository and router; existing audit tests still pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-governance/04-CONTEXT.md
@.planning/phases/04-ai-governance/04-RESEARCH.md
@.planning/phases/04-ai-governance/04-01-SUMMARY.md

<interfaces>
<!-- Current signatures to extend. From direct codebase reading in RESEARCH.md. -->

Current list_filtered() signature (to add actor_type to):
```python
async def list_filtered(
    self,
    *,
    workspace_id: uuid.UUID,
    actor_id: uuid.UUID | None = None,
    # actor_type: ActorType | None = None,   ← ADD THIS
    action: str | None = None,
    resource_type: str | None = None,
    start_date: datetime | None = None,
    end_date: datetime | None = None,
    cursor: str | None = None,
    page_size: int = 50,
) -> AuditLogPage: ...
```

ActorType enum (already exists in models/audit_log.py):
```python
class ActorType(str, enum.Enum):
    USER = "USER"
    AI = "AI"
    SYSTEM = "SYSTEM"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add actor_type filter to AuditLogRepository</name>
  <files>
    backend/src/pilot_space/infrastructure/database/repositories/audit_log_repository.py,
    tests/unit/repositories/test_audit_log_repository.py
  </files>
  <behavior>
    - test_list_filtered_actor_type_ai: query with actor_type=AI returns only rows where AuditLog.actor_type == "AI"
    - test_list_filtered_actor_type_user: query with actor_type=USER excludes AI rows
    - test_list_for_export_actor_type_filter: list_for_export with actor_type=AI filters same as list_filtered
    - test_list_filtered_no_actor_type: omitting actor_type param returns all rows (no filter applied)
  </behavior>
  <action>
Read `backend/src/pilot_space/infrastructure/database/repositories/audit_log_repository.py` in full before editing.

Step 1 — Add `actor_type: ActorType | None = None` parameter to `list_filtered()`. Add the WHERE clause:
```python
if actor_type is not None:
    stmt = stmt.where(AuditLog.actor_type == actor_type)
```
Place this filter in the same block as the existing `actor_id`, `action`, `resource_type` filters (maintain logical grouping).

Step 2 — Add `actor_type: ActorType | None = None` parameter to `list_for_export()`. Add the same WHERE clause pattern. Confirm the `list_for_export()` method exists (from Phase 2 AUDIT-04) — read it if needed.

Step 3 — Implement the xfail stubs in `tests/unit/repositories/test_audit_log_repository.py` (replacing `pass` with real test bodies). Use `db_session` fixture. Create two `AuditLog` rows — one with `actor_type=AI`, one with `actor_type=USER` — then assert filter returns only the correct one.

Note: Tests run against SQLite in-memory by default (no RLS). For actor_type filter tests this is fine — filter is a simple WHERE clause, not RLS-dependent.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/repositories/test_audit_log_repository.py -x -q</automated>
  </verify>
  <done>All 4 audit repository tests pass; list_filtered and list_for_export both accept actor_type</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire actor_type through audit router</name>
  <files>
    backend/src/pilot_space/api/v1/routers/audit.py,
    tests/unit/routers/test_audit.py
  </files>
  <behavior>
    - test_audit_list_actor_type_param_passed_to_repo: GET /audit?actor_type=AI → repo.list_filtered called with actor_type=ActorType.AI
    - test_audit_export_actor_type_param_passed_to_repo: GET /audit/export?actor_type=AI → repo.list_for_export called with actor_type=ActorType.AI
    - test_audit_list_invalid_actor_type_returns_422: actor_type=INVALID → FastAPI returns 422 validation error
  </behavior>
  <action>
Read `backend/src/pilot_space/api/v1/routers/audit.py` in full before editing.

Step 1 — Add `actor_type: ActorType | None = None` as a Query parameter to the GET `/workspaces/{slug}/audit` endpoint. FastAPI will validate the enum value automatically.

Step 2 — Pass `actor_type=actor_type` to the `await repo.list_filtered(...)` call.

Step 3 — Add the same `actor_type: ActorType | None = None` parameter to the GET `/workspaces/{slug}/audit/export` endpoint and pass it to `await repo.list_for_export(...)`.

Step 4 — Implement the xfail stubs in `tests/unit/routers/test_audit.py` with real test bodies. Use FastAPI `TestClient` (or `AsyncClient` if the router uses async). Mock the `AuditLogRepository.list_filtered` at the dependency level. Assert `mock_repo.list_filtered.assert_called_with(..., actor_type=ActorType.AI)`.

Step 5 — Run existing audit router tests to confirm no regression:
```bash
cd backend && uv run pytest tests/unit/routers/test_audit.py -q
```
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/routers/test_audit.py -x -q</automated>
  </verify>
  <done>All audit router tests pass; actor_type query param live on GET /audit and GET /audit/export; 422 returned for invalid values</done>
</task>

</tasks>

<verification>
- `grep -n "actor_type" backend/src/pilot_space/infrastructure/database/repositories/audit_log_repository.py | grep "def list_"` shows both list_filtered and list_for_export accept actor_type
- `grep -n "actor_type" backend/src/pilot_space/api/v1/routers/audit.py` shows Query param in both endpoints
- `cd backend && uv run pytest tests/unit/repositories/ tests/unit/routers/test_audit.py -q` all pass
</verification>

<success_criteria>
actor_type filter fully plumbed from router through repository WHERE clause; both list and export endpoints support it; 5 new tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-governance/04-03-SUMMARY.md`
</output>
