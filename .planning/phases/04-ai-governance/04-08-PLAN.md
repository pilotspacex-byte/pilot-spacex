---
phase: 04-ai-governance
plan: "08"
type: execute
wave: 5
depends_on:
  - "04-05"
  - "04-06"
  - "04-07"
files_modified: []
autonomous: false
requirements:
  - AIGOV-01
  - AIGOV-02
  - AIGOV-03
  - AIGOV-04
  - AIGOV-05
  - AIGOV-06
  - AIGOV-07

must_haves:
  truths:
    - "Admin can configure policy matrix and AI respects it at runtime"
    - "Pending approval requests appear on the Approvals page with Approve/Reject working"
    - "AI audit trail shows AI rows with rationale, model, cost when actor_type=AI filter applied"
    - "Rollback button appears on eligible AI audit rows and restores the artifact"
    - "AI features disabled with clear message when no BYOK key configured"
    - "Cost dashboard By Feature tab shows operation_type breakdown"
    - "Rationale popover shows in ExtractionReviewPanel and PR review"
  artifacts:
    - path: "frontend/src/app/(workspace)/[workspaceSlug]/approvals/page.tsx"
      provides: "Approvals page (human verification)"
    - path: "frontend/src/features/settings/pages/ai-governance-settings-page.tsx"
      provides: "Policy matrix (human verification)"
  key_links:
    - from: "All Phase 4 backend tests"
      to: "Green CI status"
      via: "uv run pytest --cov"
      pattern: "passed"
    - from: "All Phase 4 frontend"
      to: "Clean TypeScript + lint"
      via: "pnpm type-check && pnpm lint"
      pattern: "0 errors"
---

<objective>
Final quality gate and human verification checkpoint for Phase 4. Run full backend + frontend test suites, verify quality gates pass, then present a structured verification checklist for manual browser testing of all 7 AIGOV requirements.

Purpose: Confirm all Phase 4 requirements are met before marking the phase complete.
Output: Phase gate green; human confirmation of each AIGOV requirement.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-governance/04-CONTEXT.md
@.planning/phases/04-ai-governance/04-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full quality gates — backend + frontend</name>
  <files></files>
  <action>
Run all quality gates and fix any failures before presenting the human verification checkpoint.

Backend:
```bash
cd /Users/tindang/workspaces/tind-repo/pilot-space/backend
uv run ruff check src/                     # Lint
uv run pyright                             # Type check
uv run pytest tests/unit/ai/ tests/unit/routers/ tests/unit/repositories/ tests/unit/infrastructure/ --cov -q  # Tests
alembic heads                              # Single head at 069
alembic check                              # Head matches models
```

Frontend:
```bash
cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend
pnpm lint                                  # ESLint
pnpm type-check                            # tsc --noEmit
pnpm test                                  # Vitest (if any frontend tests added)
```

Fix any failures found. Common issues at this stage:
- Import cycle from new files not registered in `__init__.py`
- Pyright errors from Optional type changes after CostTracker Factory fix
- ESLint `no-unused-vars` on new hooks
- Missing `@/` path aliases on new component imports

Report results: N tests passed, N warnings, 0 errors (or list remaining issues).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/ai/ tests/unit/routers/ tests/unit/repositories/ -q --tb=short 2>&1 | tail -20 && cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <done>Backend tests pass with coverage ≥ 80%; frontend type-checks with 0 errors; lint clean on both</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4: AI Governance — all 7 AIGOV requirements implemented across 7 plans:
- AIGOV-01: Per-role × per-action-type policy matrix (Settings > AI Governance)
- AIGOV-02: Approval queue page with Approve/Reject workflow
- AIGOV-03: AI audit trail with actor_type=AI filter and AI row expansion
- AIGOV-04: Rollback button on eligible AI audit rows
- AIGOV-05: BYOK enforcement — AiNotConfiguredBanner + disabled AI controls
- AIGOV-06: Cost dashboard By Feature tab (operation_type breakdown)
- AIGOV-07: Rationale popovers in ExtractionReviewPanel + PR review Collapsible
  </what-built>
  <how-to-verify>
Start the dev servers:
```bash
cd backend && uv run uvicorn pilot_space.main:app --reload --port 8000
cd frontend && pnpm dev
```
Open http://localhost:3000 and log in as workspace owner (e2e-test@pilotspace.dev / TestPassword123!).

**AIGOV-01: Policy matrix**
1. Navigate to Settings > AI Governance
2. Confirm matrix table appears with action type rows grouped (Content/Code/Administrative/Always Required)
3. Confirm Owner column is greyed-out "Auto" (non-editable)
4. Confirm ALWAYS_REQUIRE rows show "Always" locked in all cells
5. Toggle a cell (e.g., Member × CREATE_ISSUE to "Approval") — confirm it saves and persists on page reload

**AIGOV-02: Approval queue**
1. Navigate to /[workspaceSlug]/approvals
2. Confirm page loads with Pending/Expired tabs
3. Confirm sidebar shows Approvals nav item (Owner/Admin only)
4. If pending items exist: expand a row, confirm payload and rationale shown, Approve/Reject buttons work

**AIGOV-03: AI audit trail**
1. Navigate to Settings > Audit
2. Find Actor Type filter — select "AI"
3. Confirm only AI-authored rows appear
4. Expand an AI row — confirm Model, Token cost, AI rationale fields visible

**AIGOV-04: Rollback**
1. In Settings > Audit, find an AI row for an issue create or update action
2. Expand the row — confirm "Rollback" button appears
3. Click Rollback — confirm confirmation dialog shows what will be restored
4. Approve rollback — confirm the artifact is restored and a new audit entry with action="ai.rollback" appears

**AIGOV-05: BYOK enforcement**
1. Remove the workspace API key (Settings > API Keys — delete or deactivate)
2. Confirm AiNotConfiguredBanner appears at top of workspace (Owner only)
3. Confirm banner links to Settings > API Keys
4. Dismiss banner — confirm it doesn't reappear until next page reload
5. Confirm AI controls (ghost text, slash commands, PR review) show disabled state with tooltip
6. Log in as a Member — confirm no banner appears (controls simply disabled)

**AIGOV-06: Cost dashboard**
1. Navigate to /[workspaceSlug]/costs
2. Find "By Feature" tab — click it
3. Confirm bar chart renders showing operation_type breakdown (or empty state if no AI calls recorded)

**AIGOV-07: Rationale display**
1. Open a note with the extraction panel visible (create extraction if needed)
2. Confirm "ⓘ" icon appears on each extracted issue card
3. Click the icon — confirm Popover opens with rationale text or "No rationale available"
4. Open a PR review — confirm "AI reasoning" collapsible appears on review comments
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific failures</resume-signal>
</task>

</tasks>

<verification>
- Backend: `uv run pytest --cov` passes with ≥ 80% coverage
- Frontend: `pnpm type-check && pnpm lint` exits 0
- Human verification: all 7 AIGOV requirements confirmed in browser
</verification>

<success_criteria>
Phase 4 complete: all AIGOV-01 through AIGOV-07 requirements verified; quality gates green; human approves browser verification.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-governance/04-08-SUMMARY.md`
Update ROADMAP.md: Phase 4 status → Complete with today's date.
Update STATE.md: phase 4 complete, progress 4/5 phases.
</output>
