---
phase: 20-skill-template-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/pilot_space/infrastructure/database/models/skill_template.py
  - backend/src/pilot_space/infrastructure/database/models/user_skill.py
  - backend/src/pilot_space/infrastructure/database/models/__init__.py
  - backend/src/pilot_space/infrastructure/database/repositories/skill_template_repository.py
  - backend/src/pilot_space/infrastructure/database/repositories/user_skill_repository.py
  - backend/alembic/versions/077_add_skill_templates_and_user_skills.py
  - backend/tests/unit/repositories/test_skill_template_repository.py
  - backend/tests/unit/repositories/test_user_skill_repository.py
autonomous: true
requirements:
  - P20-01
  - P20-02
  - P20-03

must_haves:
  truths:
    - "skill_templates table exists with workspace_id FK, source enum, nullable role_type"
    - "user_skills table exists with user_id, template_id FK (nullable), no role_type column"
    - "Data migration copies RoleTemplate rows to skill_templates (source=built_in) per workspace"
    - "Data migration copies WorkspaceRoleSkill rows to skill_templates (source=workspace)"
    - "Data migration copies UserRoleSkill rows to user_skills with template_id linked by role_type"
    - "Old tables are NOT dropped"
  artifacts:
    - path: "backend/src/pilot_space/infrastructure/database/models/skill_template.py"
      provides: "SkillTemplate SQLAlchemy model"
      contains: "class SkillTemplate"
    - path: "backend/src/pilot_space/infrastructure/database/models/user_skill.py"
      provides: "UserSkill SQLAlchemy model"
      contains: "class UserSkill"
    - path: "backend/alembic/versions/077_add_skill_templates_and_user_skills.py"
      provides: "Schema creation + data migration"
      contains: "skill_templates"
    - path: "backend/src/pilot_space/infrastructure/database/repositories/skill_template_repository.py"
      provides: "CRUD repository for skill templates"
      exports: ["SkillTemplateRepository"]
    - path: "backend/src/pilot_space/infrastructure/database/repositories/user_skill_repository.py"
      provides: "CRUD repository for user skills"
      exports: ["UserSkillRepository"]
  key_links:
    - from: "077_add_skill_templates_and_user_skills.py"
      to: "skill_templates table"
      via: "op.create_table + op.execute data migration"
      pattern: "INSERT INTO skill_templates"
    - from: "SkillTemplate model"
      to: "WorkspaceScopedModel"
      via: "class inheritance"
      pattern: "class SkillTemplate\\(WorkspaceScopedModel\\)"
---

<objective>
Create the SkillTemplate and UserSkill persistence layer: SQLAlchemy models, Alembic migration with data migration, and CRUD repositories.

Purpose: Foundation tables that all subsequent plans depend on. Data migration ensures existing role-based skills are preserved in the new decoupled system.
Output: Two new models, one migration (schema + data), two repositories, passing unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-migrate-all-role-skill-template-of-each-role-then-user-setup-skill-can-pick-template-include-role-template-skill-do-not-depend-on-role/20-RESEARCH.md

@backend/src/pilot_space/infrastructure/database/base.py
@backend/src/pilot_space/infrastructure/database/models/workspace_role_skill.py
@backend/src/pilot_space/infrastructure/database/models/user_role_skill.py
@backend/src/pilot_space/infrastructure/database/repositories/workspace_role_skill_repository.py
@backend/src/pilot_space/infrastructure/database/repositories/role_skill_repository.py
@backend/alembic/versions/073_add_workspace_role_skills.py
@backend/src/pilot_space/infrastructure/database/rls.py

<interfaces>
<!-- Existing base classes and patterns the executor needs -->

From base.py:
```python
class WorkspaceScopedModel(Base):
    # Includes: id (UUID PK), workspace_id (FK), created_at, updated_at, is_deleted, deleted_at
    # via TimestampMixin, WorkspaceScopedMixin, SoftDeleteMixin
```

From workspace_role_skill.py (pattern reference for SkillTemplate):
```python
class WorkspaceRoleSkill(WorkspaceScopedModel):
    __tablename__ = "workspace_role_skills"
    role_type: Mapped[str] = mapped_column(String(50), nullable=False)
    role_name: Mapped[str] = mapped_column(String(100), nullable=False)
    skill_content: Mapped[str] = mapped_column(Text, nullable=False)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False, server_default=text("false"))
    created_by: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("users.id", ondelete="SET NULL"), nullable=True)
```

From rls.py:
```python
def get_workspace_rls_policy_sql(table_name: str) -> list[str]:
    # Returns list of SQL statements for RLS policies
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SkillTemplate + UserSkill models and repositories</name>
  <files>
    backend/src/pilot_space/infrastructure/database/models/skill_template.py,
    backend/src/pilot_space/infrastructure/database/models/user_skill.py,
    backend/src/pilot_space/infrastructure/database/models/__init__.py,
    backend/src/pilot_space/infrastructure/database/repositories/skill_template_repository.py,
    backend/src/pilot_space/infrastructure/database/repositories/user_skill_repository.py,
    backend/tests/unit/repositories/test_skill_template_repository.py,
    backend/tests/unit/repositories/test_user_skill_repository.py
  </files>
  <behavior>
    - SkillTemplate model: name (varchar 100), description (text), skill_content (text, max 15000), icon (varchar 50, default 'Wand2'), sort_order (int, default 0), source (varchar 20, NOT NULL), role_type (varchar 50, nullable), is_active (bool, default true), created_by (UUID FK users.id SET NULL, nullable)
    - SkillTemplate: partial unique index on (workspace_id, name) WHERE is_deleted = false
    - SkillTemplate: indexes on (workspace_id, source) and (workspace_id, is_active)
    - SkillTemplate: CheckConstraint length(skill_content) <= 15000, length(name) <= 100
    - UserSkill model: user_id (UUID FK users.id CASCADE), template_id (UUID FK skill_templates.id SET NULL, nullable), skill_content (text, max 15000), experience_description (text, nullable), is_active (bool, default true)
    - UserSkill: partial unique index on (user_id, workspace_id, template_id) WHERE is_deleted = false
    - UserSkill: indexes on (user_id, workspace_id) and (workspace_id)
    - SkillTemplateRepository.get_active_by_workspace(workspace_id) returns active, non-deleted templates ordered by sort_order
    - SkillTemplateRepository.get_by_workspace(workspace_id) returns all non-deleted templates
    - SkillTemplateRepository.create(...) creates a new template
    - UserSkillRepository.get_by_user_workspace(user_id, workspace_id) returns active, non-deleted user skills
    - UserSkillRepository.get_by_user_workspace_template(user_id, workspace_id, template_id) returns specific skill or None
    - UserSkillRepository.create(...) creates a new user skill
  </behavior>
  <action>
    1. Create `skill_template.py` model following WorkspaceRoleSkill pattern exactly. Use `length()` not `char_length()` for CheckConstraints (SQLite compat). Use `server_default=text("true")` for is_active (different from WorkspaceRoleSkill which defaults false).
    2. Create `user_skill.py` model following UserRoleSkill pattern. Add `relationship("SkillTemplate", lazy="joined")` for template. Add `relationship("User", lazy="joined")` for user.
    3. Add both model imports to `models/__init__.py`.
    4. Create `SkillTemplateRepository` with methods: `get_active_by_workspace`, `get_by_workspace`, `create`, `update`, `soft_delete`. Follow WorkspaceRoleSkillRepository pattern (direct instantiation, not DI).
    5. Create `UserSkillRepository` with methods: `get_by_user_workspace`, `get_by_user_workspace_template`, `create`, `update`, `soft_delete`. Follow RoleSkillRepository pattern.
    6. Write tests using `db_session` fixture. Use xfail for tests that need PostgreSQL features (partial unique indexes). Test CRUD operations for both repositories.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/repositories/test_skill_template_repository.py tests/unit/repositories/test_user_skill_repository.py -x -v</automated>
  </verify>
  <done>Both models exist with correct columns, constraints, and indexes. Both repositories pass CRUD unit tests. Models registered in __init__.py.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration 077 with schema creation and data migration</name>
  <files>
    backend/alembic/versions/077_add_skill_templates_and_user_skills.py
  </files>
  <action>
    1. Run `cd backend && alembic heads` to verify single head (should be 076).
    2. Create migration `077_add_skill_templates_and_user_skills.py` with `Revises:` pointing to 076 head.
    3. `upgrade()`:
       a. Create `skill_templates` table with all columns matching the model (id UUID PK, workspace_id FK, name, description, skill_content, icon, sort_order, source, role_type, is_active, created_by FK, created_at, updated_at, is_deleted, deleted_at).
       b. Create all indexes: partial unique (workspace_id, name) WHERE is_deleted=false, (workspace_id, source), (workspace_id, is_active).
       c. Create CheckConstraints: length(skill_content) <= 15000, length(name) <= 100.
       d. Enable RLS + add policies using `get_workspace_rls_policy_sql('skill_templates')` pattern from migration 073.
       e. Create `user_skills` table with all columns (id UUID PK, user_id FK users.id CASCADE, workspace_id FK, template_id FK skill_templates.id SET NULL, skill_content, experience_description, is_active, created_at, updated_at, is_deleted, deleted_at).
       f. Create all indexes: partial unique (user_id, workspace_id, template_id) WHERE is_deleted=false, (user_id, workspace_id), (workspace_id).
       g. Create CheckConstraint: length(skill_content) <= 15000.
       h. Enable RLS + add policies for user_skills.
       i. Data migration using raw SQL via `op.execute(text(...))`:
          - Copy RoleTemplate rows to skill_templates per workspace (CROSS JOIN workspace_members for distinct workspace_ids, source='built_in')
          - Copy WorkspaceRoleSkill rows to skill_templates (source='workspace')
          - Copy UserRoleSkill rows to user_skills with template_id linked via LEFT JOIN on (workspace_id, role_type, source='built_in')
       j. Use `gen_random_uuid()` for new IDs.
    4. `downgrade()`:
       a. Drop user_skills table (including RLS policies).
       b. Drop skill_templates table (including RLS policies).
       c. Do NOT touch old tables.
    5. Validate: `cd backend && alembic heads` (single head) and `alembic check`.

    CRITICAL: Do NOT use Python ORM in migration. Use raw SQL via `op.execute(text(...))` for all data migration. Do NOT drop old tables.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && alembic heads && alembic check</automated>
  </verify>
  <done>Migration 077 creates both tables with RLS, indexes, constraints. Data migration copies existing role-based data. Single Alembic head. Old tables untouched.</done>
</task>

</tasks>

<verification>
- `cd backend && alembic heads` shows single head at 077
- `cd backend && alembic check` passes (models match head)
- `cd backend && uv run pytest tests/unit/repositories/test_skill_template_repository.py tests/unit/repositories/test_user_skill_repository.py -x -v` all green
- `cd backend && uv run ruff check src/pilot_space/infrastructure/database/models/skill_template.py src/pilot_space/infrastructure/database/models/user_skill.py` no errors
- `cd backend && uv run pyright src/pilot_space/infrastructure/database/models/skill_template.py src/pilot_space/infrastructure/database/models/user_skill.py` no errors
</verification>

<success_criteria>
- SkillTemplate model: workspace-scoped, source enum field, nullable role_type, is_active defaults true
- UserSkill model: user_id + template_id (nullable FK), no role_type, no is_primary
- Migration 077: schema + data migration in single file, RLS policies, no old table drops
- Both repositories: CRUD operations with workspace-scoping
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-migrate-all-role-skill-template-of-each-role-then-user-setup-skill-can-pick-template-include-role-template-skill-do-not-depend-on-role/20-01-SUMMARY.md`
</output>
