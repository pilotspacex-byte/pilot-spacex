---
phase: 07-wire-storage-quota-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/services/test_storage_quota_wiring.py
autonomous: true
requirements:
  - TENANT-03
gap_closure: true

must_haves:
  truths:
    - "Test file exists with failing stubs that drive the implementation in plan 02"
    - "All 7 TENANT-03 wiring behaviors have named test functions"
    - "Running the test file produces FAILED (not error/syntax) status for all tests"
  artifacts:
    - path: "backend/tests/unit/services/test_storage_quota_wiring.py"
      provides: "Unit test stubs for quota wiring across issue create, issue update, note create, note update, attachment upload"
      min_lines: 80
  key_links:
    - from: "backend/tests/unit/services/test_storage_quota_wiring.py"
      to: "backend/src/pilot_space/api/v1/routers/workspace_issues.py"
      via: "patches _check_storage_quota in workspace_issues module"
      pattern: "patch.*workspace_issues.*_check_storage_quota"
    - from: "backend/tests/unit/services/test_storage_quota_wiring.py"
      to: "backend/src/pilot_space/api/v1/routers/workspace_notes.py"
      via: "patches _check_storage_quota in workspace_notes module"
      pattern: "patch.*workspace_notes.*_check_storage_quota"
---

<objective>
Create the test scaffold for Phase 7 storage quota wiring. All tests start FAILING (red) to drive the router-level wiring in plan 02.

Purpose: Nyquist compliance requires tests to exist before implementation. The stubs define the contract the implementation must satisfy.
Output: `tests/unit/services/test_storage_quota_wiring.py` with 7 failing tests covering 507 blocks, X-Storage-Warning headers, and _update_storage_usage call verification across all write paths.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-wire-storage-quota-enforcement/07-RESEARCH.md
@.planning/phases/07-wire-storage-quota-enforcement/07-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts the test file must patch. Extracted from codebase. -->
<!-- Executor: use these patch targets directly — no exploration needed. -->

From workspace_quota.py (the helpers under test):
```python
async def _check_storage_quota(
    session: AsyncSession,
    workspace_id: UUID,
    delta_bytes: int,
) -> tuple[bool, float | None]:
    # Returns:
    # (True, None)  — allowed, no warning
    # (True, pct)   — allowed, pct >= 0.80 → caller adds X-Storage-Warning header
    # (False, None) — blocked, caller raises HTTP 507

async def _update_storage_usage(
    session: AsyncSession,
    workspace_id: UUID,
    delta_bytes: int,
) -> None:
    # Atomically increments storage_used_bytes by delta_bytes
```

Patch targets (module-level imports in the routers):
- `pilot_space.api.v1.routers.workspace_issues._check_storage_quota`
- `pilot_space.api.v1.routers.workspace_issues._update_storage_usage`
- `pilot_space.api.v1.routers.workspace_notes._check_storage_quota`
- `pilot_space.api.v1.routers.workspace_notes._update_storage_usage`
- `pilot_space.api.v1.routers.ai_attachments._check_storage_quota`
- `pilot_space.api.v1.routers.ai_attachments._update_storage_usage`

Note: These patch paths will be valid only AFTER plan 02 wires the imports into those router files.
For Wave 1 (this plan), the tests will fail with ImportError or AttributeError on the patch target.
That is the expected red state — do NOT pre-import them in the test file.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test stubs for all TENANT-03 wiring behaviors</name>
  <files>backend/tests/unit/services/test_storage_quota_wiring.py</files>
  <behavior>
    - test_create_issue_507_when_quota_exceeded: patch _check_storage_quota to return (False, None) on workspace_issues module, call create_workspace_issue router function, assert HTTPException with status_code 507
    - test_create_issue_warning_header_at_80pct: patch _check_storage_quota to return (True, 0.85), call create_workspace_issue, assert response.headers["X-Storage-Warning"] == "0.85"
    - test_update_issue_507_when_quota_exceeded: patch _check_storage_quota to return (False, None) on workspace_issues module for update path, assert HTTPException 507
    - test_create_note_507_when_quota_exceeded: patch _check_storage_quota to return (False, None) on workspace_notes module, assert HTTPException 507
    - test_create_note_warning_header_at_80pct: patch _check_storage_quota to return (True, 0.82), assert response.headers["X-Storage-Warning"] set
    - test_attachment_upload_507_when_quota_exceeded: patch _check_storage_quota to return (False, None) on ai_attachments module, assert HTTPException 507
    - test_update_storage_usage_called_after_create: patch both helpers on workspace_issues, verify _update_storage_usage is called once after successful create
  </behavior>
  <action>
    Create `backend/tests/unit/services/test_storage_quota_wiring.py`.

    Use `pytest.mark.asyncio` and `unittest.mock.patch` / `AsyncMock`.

    Structure each test using the direct router function call pattern (not TestClient) — this is faster and avoids the full FastAPI app setup:

    ```python
    import pytest
    from unittest.mock import AsyncMock, MagicMock, patch

    ISSUE_MODULE = "pilot_space.api.v1.routers.workspace_issues"
    NOTE_MODULE  = "pilot_space.api.v1.routers.workspace_notes"
    ATTACH_MODULE = "pilot_space.api.v1.routers.ai_attachments"
    ```

    For each test, patch the quota helpers at the module-level path (they will exist after plan 02 wires the imports). In Wave 1, these patches will fail — that is the expected red state.

    All tests must be `async def` with `@pytest.mark.asyncio`.

    Do NOT use `pytest.mark.xfail` — these must be strict RED tests that turn GREEN when plan 02 completes.

    Do NOT add `skip` markers.

    File must be syntactically valid Python even though all tests will fail at runtime.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/services/test_storage_quota_wiring.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    File exists at `backend/tests/unit/services/test_storage_quota_wiring.py`.
    Contains 7 test functions matching the behavior list above.
    `pytest --collect-only tests/unit/services/test_storage_quota_wiring.py` collects all 7 tests without syntax errors.
    Running the tests produces FAILED status (not ERROR/syntax-error) because the patch targets don't exist yet.
  </done>
</task>

</tasks>

<verification>
cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/unit/services/test_storage_quota_wiring.py --collect-only -q
</verification>

<success_criteria>
- `tests/unit/services/test_storage_quota_wiring.py` exists with 7 test functions
- All 7 tests are collected without import/syntax errors
- Tests fail (FAILED, not ERROR) because patch targets not yet in routers
- Test names match the validation map in 07-VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/07-wire-storage-quota-enforcement/07-01-SUMMARY.md`
</output>
