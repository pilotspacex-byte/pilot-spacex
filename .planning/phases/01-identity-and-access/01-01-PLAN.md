---
phase: 01-identity-and-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/064_add_sso_rbac_session_tables.py
  - backend/src/pilot_space/infrastructure/database/models/custom_role.py
  - backend/src/pilot_space/infrastructure/database/models/workspace_session.py
  - backend/src/pilot_space/infrastructure/database/models/workspace_member.py
  - backend/tests/unit/services/test_sso_service.py
  - backend/tests/unit/services/test_rbac_service.py
  - backend/tests/unit/services/test_session_service.py
  - backend/tests/unit/routers/test_auth_sso.py
  - backend/tests/unit/routers/test_scim.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07

must_haves:
  truths:
    - "custom_roles table exists in DB with workspace_id, name, description, permissions JSONB columns"
    - "workspace_sessions table exists in DB with token_hash, ip_address, user_agent, revoked_at columns"
    - "workspace_member has nullable custom_role_id FK and is_active boolean"
    - "workspace.settings JSONB column exists (existing) and can store sso_required, scim_token_hash, sso_config, role_claim_mapping keys"
    - "All test scaffold files exist with at least one failing/pending test per requirement"
  artifacts:
    - path: "backend/alembic/versions/064_add_sso_rbac_session_tables.py"
      provides: "DB migration for all Phase 1 new tables and column additions"
      contains: "custom_roles, workspace_sessions, custom_role_id FK, is_active"
    - path: "backend/src/pilot_space/infrastructure/database/models/custom_role.py"
      provides: "CustomRole SQLAlchemy model"
      exports: ["CustomRole"]
    - path: "backend/src/pilot_space/infrastructure/database/models/workspace_session.py"
      provides: "WorkspaceSession SQLAlchemy model"
      exports: ["WorkspaceSession"]
    - path: "backend/tests/unit/services/test_sso_service.py"
      provides: "Test scaffold for SSO service (AUTH-01, AUTH-02, AUTH-03, AUTH-04)"
    - path: "backend/tests/unit/services/test_rbac_service.py"
      provides: "Test scaffold for RBAC service (AUTH-05)"
    - path: "backend/tests/unit/services/test_session_service.py"
      provides: "Test scaffold for session service (AUTH-06)"
    - path: "backend/tests/unit/routers/test_auth_sso.py"
      provides: "Test scaffold for SSO router (AUTH-04 enforcement)"
    - path: "backend/tests/unit/routers/test_scim.py"
      provides: "Test scaffold for SCIM router (AUTH-07)"
  key_links:
    - from: "custom_role.py"
      to: "workspace_member.py"
      via: "custom_role_id FK"
      pattern: "ForeignKey.*custom_roles.id"
    - from: "064_migration"
      to: "workspace_sessions table"
      via: "op.create_table"
      pattern: "workspace_sessions"
---

<objective>
Create the database foundation and test scaffolds that all other Phase 1 plans depend on.

Purpose: Every Wave 2+ plan needs models and test files to exist before implementation. Creating them first in a single migration prevents revision chain conflicts and ensures test-driven development from the start.

Output: One Alembic migration (064) creating custom_roles and workspace_sessions tables, altering workspace_members to add custom_role_id FK and is_active, plus 5 test scaffold files covering all 7 AUTH requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-and-access/01-CONTEXT.md
@.planning/phases/01-identity-and-access/01-RESEARCH.md

<interfaces>
<!-- Key patterns extracted from codebase for executor use. No exploration needed. -->

From backend/src/pilot_space/infrastructure/database/base.py:
```python
class WorkspaceScopedModel(BaseModel, WorkspaceScopedMixin):
    """Base for all workspace-scoped tables. Auto-adds workspace_id FK + RLS."""
    # Inherits: id (UUID pk), created_at, updated_at, is_deleted, deleted_at, workspace_id
```

From backend/src/pilot_space/infrastructure/database/models/workspace_member.py:
```python
class WorkspaceRole(str, Enum):
    OWNER = "owner"
    ADMIN = "admin"
    MEMBER = "member"
    GUEST = "guest"

class WorkspaceMember(BaseModel):
    __tablename__ = "workspace_members"
    # existing columns: id, user_id, workspace_id, role (WorkspaceRole), invited_by, created_at, updated_at, is_deleted
    # ADD: custom_role_id (nullable FK to custom_roles.id), is_active (bool, default True)
```

From backend/src/pilot_space/infrastructure/database/models/workspace.py:
```python
class Workspace(BaseModel):
    settings: Mapped[dict[str, Any] | None] = mapped_column(JSONB)
    # settings JSONB already exists — add keys: sso_required, sso_config, role_claim_mapping, scim_token_hash
```

RLS Policy Template (from backend/src/pilot_space/infrastructure/database/rls.py):
```python
# Always use UPPERCASE enum strings in RLS policies
# Known bug: DB stores lowercase but policies must use UPPERCASE
# Verify with: SELECT DISTINCT role FROM workspace_members LIMIT 10
```

Migration head info:
- Current head: 063_add_ci_status_and_ai_review.py
- Next migration number: 064
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CustomRole and WorkspaceSession SQLAlchemy models</name>
  <files>
    backend/src/pilot_space/infrastructure/database/models/custom_role.py
    backend/src/pilot_space/infrastructure/database/models/workspace_session.py
    backend/src/pilot_space/infrastructure/database/models/__init__.py
  </files>
  <behavior>
    - CustomRole model exports class with: id (UUID pk), workspace_id (FK), name (str 100), description (str nullable), permissions (JSONB list[str] nullable), created_at, updated_at
    - CustomRole has unique constraint on (workspace_id, name)
    - WorkspaceSession model exports class with: id (UUID pk), user_id (UUID FK to users.id), workspace_id (UUID FK to workspaces.id), session_token_hash (str 64, SHA-256 hex), ip_address (str nullable), user_agent (str nullable), last_seen_at (datetime), revoked_at (datetime nullable)
    - Both models use WorkspaceScopedModel base class (not BaseModel directly)
    - Both are exported from models/__init__.py
  </behavior>
  <action>
    Create `backend/src/pilot_space/infrastructure/database/models/custom_role.py`:
    - Extend WorkspaceScopedModel
    - `__tablename__ = "custom_roles"`
    - Columns: name (String(100), not null), description (Text, nullable), permissions (JSONBCompat mapped to list[str]|None)
    - Table args: Index("ix_custom_roles_workspace_id", "workspace_id"), UniqueConstraint("workspace_id", "name", name="uq_custom_roles_workspace_name")
    - Use `from pilot_space.infrastructure.database.base import WorkspaceScopedModel` and `from pilot_space.infrastructure.database.compat import JSONBCompat`
    - Check existing models (e.g., ai_configuration.py) for how JSONBCompat is imported

    Create `backend/src/pilot_space/infrastructure/database/models/workspace_session.py`:
    - Extend WorkspaceScopedModel (adds workspace_id FK automatically)
    - `__tablename__ = "workspace_sessions"`
    - Columns: user_id (UUID, ForeignKey("users.id"), not null), session_token_hash (String(64), not null, indexed), ip_address (String(45), nullable — supports IPv6), user_agent (Text, nullable), last_seen_at (DateTime(timezone=True), server_default=func.now()), revoked_at (DateTime(timezone=True), nullable)
    - Table args: Index("ix_workspace_sessions_token_hash", "session_token_hash"), Index("ix_workspace_sessions_user_id", "user_id")

    Update `models/__init__.py` to export both new models (follow existing pattern for how other models are exported).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run python -c "from pilot_space.infrastructure.database.models.custom_role import CustomRole; from pilot_space.infrastructure.database.models.workspace_session import WorkspaceSession; print('models OK')"</automated>
  </verify>
  <done>Both model files importable without errors; CustomRole and WorkspaceSession classes exist with all required columns.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration 064 and test scaffolds</name>
  <files>
    backend/alembic/versions/064_add_sso_rbac_session_tables.py
    backend/src/pilot_space/infrastructure/database/models/workspace_member.py
    backend/tests/unit/services/test_sso_service.py
    backend/tests/unit/services/test_rbac_service.py
    backend/tests/unit/services/test_session_service.py
    backend/tests/unit/routers/test_auth_sso.py
    backend/tests/unit/routers/test_scim.py
  </files>
  <action>
    STEP 1 — Pre-flight migration check (MANDATORY per migration skill):
    ```bash
    cd backend && alembic heads
    ```
    If multiple heads: STOP. Alert executor to resolve before continuing.
    The current single head should be the 063_* migration.

    STEP 2 — Create migration file `backend/alembic/versions/064_add_sso_rbac_session_tables.py`:
    - Set `revision = "064_add_sso_rbac_session_tables"` (use autogenerate-style naming, but write manually for precision)
    - Set `down_revision = "<current 063 revision id>"` — get exact revision ID from `alembic heads` output
    - upgrade() must:
      1. Create `custom_roles` table (id UUID pk, workspace_id UUID FK not null, name varchar(100) not null, description text nullable, permissions JSONB nullable, created_at timestamp, updated_at timestamp, is_deleted bool, deleted_at timestamp nullable; unique constraint on workspace_id+name; index on workspace_id)
      2. Create `workspace_sessions` table (id UUID pk, workspace_id UUID FK not null, user_id UUID FK not null (ref users.id), session_token_hash varchar(64) not null, ip_address varchar(45) nullable, user_agent text nullable, last_seen_at timestamptz not null default now(), revoked_at timestamptz nullable, created_at timestamp, updated_at timestamp, is_deleted bool, deleted_at timestamp nullable; index on session_token_hash; index on user_id)
      3. Add column to workspace_members: `custom_role_id UUID nullable FK → custom_roles.id` (on delete SET NULL)
      4. Add column to workspace_members: `is_active BOOLEAN not null default true`
      5. RLS for custom_roles: ENABLE + FORCE RLS; workspace isolation policy (uses current_setting('app.current_user_id') checks workspace_members); service_role bypass; use UPPERCASE 'OWNER', 'ADMIN', 'MEMBER', 'GUEST' in enum comparisons — BUT first verify actual stored values (known enum case mismatch bug, see STATE.md). Use `op.execute(text(...))` for all raw SQL.
      6. RLS for workspace_sessions: same pattern as custom_roles RLS
    - downgrade() must reverse all operations in reverse order (drop FKs first, then tables, then columns from workspace_members)

    STEP 3 — Add nullable custom_role_id FK to WorkspaceMember model:
    In `workspace_member.py`, add after existing role column:
    ```python
    custom_role_id: Mapped[uuid.UUID | None] = mapped_column(
        UUID(as_uuid=True),
        ForeignKey("custom_roles.id", ondelete="SET NULL"),
        nullable=True,
        index=True,
    )
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True, server_default="true")
    ```
    Also add relationship: `custom_role: Mapped["CustomRole | None"] = relationship("CustomRole", lazy="select")`

    STEP 4 — Create test scaffold files. Each file must have at least 3 pytest functions that use `pytest.mark.skip(reason="implementation pending")` or `pytest.mark.xfail` so they are recognized by the test runner but don't fail the suite yet. Use async def with `@pytest.mark.asyncio`.

    `tests/unit/services/test_sso_service.py`:
    - test_saml_config_stored_and_retrieved (AUTH-01)
    - test_saml_invalid_assertion_rejected (AUTH-01)
    - test_oidc_config_stored_and_retrieved (AUTH-02)
    - test_role_claim_mapping_applies_correct_role (AUTH-03)
    - test_unmapped_claim_defaults_to_member (AUTH-03)
    - test_sso_only_flag_stored_in_workspace_settings (AUTH-04)

    `tests/unit/services/test_rbac_service.py`:
    - test_custom_role_created_with_permissions (AUTH-05)
    - test_check_permission_returns_true_for_allowed (AUTH-05)
    - test_check_permission_returns_false_for_denied (AUTH-05)
    - test_member_without_custom_role_falls_back_to_builtin (AUTH-05)
    - test_custom_role_name_unique_per_workspace (AUTH-05)

    `tests/unit/services/test_session_service.py`:
    - test_session_recorded_on_first_auth (AUTH-06)
    - test_last_seen_throttled_within_60s (AUTH-06)
    - test_force_terminate_sets_revoked_at (AUTH-06)
    - test_force_terminate_deletes_redis_key (AUTH-06)
    - test_terminate_all_user_sessions (AUTH-06)

    `tests/unit/routers/test_auth_sso.py`:
    - test_sso_only_workspace_rejects_password_login (AUTH-04)
    - test_saml_sso_endpoint_returns_redirect_url (AUTH-01)
    - test_revoked_session_returns_401 (AUTH-06)

    `tests/unit/routers/test_scim.py`:
    - test_scim_provision_user_creates_workspace_member (AUTH-07)
    - test_scim_deprovision_deactivates_not_deletes (AUTH-07)
    - test_scim_patch_user_updates_fields (AUTH-07)
    - test_scim_invalid_bearer_token_returns_401 (AUTH-07)

    STEP 5 — Post-migration validation:
    ```bash
    cd backend && alembic heads && alembic check
    ```
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && alembic heads && uv run pytest tests/unit/services/test_sso_service.py tests/unit/services/test_rbac_service.py tests/unit/services/test_session_service.py tests/unit/routers/test_auth_sso.py tests/unit/routers/test_scim.py -q --co 2>&1 | head -30</automated>
  </verify>
  <done>Migration 064 file exists with valid revision chain; all 5 test scaffold files exist and are discovered by pytest (showing as skipped/xfail, not errors); `alembic heads` shows single head pointing to 064.</done>
</task>

</tasks>

<verification>
After both tasks:
- `cd backend && uv run python -c "from pilot_space.infrastructure.database.models.custom_role import CustomRole; from pilot_space.infrastructure.database.models.workspace_session import WorkspaceSession; print('OK')"` — no import errors
- `cd backend && alembic heads` — single head
- `cd backend && uv run pytest tests/unit/services/ tests/unit/routers/test_auth_sso.py tests/unit/routers/test_scim.py -q --co` — all test functions collected, no import errors
- Migration file contains RLS policies for both new tables
- WorkspaceMember model has custom_role_id and is_active columns
</verification>

<success_criteria>
- Two new SQLAlchemy models (CustomRole, WorkspaceSession) exist and import cleanly
- Migration 064 creates custom_roles, workspace_sessions tables and alters workspace_members
- Migration has complete RLS policies (ENABLE, FORCE, workspace isolation, service_role bypass)
- All 5 test scaffold files exist with ~20 total test stubs collected by pytest
- `alembic heads` shows exactly one head after migration creation
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-access/01-01-SUMMARY.md`
</output>
