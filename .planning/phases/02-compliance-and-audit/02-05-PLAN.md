---
phase: 02-compliance-and-audit
plan: 05
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - frontend/src/app/(workspace)/[workspaceSlug]/settings/audit/page.tsx
  - frontend/src/features/settings/pages/audit-settings-page.tsx
  - frontend/src/features/settings/hooks/use-audit-log.ts
  - frontend/src/features/settings/pages/index.ts
  - frontend/src/app/(workspace)/[workspaceSlug]/settings/layout.tsx
  - frontend/src/features/settings/pages/__tests__/audit-settings-page.test.tsx
autonomous: false
requirements:
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06

must_haves:
  truths:
    - "Admin navigates to Settings > Audit and sees the audit log table"
    - "Admin can filter by actor (user search), action type (dropdown), resource type (dropdown), date range (start/end date pickers)"
    - "Admin can click Export and choose JSON or CSV — file downloads"
    - "If filtered result exceeds 10,000 rows, a warning prompt appears before download proceeds"
    - "No delete or edit buttons appear on any audit log row"
    - "Timestamp, Actor, Action, Resource Type, Resource ID (truncated), IP Address columns visible"
    - "Row expansion reveals full payload diff and AI fields"
  artifacts:
    - path: "frontend/src/features/settings/pages/audit-settings-page.tsx"
      provides: "AuditSettingsPage plain React component (no observer)"
      min_lines: 80
    - path: "frontend/src/features/settings/hooks/use-audit-log.ts"
      provides: "TanStack Query hooks: useAuditLog, useExportAuditLog"
      exports: ["useAuditLog", "useExportAuditLog"]
    - path: "frontend/src/app/(workspace)/[workspaceSlug]/settings/audit/page.tsx"
      provides: "Next.js route shell for /settings/audit"
  key_links:
    - from: "frontend/src/app/(workspace)/[workspaceSlug]/settings/layout.tsx"
      to: "audit settings nav item"
      via: "nav link href=/settings/audit"
      pattern: "/settings/audit"
    - from: "AuditSettingsPage"
      to: "GET /api/v1/workspaces/{slug}/audit"
      via: "useAuditLog TanStack Query hook"
      pattern: "useAuditLog"
    - from: "Export button"
      to: "GET /api/v1/workspaces/{slug}/audit/export"
      via: "browser download trigger (window.open or fetch + blob)"
      pattern: "audit/export"
---

<objective>
Build the AuditSettingsPage frontend: filterable table, row expansion, JSON/CSV export, and settings nav entry. Then human-verify the complete audit log flow end-to-end.

Purpose: Satisfies AUDIT-03 (filter UI), AUDIT-04 (export UI), AUDIT-05 (retention note — API-only, no UI in this phase per locked decision), AUDIT-06 (no write affordances visible). Closes the user-visible compliance story.
Output: AuditSettingsPage, TanStack Query hooks, Next.js route, settings nav update, passing frontend tests, human verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-compliance-and-audit/02-CONTEXT.md
@.planning/phases/02-compliance-and-audit/02-04-SUMMARY.md

<interfaces>
<!-- Settings page patterns from codebase -->

From frontend/src/features/settings/pages/security-settings-page.tsx (Phase 1 pattern):
- Plain React component (NOT observer())
- TanStack Query for all data (useQuery, useMutation)
- shadcn/ui components: Table, TableHeader, TableRow, TableCell, Button, Select, Dialog
- No MobX observables in settings pages

From frontend/src/app/(workspace)/[workspaceSlug]/settings/layout.tsx:
- Existing nav items: profile, members, roles, sso, security, ai-providers, billing, integrations
- Add "Audit" nav entry with href to /settings/audit
- Existing nav item pattern: `{ label: "Audit", href: `/${workspaceSlug}/settings/audit` }`

From frontend/src/features/settings/pages/index.ts:
- Barrel exports — add AuditSettingsPage export

API routes (from Plan 04):
- GET /api/v1/workspaces/{slug}/audit?actor_id=&action=&resource_type=&start_date=&end_date=&cursor=&page_size=
- GET /api/v1/workspaces/{slug}/audit/export?format=json|csv&[same filters]
- Returns PaginatedResponse[AuditLogResponse]

AuditLogResponse shape:
```typescript
interface AuditLogEntry {
  id: string;
  actor_id: string | null;
  actor_type: "USER" | "SYSTEM" | "AI";
  action: string;         // e.g. "issue.create"
  resource_type: string;  // e.g. "issue"
  resource_id: string | null;
  payload: { before: Record<string, unknown>; after: Record<string, unknown> } | null;
  ai_model: string | null;
  ai_token_cost: number | null;
  ai_rationale: string | null;
  ip_address: string | null;
  created_at: string;     // ISO timestamp
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TanStack Query hooks and AuditSettingsPage</name>
  <files>
    frontend/src/features/settings/hooks/use-audit-log.ts,
    frontend/src/features/settings/pages/audit-settings-page.tsx,
    frontend/src/features/settings/pages/index.ts,
    frontend/src/features/settings/pages/__tests__/audit-settings-page.test.tsx
  </files>
  <behavior>
    Hooks (use-audit-log.ts):
    - `useAuditLog(slug, filters, cursor)` — TanStack Query useQuery, fetches paginated list
    - `useExportAuditLog(slug)` — returns a `triggerExport(format, filters)` function that fetches the export URL and triggers browser download via Blob + Object URL

    AuditSettingsPage (plain React, NOT observer()):
    - Filter bar: actor search input (text, debounced 300ms), action dropdown, resource_type dropdown, start_date + end_date date inputs. All filters are optional. Filter state managed with useState.
    - Table columns: Timestamp (human-readable local time), Actor (actor_id truncated + actor_type badge), Action (dot-notation string), Resource Type, Resource ID (first 8 chars), IP Address.
    - Row expansion: clicking a row reveals full payload JSON (pretty-printed in a `<pre>` block), and AI fields (model, token cost, rationale) if actor_type=AI. No edit controls in expanded row.
    - Export bar: "Export JSON" and "Export CSV" buttons. On click: if estimated result > 10,000 (returned in response metadata or estimated from total_count), show a shadcn/ui AlertDialog warning prompt. User confirms → triggers download.
    - Cursor pagination: "Load more" button at bottom using next_cursor.
    - Read-only: zero delete, edit, or action buttons on rows.
    - Loading state: shadcn/ui Skeleton components while fetching.

    Tests (audit-settings-page.test.tsx):
    - Renders filter controls (actor input, action select, resource_type select, date inputs)
    - Renders table with correct column headers
    - Renders no delete/edit buttons
    - Export CSV button visible
    - Row expansion shows payload on click

    test_audit_log_model.py already exists from Wave 0 — update frontend test file only.
  </behavior>
  <action>
    1. Create `use-audit-log.ts` with `useAuditLog` and `useExportAuditLog`. Use the workspace API client already established in the codebase (check existing hooks in `frontend/src/features/settings/hooks/` for the fetch pattern). Export download: `fetch(exportUrl) → response.blob() → URL.createObjectURL(blob) → <a> click → URL.revokeObjectURL`.
    2. Create `audit-settings-page.tsx`. Do NOT use `observer()`. Import shadcn/ui Table, Select, Input, Button, AlertDialog, Skeleton. Manage filter state with `useState<AuditFilters>`. Debounce actor input with `useDebouncedValue` or `useEffect` + `setTimeout`.
    3. Add `AuditSettingsPage` to `frontend/src/features/settings/pages/index.ts` barrel.
    4. Create `__tests__/audit-settings-page.test.tsx` with Vitest + React Testing Library. Mock `use-audit-log.ts`. Test renders, column headers, no write buttons, export button visible.
    5. Run `cd frontend && pnpm test audit-settings-page` to validate.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm test -- --reporter=verbose audit-settings-page 2>&1 | tail -20</automated>
  </verify>
  <done>AuditSettingsPage renders with correct columns. All Vitest tests pass. No observer() wrapper. No delete/edit affordances.</done>
</task>

<task type="auto">
  <name>Task 2: Route shell and settings nav entry</name>
  <files>
    frontend/src/app/(workspace)/[workspaceSlug]/settings/audit/page.tsx,
    frontend/src/app/(workspace)/[workspaceSlug]/settings/layout.tsx
  </files>
  <action>
    1. Read `frontend/src/app/(workspace)/[workspaceSlug]/settings/layout.tsx` fully before editing. Add an "Audit" nav item after "Security" in the nav list, using the same `href` pattern as existing items. href: `/${workspaceSlug}/settings/audit`.
    2. Create `frontend/src/app/(workspace)/[workspaceSlug]/settings/audit/page.tsx` as a Next.js route shell. Import and render `AuditSettingsPage`. Pattern matches `security/page.tsx` and `sso/page.tsx`.
    3. Run `cd frontend && pnpm type-check` to verify no TypeScript errors in modified files.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm type-check 2>&1 | tail -10</automated>
  </verify>
  <done>pnpm type-check passes. /settings/audit route exists as Next.js page. "Audit" appears in settings nav.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete audit log UI at /settings/audit:
    - Filter bar (actor, action type, resource type, date range)
    - Read-only table (Timestamp, Actor, Action, Resource Type, Resource ID, IP Address)
    - Row expansion revealing payload diff and AI fields
    - Export JSON and Export CSV buttons with 10k-row warning
    - "Audit" nav entry in Settings sidebar
    - Backend: GET /audit (filtered, paginated), GET /audit/export, PATCH /audit/retention
    - Immutability trigger: UPDATE/DELETE on audit_log raises DB exception
    - AI hook writes audit_log rows on every AI action
    - All resource services emit audit rows on create/update/delete
  </what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uv run uvicorn pilot_space.main:app --reload --port 8000`
    2. Start frontend: `cd frontend && pnpm dev`
    3. Log in as admin (e2e-test@pilotspace.dev / TestPassword123!)
    4. Navigate to workspace → Settings → Audit (sidebar)
    5. Verify: Audit page loads with table columns visible
    6. Create an issue via the issues page — return to Audit, verify an `issue.create` row appears
    7. Update the issue title — verify `issue.update` row appears with payload diff showing only changed fields
    8. Apply an action filter (e.g., "issue.create") — verify only matching rows shown
    9. Apply a date range — verify filter applies correctly
    10. Click "Export CSV" — verify file downloads and opens in a text editor showing comma-separated rows
    11. Click "Export JSON" — verify JSON file downloads
    12. Expand a row — verify payload diff visible, no delete/edit button
    13. Verify "Audit" appears in the Settings sidebar navigation
    14. Run: `cd backend && uv run pytest tests/audit/ -q` — all pass (integration tests skip if no PostgreSQL)
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && pnpm type-check` → 0 errors
- `cd frontend && pnpm lint` → 0 errors
- `cd frontend && pnpm test -- audit-settings-page` → all pass
- `cd backend && uv run pytest tests/audit/ -q` → all pass
- `make quality-gates-backend && make quality-gates-frontend` → green
</verification>

<success_criteria>
AuditSettingsPage at /settings/audit shows filtered, paginated, read-only audit log table with JSON/CSV export. Settings nav includes "Audit" entry. All frontend tests pass. Human verifies complete audit flow end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/02-compliance-and-audit/02-05-SUMMARY.md`
</output>
