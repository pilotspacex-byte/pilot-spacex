---
phase: 02-compliance-and-audit
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/alembic/versions/065_add_audit_log_table.py
  - backend/src/pilot_space/infrastructure/database/models/audit_log.py
  - backend/tests/audit/__init__.py
  - backend/tests/audit/conftest.py
  - backend/tests/audit/test_audit_log_model.py
  - backend/tests/audit/test_audit_hook.py
  - backend/tests/audit/test_ai_audit.py
  - backend/tests/audit/test_audit_api.py
  - backend/tests/audit/test_audit_export.py
  - backend/tests/audit/test_retention.py
  - backend/tests/audit/test_immutability.py
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06

must_haves:
  truths:
    - "AuditLog model exists with all required fields and correct base classes (no SoftDeleteMixin)"
    - "Migration 065 runs cleanly from migration 064 head"
    - "Immutability trigger blocks UPDATE and DELETE (except pg_cron purge via session variable)"
    - "All test scaffolds exist and run as xfail (not skip)"
  artifacts:
    - path: "backend/src/pilot_space/infrastructure/database/models/audit_log.py"
      provides: "AuditLog model, ActorType enum"
      exports: ["AuditLog", "ActorType"]
    - path: "backend/alembic/versions/065_add_audit_log_table.py"
      provides: "DB migration: table, trigger, RLS, pg_cron, audit_retention_days column"
      contains: "fn_audit_log_immutable"
    - path: "backend/tests/audit/"
      provides: "Test scaffolds for all AUDIT-01 through AUDIT-06"
  key_links:
    - from: "AuditLog model"
      to: "Base, TimestampMixin, WorkspaceScopedMixin"
      via: "explicit inheritance (NOT WorkspaceScopedModel which includes SoftDeleteMixin)"
      pattern: "class AuditLog\\(Base, TimestampMixin, WorkspaceScopedMixin\\)"
    - from: "migration 065"
      to: "064_add_sso_rbac_session_tables.py"
      via: "Revises: header"
      pattern: "Revises:.*064"
---

<objective>
Create the AuditLog database model, migration 065, and complete test scaffolds for all AUDIT requirements.

Purpose: Foundation that all subsequent plans depend on. Model defines the contract; migration creates the DB structure including the immutability trigger and pg_cron retention job; test scaffolds enable Nyquist-compliant verification in later waves.
Output: AuditLog model, Alembic migration 065, 9 test scaffold files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-compliance-and-audit/02-CONTEXT.md
@.planning/phases/02-compliance-and-audit/02-RESEARCH.md
@.planning/phases/02-compliance-and-audit/02-VALIDATION.md

<interfaces>
<!-- Key base classes the executor needs. Extracted from codebase. -->

From backend/src/pilot_space/infrastructure/database/base.py:
```python
class Base(DeclarativeBase): ...  # SQLAlchemy declarative base

class TimestampMixin:
    created_at: Mapped[datetime]
    updated_at: Mapped[datetime]

class SoftDeleteMixin:
    is_deleted: Mapped[bool]
    deleted_at: Mapped[datetime | None]
    def soft_delete(self) -> None: ...
    def restore(self) -> None: ...

class WorkspaceScopedMixin:
    workspace_id: Mapped[uuid.UUID]  # FK to workspaces.id

class BaseModel(Base, TimestampMixin, SoftDeleteMixin):
    # Auto-generates table name; has .id UUID PK
    id: Mapped[uuid.UUID]

class WorkspaceScopedModel(BaseModel, WorkspaceScopedMixin):
    # INCLUDES SoftDeleteMixin — DO NOT use this for AuditLog
```

From backend/src/pilot_space/api/v1/schemas/base.py:
```python
class PaginatedResponse[T](BaseSchema):
    items: list[T]
    next_cursor: str | None
    prev_cursor: str | None
    has_next: bool
    has_prev: bool
    page_size: int
```

From backend/alembic/versions/064_add_sso_rbac_session_tables.py:
- Migration head: revision = "064_add_sso_rbac_session_tables"
- Use as `Revises` value in migration 065

From backend/src/pilot_space/infrastructure/database/rls.py:
- Pattern: service_role bypass policy uses `TO service_role USING (true) WITH CHECK (true)`
- INSERT policy uses workspace_members subquery with is_deleted=false
- SELECT policy filters by role IN ('OWNER', 'ADMIN') — UPPERCASE
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuditLog model and migration 065</name>
  <files>
    backend/src/pilot_space/infrastructure/database/models/audit_log.py,
    backend/alembic/versions/065_add_audit_log_table.py
  </files>
  <behavior>
    - AuditLog inherits `Base, TimestampMixin, WorkspaceScopedMixin` — NOT WorkspaceScopedModel (avoids SoftDeleteMixin)
    - ActorType enum: USER, SYSTEM, AI (string values uppercase)
    - All nullable fields as specified in CONTEXT.md schema
    - Composite indexes: ix_audit_log_workspace_created, ix_audit_log_workspace_actor
    - Migration 065 must: (1) CREATE TABLE, (2) CREATE FUNCTION fn_audit_log_immutable with session variable bypass for pg_cron, (3) CREATE TRIGGER, (4) RLS ENABLE+FORCE, (5) INSERT policy for workspace members, (6) SELECT policy for ADMIN/OWNER, (7) service_role bypass, (8) ALTER TABLE workspaces ADD COLUMN audit_retention_days INT DEFAULT 90, (9) CREATE FUNCTION fn_purge_audit_log_expired SECURITY DEFINER with `SELECT set_config('app.audit_purge', 'true', true)` before DELETE, (10) cron.schedule daily at 2am UTC
    - Trigger bypass: `IF current_setting('app.audit_purge', true) = 'true' THEN RETURN OLD; END IF;`
    - Revises: 064_add_sso_rbac_session_tables (exact string match)
    - downgrade() reverses: DROP TRIGGER, DROP FUNCTION, DROP TABLE, cron.unschedule, DROP COLUMN audit_retention_days
  </behavior>
  <action>
    1. Verify migration head: `cd backend && alembic heads` — must show single head at 064.
    2. Create `backend/src/pilot_space/infrastructure/database/models/audit_log.py` with AuditLog model and ActorType enum. Use JSONBCompat for JSONB columns (same import as activity.py). DO NOT inherit SoftDeleteMixin — use explicit `class AuditLog(Base, TimestampMixin, WorkspaceScopedMixin)`.
    3. Create `backend/alembic/versions/065_add_audit_log_table.py` following migration 032 as the pg_cron pattern reference. Strict ordering within upgrade(): table first, then trigger function, then trigger, then RLS, then audit_retention_days column, then purge function, then cron.schedule. All SQL via `op.execute(text(...))`.
    4. Run `cd backend && alembic upgrade head` — must succeed.
    5. Run `cd backend && alembic heads` — must show single head at 065.
    6. Run `cd backend && alembic downgrade -1 && alembic upgrade head` to verify round-trip.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && alembic heads && alembic check</automated>
  </verify>
  <done>Migration 065 applied cleanly, alembic heads shows single head, alembic check passes (models match DB).</done>
</task>

<task type="auto">
  <name>Task 2: Test scaffolds for all AUDIT requirements</name>
  <files>
    backend/tests/audit/__init__.py,
    backend/tests/audit/conftest.py,
    backend/tests/audit/test_audit_log_model.py,
    backend/tests/audit/test_audit_hook.py,
    backend/tests/audit/test_ai_audit.py,
    backend/tests/audit/test_audit_api.py,
    backend/tests/audit/test_audit_export.py,
    backend/tests/audit/test_retention.py,
    backend/tests/audit/test_immutability.py
  </files>
  <action>
    Create the `tests/audit/` package with all test scaffolds. Use `@pytest.mark.xfail(strict=False, reason="implementation pending")` on all test functions — NOT skip. This matches the Phase 1 pattern (xfail runs the body and reports XFAIL/XPASS).

    `conftest.py`: shared fixtures reusing existing `db_session`, `workspace`, `user` fixtures from `tests/conftest.py`. Add `audit_log_factory` fixture that creates an AuditLog row.

    `test_audit_log_model.py` (AUDIT-01): tests for AuditLog creation with USER actor, AI actor; assert fields stored correctly; assert no soft_delete method.

    `test_audit_hook.py` (AUDIT-01): tests that service-layer audit write calls create a row in DB; test issue.create, issue.update, issue.delete actions emit correct audit rows.

    `test_ai_audit.py` (AUDIT-02): tests that AuditLogHook writes a row with actor_type=AI, ai_model, ai_token_cost, ai_rationale populated after PostToolUse.

    `test_audit_api.py` (AUDIT-03, AUDIT-05): tests for GET /workspaces/{slug}/audit (list endpoint) with filter params (actor_id, action, resource_type, start_date, end_date); cursor pagination; PATCH retention days.

    `test_audit_export.py` (AUDIT-04): tests for GET /workspaces/{slug}/audit/export?format=csv and ?format=json returning correct content-type and headers.

    `test_retention.py` (AUDIT-05): tests that `fn_purge_audit_log_expired` logic deletes rows older than retention_days and keeps rows within window; unit-testable via mocked DB query.

    `test_immutability.py` (AUDIT-06, requires PostgreSQL): mark with `@pytest.mark.integration`. Tests that direct UPDATE on audit_log raises exception; direct DELETE raises exception; no DELETE/UPDATE endpoints in audit router.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run pytest tests/audit/ -q --no-header 2>&1 | tail -5</automated>
  </verify>
  <done>All 9 test files exist, `pytest tests/audit/ -q` runs without collection errors, all tests report xfail (not ERROR or skip).</done>
</task>

</tasks>

<verification>
- `cd backend && alembic heads` → single head at 065
- `cd backend && alembic check` → no differences
- `cd backend && uv run pytest tests/audit/ -q` → all xfail, 0 errors
- `cd backend && uv run ruff check src/pilot_space/infrastructure/database/models/audit_log.py` → clean
</verification>

<success_criteria>
AuditLog model exported from models package, migration 065 applied, all 9 test scaffold files exist and run as xfail without collection errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-compliance-and-audit/02-01-SUMMARY.md`
</output>
