---
phase: 33-remote-mcp-approval
plan: 03
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - frontend/src/stores/ai/MCPServersStore.ts
  - frontend/src/services/api/mcp-servers.ts
  - frontend/src/features/settings/components/mcp-server-card.tsx
  - frontend/src/features/settings/pages/mcp-servers-settings-page.tsx
  - frontend/src/features/settings/components/__tests__/mcp-server-card.test.tsx
  - frontend/src/features/settings/pages/__tests__/mcp-servers-settings-page.test.tsx
autonomous: true
requirements:
  - MCPA-02
  - MCPA-03

must_haves:
  truths:
    - "MCPServer TypeScript type includes approval_mode field"
    - "Admin can toggle approval_mode per server via a Switch in the MCPServerCard"
    - "Toggle calls PATCH /mcp-servers/{id}/approval-mode and updates MobX store"
    - "ChatView inline approval card renders correctly for remote MCP tool calls (no new component needed — InlineApprovalCard handles unknown action types)"
  artifacts:
    - path: "frontend/src/stores/ai/MCPServersStore.ts"
      provides: "approval_mode field on MCPServer interface + updateApprovalMode action"
      contains: "updateApprovalMode"
    - path: "frontend/src/services/api/mcp-servers.ts"
      provides: "updateApprovalMode API method calling PATCH endpoint"
      contains: "approval-mode"
    - path: "frontend/src/features/settings/components/mcp-server-card.tsx"
      provides: "Switch toggle for require_approval per server + onUpdateApprovalMode prop"
      contains: "onUpdateApprovalMode"
    - path: "frontend/src/features/settings/pages/mcp-servers-settings-page.tsx"
      provides: "onUpdateApprovalMode handler wired to MCPServersStore.updateApprovalMode"
      contains: "updateApprovalMode"
  key_links:
    - from: "frontend/src/features/settings/components/mcp-server-card.tsx"
      to: "frontend/src/features/settings/pages/mcp-servers-settings-page.tsx"
      via: "onUpdateApprovalMode prop callback"
      pattern: "onUpdateApprovalMode"
    - from: "frontend/src/features/settings/pages/mcp-servers-settings-page.tsx"
      to: "frontend/src/stores/ai/MCPServersStore.ts"
      via: "mcpServersStore.updateApprovalMode(workspaceId, serverId, mode)"
      pattern: "updateApprovalMode"
    - from: "frontend/src/stores/ai/MCPServersStore.ts"
      to: "frontend/src/services/api/mcp-servers.ts"
      via: "mcpServersApi.updateApprovalMode"
      pattern: "approval-mode"
---

<objective>
Add the admin-facing approval mode toggle to the MCP server settings UI: MCPServer type gets approval_mode, MCPServersStore gets an updateApprovalMode action, the API client gets the PATCH method, MCPServerCard gets a shadcn Switch toggle, and the settings page wires them together.

Purpose: MCPA-02 (admin can configure per-server approval policy) and MCPA-03 (ChatView inline cards — no new component needed; InlineApprovalCard already handles unknown actionTypes via GenericJSON fallback).
Output: Full frontend approval mode toggle flow; no new React components for chat (existing InlineApprovalCard handles it).
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33/RESEARCH.md
@.planning/phases/33/33-01-SUMMARY.md

@frontend/src/stores/ai/MCPServersStore.ts
@frontend/src/services/api/mcp-servers.ts
@frontend/src/features/settings/components/mcp-server-card.tsx
@frontend/src/features/settings/pages/mcp-servers-settings-page.tsx
@frontend/src/features/settings/components/__tests__/mcp-server-card.test.tsx
@frontend/src/features/settings/pages/__tests__/mcp-servers-settings-page.test.tsx

<interfaces>
<!-- PATCH endpoint added in plan 33-01 -->
PATCH /workspaces/{workspaceSlug}/mcp-servers/{serverId}/approval-mode
Body: { "approval_mode": "auto_approve" | "require_approval" }
Response: WorkspaceMcpServerResponse (includes approval_mode field)

<!-- MCPServerCard is a plain component (NOT observer) — per RESEARCH.md pitfall 5 -->
<!-- mcp-servers-settings-page.tsx IS an observer and owns store calls -->

<!-- shadcn Switch component is already available — used elsewhere in the codebase -->
<!-- Follow the same pattern as ExpiryBadge: co-locate related UI in mcp-server-card.tsx -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCPServer type + store action + API method</name>
  <files>
    frontend/src/stores/ai/MCPServersStore.ts
    frontend/src/services/api/mcp-servers.ts
  </files>
  <behavior>
    - MCPServer interface: add approval_mode: 'auto_approve' | 'require_approval' field (defaults to 'auto_approve' for backwards compat on list items that pre-date the column)
    - MCPServersStore.updateApprovalMode(workspaceId: string, serverId: string, mode: 'auto_approve' | 'require_approval'): Promise<void> — calls mcpServersApi.updateApprovalMode, then runInAction updates the servers array entry with new approval_mode
    - mcpServersApi.updateApprovalMode(workspaceId, serverId, mode): calls apiClient.patch<MCPServer> on `${base(workspaceId)}/${serverId}/approval-mode` with body { approval_mode: mode }
    - Test: updateApprovalMode calls api with correct args and updates observable servers array
  </behavior>
  <action>
    1. Read MCPServersStore.ts fully to understand the existing MCPServer interface and store action patterns (how existing actions like deleteServer call the API and update observables).
    2. Add approval_mode to the MCPServer interface. Since older API responses may omit the field, make it: approval_mode?: 'auto_approve' | 'require_approval' — or give it a default in the mapper. Follow how token_expires_at was handled in Phase 32 for guidance.
    3. Add updateApprovalMode action to MCPServersStore following the existing action pattern (async, makeAutoObservable already wraps it). Use runInAction for the observable mutation.
    4. Read mcp-servers.ts to confirm the base URL helper pattern, then add updateApprovalMode method.
    5. In the store test file (if it exists under src/stores/ai/__tests__/), add test for updateApprovalMode. If no test file exists, add the test to mcp-servers-settings-page.test.tsx instead — check before creating a new file.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space-3/frontend && pnpm type-check 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -20</automated>
  </verify>
  <done>MCPServer interface has approval_mode field; updateApprovalMode action exists in store; mcpServersApi.updateApprovalMode exists; tsc reports 0 new errors on touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCPServerCard Switch toggle + page wiring + tests</name>
  <files>
    frontend/src/features/settings/components/mcp-server-card.tsx
    frontend/src/features/settings/pages/mcp-servers-settings-page.tsx
    frontend/src/features/settings/components/__tests__/mcp-server-card.test.tsx
    frontend/src/features/settings/pages/__tests__/mcp-servers-settings-page.test.tsx
  </files>
  <behavior>
    - MCPServerCard props: add onUpdateApprovalMode?: (serverId: string, mode: 'auto_approve' | 'require_approval') => void
    - MCPServerCard renders a shadcn Switch (id=`approval-mode-${server.id}`) after the ExpiryBadge and before the action buttons. Switch checked={server.approval_mode === 'require_approval'}. Label text: "Require approval for tool calls". The Switch + label are wrapped in a flex row div with gap-2.
    - mcp-servers-settings-page.tsx: pass onUpdateApprovalMode to MCPServerCard. Handler calls mcpServersStore.updateApprovalMode(workspaceSlug, serverId, mode). The page is already an observer and has access to the store.
    - Test mcp-server-card.test.tsx: renders Switch unchecked when approval_mode='auto_approve'; renders Switch checked when approval_mode='require_approval'; toggling Switch calls onUpdateApprovalMode with correct args
    - Test mcp-servers-settings-page.test.tsx: onUpdateApprovalMode handler calls store.updateApprovalMode (extend existing test or add new describe block)
  </behavior>
  <action>
    1. Read mcp-server-card.tsx fully before modifying. Note: MCPServerCard is NOT an observer (per RESEARCH.md pitfall 5). Add the new prop to the props interface and render the Switch inside the card body. Import Switch from "@/components/ui/switch". Do not add MobX observer wrapping.
    2. Read mcp-servers-settings-page.tsx to find where MCPServerCard is rendered and how existing callbacks (onDelete, onEdit, etc.) are wired. Add onUpdateApprovalMode in the same pattern.
    3. Update mcp-server-card.test.tsx. Read the existing test file first to understand the mock setup. Add three tests in a new describe("approval mode toggle") block.
    4. Update mcp-servers-settings-page.test.tsx to cover the onUpdateApprovalMode wiring. Read existing test first.
    5. MCPA-03 note: No new chat component needed. The InlineApprovalCard already handles unknown actionType values via GenericJSON fallback. The backend sets actionType to "remote_mcp_tool" (or the bare tool name), which is not in DESTRUCTIVE_ACTIONS, so it renders inline (not modal). Verify this by checking ChatView/utils.ts DESTRUCTIVE_ACTIONS set and confirming "remote_mcp_tool" is absent — no code changes needed for MCPA-03.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space-3/frontend && pnpm test -- --run src/features/settings/components/__tests__/mcp-server-card.test.tsx src/features/settings/pages/__tests__/mcp-servers-settings-page.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>MCPServerCard renders approval mode Switch; toggle calls onUpdateApprovalMode; page wires callback to store; all new tests pass; tsc clean.</done>
</task>

</tasks>

<verification>
cd /Users/tindang/workspaces/tind-repo/pilot-space-3/frontend && pnpm type-check 2>&1 | grep -v node_modules | grep -E "error|Error" | head -10 && pnpm test -- --run src/features/settings/ 2>&1 | tail -20
</verification>

<success_criteria>
- MCPServer interface includes approval_mode field
- MCPServersStore.updateApprovalMode action calls API and updates observables
- MCPServerCard shows shadcn Switch for "Require approval for tool calls"
- mcp-servers-settings-page wires onUpdateApprovalMode to store action
- All new tests pass; existing tests pass
- tsc --noEmit reports 0 new errors
- No new React components required for ChatView (InlineApprovalCard handles MCPA-03 automatically via GenericJSON fallback)
</success_criteria>

<output>
After completion, create `.planning/phases/33/33-03-SUMMARY.md`
</output>
