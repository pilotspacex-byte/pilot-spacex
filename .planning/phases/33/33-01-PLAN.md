---
phase: 33-remote-mcp-approval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/093_add_mcp_approval_mode.py
  - backend/src/pilot_space/infrastructure/database/models/workspace_mcp_server.py
  - backend/src/pilot_space/api/v1/routers/_mcp_server_schemas.py
  - backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py
  - backend/src/pilot_space/ai/infrastructure/approval.py
  - backend/tests/api/test_workspace_mcp_servers.py
autonomous: true
requirements:
  - MCPA-02

must_haves:
  truths:
    - "Alembic migration 093 adds approval_mode column to workspace_mcp_servers"
    - "WorkspaceMcpServer ORM model exposes approval_mode as a typed attribute"
    - "WorkspaceMcpServerResponse includes approval_mode in API responses"
    - "PATCH /mcp-servers/{id}/approval-mode updates the column and returns updated server"
    - "ActionType.REMOTE_MCP_TOOL enum member exists in approval.py"
  artifacts:
    - path: "backend/alembic/versions/093_add_mcp_approval_mode.py"
      provides: "DB migration: approval_mode VARCHAR(16) NOT NULL DEFAULT auto_approve"
      contains: "revision.*093"
    - path: "backend/src/pilot_space/infrastructure/database/models/workspace_mcp_server.py"
      provides: "McpApprovalMode StrEnum + approval_mode mapped_column"
      contains: "McpApprovalMode"
    - path: "backend/src/pilot_space/api/v1/routers/_mcp_server_schemas.py"
      provides: "McpApprovalModeUpdate request schema + approval_mode field in response"
      exports: ["McpApprovalModeUpdate"]
    - path: "backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py"
      provides: "PATCH /{server_id}/approval-mode endpoint"
      contains: "approval-mode"
    - path: "backend/src/pilot_space/ai/infrastructure/approval.py"
      provides: "ActionType.REMOTE_MCP_TOOL = remote_mcp_tool in DEFAULT_REQUIRE_ACTIONS"
      contains: "REMOTE_MCP_TOOL"
  key_links:
    - from: "backend/alembic/versions/093_add_mcp_approval_mode.py"
      to: "workspace_mcp_servers table"
      via: "op.add_column"
      pattern: "approval_mode"
    - from: "backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py"
      to: "backend/src/pilot_space/api/v1/routers/_mcp_server_schemas.py"
      via: "McpApprovalModeUpdate import"
      pattern: "McpApprovalModeUpdate"
---

<objective>
Add the database column, ORM model changes, PATCH endpoint, and ActionType enum member that are shared dependencies for the backend approval wiring (plan 33-02) and frontend admin UI (plan 33-03).

Purpose: Plans 33-02 and 33-03 cannot run without the approval_mode column in the DB and ORM model. This plan provides that foundation in wave 1 so waves 2 can run in parallel.
Output: Migration 093, ORM McpApprovalMode enum, approval_mode column on WorkspaceMcpServer, PATCH endpoint, ActionType.REMOTE_MCP_TOOL.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33/RESEARCH.md

@backend/src/pilot_space/infrastructure/database/models/workspace_mcp_server.py
@backend/src/pilot_space/api/v1/routers/_mcp_server_schemas.py
@backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py
@backend/src/pilot_space/ai/infrastructure/approval.py
@backend/alembic/versions/092_add_oauth_refresh_token.py
@backend/tests/api/test_workspace_mcp_servers.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 093 + ORM McpApprovalMode + ActionType.REMOTE_MCP_TOOL</name>
  <files>
    backend/alembic/versions/093_add_mcp_approval_mode.py
    backend/src/pilot_space/infrastructure/database/models/workspace_mcp_server.py
    backend/src/pilot_space/ai/infrastructure/approval.py
  </files>
  <behavior>
    - Migration upgrades: adds approval_mode VARCHAR(16) NOT NULL server_default 'auto_approve' with CHECK constraint (auto_approve | require_approval); downgrade drops constraint then column
    - WorkspaceMcpServer ORM: new McpApprovalMode StrEnum with AUTO_APPROVE = "auto_approve" and REQUIRE_APPROVAL = "require_approval"; approval_mode: Mapped[str] = mapped_column(String(16), nullable=False, default=McpApprovalMode.AUTO_APPROVE, server_default="auto_approve")
    - ActionType enum: add REMOTE_MCP_TOOL = "remote_mcp_tool" to DEFAULT_REQUIRE section; add to DEFAULT_REQUIRE_ACTIONS frozenset
    - down_revision for migration 093 must be "092_add_oauth_refresh_token" (verify from 092 file before writing)
  </behavior>
  <action>
    1. Read backend/alembic/versions/092_add_oauth_refresh_token.py to confirm the exact revision string to use as down_revision.
    2. Create 093_add_mcp_approval_mode.py following the 092 file pattern. Use op.add_column with sa.String(16) and server_default="auto_approve". Add CHECK constraint named "ck_mcp_server_approval_mode". Downgrade drops constraint first, then column.
    3. In workspace_mcp_server.py: add McpApprovalMode StrEnum (above the WorkspaceMcpServer class) and add approval_mode mapped_column. Do NOT add server_default to mapped_column (that belongs in the migration only) — use only Python-level default=McpApprovalMode.AUTO_APPROVE.
    4. In approval.py: add REMOTE_MCP_TOOL = "remote_mcp_tool" to ActionType enum in the DEFAULT_REQUIRE block, and add ActionType.REMOTE_MCP_TOOL to the DEFAULT_REQUIRE_ACTIONS frozenset.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space-3/backend && uv run alembic check 2>/dev/null || true && uv run pyright src/pilot_space/infrastructure/database/models/workspace_mcp_server.py src/pilot_space/ai/infrastructure/approval.py --outputjson 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); errs=[e for e in d.get('generalDiagnostics',[]) if e['severity']=='error']; print(f'{len(errs)} errors'); sys.exit(len(errs))"</automated>
  </verify>
  <done>Migration 093 file exists with correct down_revision; WorkspaceMcpServer has McpApprovalMode enum and approval_mode column; ActionType.REMOTE_MCP_TOOL exists in approval.py DEFAULT_REQUIRE_ACTIONS; pyright reports 0 errors on touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH endpoint + schema + tests</name>
  <files>
    backend/src/pilot_space/api/v1/routers/_mcp_server_schemas.py
    backend/src/pilot_space/api/v1/routers/workspace_mcp_servers.py
    backend/tests/api/test_workspace_mcp_servers.py
  </files>
  <behavior>
    - McpApprovalModeUpdate schema: Pydantic BaseModel with approval_mode: Literal["auto_approve", "require_approval"]
    - WorkspaceMcpServerResponse gains approval_mode: str = "auto_approve" field
    - PATCH /{server_id}/approval-mode: requires ADMIN/OWNER role, loads server, sets approval_mode, commits, returns updated WorkspaceMcpServerResponse
    - test_update_approval_mode_success: PATCH with require_approval returns 200 + approval_mode == "require_approval"
    - test_update_approval_mode_invalid: PATCH with "bad_value" returns 422
    - test_update_approval_mode_unauthorized: PATCH as MEMBER returns 403
  </behavior>
  <action>
    1. Check current line count of workspace_mcp_servers.py (known ~670 lines). If adding the PATCH endpoint would exceed 695 lines (pre-commit 700-line limit), extract the handler to a helper or keep action body tight. The endpoint itself is ~20 lines so it should fit.
    2. Add McpApprovalModeUpdate to _mcp_server_schemas.py, and add approval_mode: str = "auto_approve" to WorkspaceMcpServerResponse.
    3. Add PATCH endpoint to workspace_mcp_servers.py. Import McpApprovalModeUpdate from _mcp_server_schemas. Follow the exact same role-check pattern used by the existing update endpoint (check for OWNER or ADMIN role, raise 403 otherwise). Use SQLAlchemy update statement or load-then-set pattern matching the existing codebase style.
    4. Write tests extending test_workspace_mcp_servers.py. Mock the DB session and repository following the existing test patterns in that file. Write the three test cases listed in the behavior block above.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space-3/backend && uv run pytest tests/api/test_workspace_mcp_servers.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>PATCH /{server_id}/approval-mode endpoint exists and all three test cases pass; workspace_mcp_servers.py stays under 700 lines; pyright reports 0 new errors.</done>
</task>

</tasks>

<verification>
cd /Users/tindang/workspaces/tind-repo/pilot-space-3/backend && uv run pytest tests/api/test_workspace_mcp_servers.py -x -q && uv run pyright src/pilot_space/infrastructure/database/models/workspace_mcp_server.py src/pilot_space/api/v1/routers/ src/pilot_space/ai/infrastructure/approval.py 2>/dev/null | tail -5
</verification>

<success_criteria>
- Migration 093 exists with down_revision = "092_add_oauth_refresh_token"
- WorkspaceMcpServer.approval_mode column present in ORM model
- McpApprovalMode StrEnum exported from workspace_mcp_server.py
- ActionType.REMOTE_MCP_TOOL in DEFAULT_REQUIRE_ACTIONS in approval.py
- PATCH /mcp-servers/{id}/approval-mode returns 200 with updated approval_mode
- WorkspaceMcpServerResponse includes approval_mode field
- All existing + new tests pass; pyright clean
</success_criteria>

<output>
After completion, create `.planning/phases/33/33-01-SUMMARY.md`
</output>
