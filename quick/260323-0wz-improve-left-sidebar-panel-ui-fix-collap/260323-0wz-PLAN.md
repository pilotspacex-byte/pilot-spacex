---
phase: quick
plan: 260323-0wz
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/sidebar.tsx
autonomous: true
requirements: [sidebar-collapsed-spacing, sidebar-vertical-rhythm, sidebar-active-state]

must_haves:
  truths:
    - "Collapsed sidebar icons have comfortable breathing room with clear visual separation"
    - "Active nav item is clearly distinguishable in both expanded and collapsed states"
    - "Collapse toggle button has adequate touch/click target size (min 32px)"
    - "Vertical spacing between sections is consistent and visually balanced"
    - "New Note button in collapsed state is visually centered and adequately sized"
    - "Bottom user controls in collapsed state don't feel cramped"
  artifacts:
    - path: "frontend/src/components/layout/sidebar.tsx"
      provides: "Improved sidebar layout with better collapsed state UX"
      contains: "py-2"
  key_links:
    - from: "frontend/src/components/layout/sidebar.tsx"
      to: "frontend/src/components/layout/app-shell.tsx"
      via: "60px collapsed width constraint from app-shell"
      pattern: "width.*60"
---

<objective>
Improve the left sidebar panel UI focusing on the collapsed state layout and overall vertical rhythm.

Purpose: The collapsed sidebar (60px) currently feels cramped — icons are tight, the section divider is barely visible, the collapse toggle is too thin, and active states are hard to distinguish. These issues violate the project's "spacious calm over dense efficiency" design principle.

Output: Updated sidebar.tsx with refined spacing, better collapsed-state proportions, stronger active indicators, and consistent vertical rhythm.
</objective>

<execution_context>
@workflows/execute-plan.md
@templates/summary.md
</execution_context>

<context>
@frontend/src/components/layout/sidebar.tsx
@frontend/src/components/layout/app-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix collapsed state spacing and icon layout</name>
  <files>frontend/src/components/layout/sidebar.tsx</files>
  <action>
Update the collapsed-state layout classes throughout sidebar.tsx to provide more breathing room within the 60px container:

1. **Nav items collapsed padding** (line ~469-473): Change the collapsed override from `collapsed && 'justify-center px-2'` to `collapsed && 'justify-center px-0 py-2'`. Then wrap the icon in a centered container: the Link itself should use `w-full justify-center` when collapsed, with `py-2` for more vertical breathing room (currently `py-1.5`).

2. **Section divider** (line ~443-448): Change `w-4 h-px` to `w-8 h-px` so the divider between Main and AI sections spans more of the 60px width. Also increase bottom margin from `mb-1.5` to `mb-2`.

3. **Header area collapsed** (line ~400-403): When collapsed, change from `justify-center px-0` to `justify-center px-2` — the logo needs slight horizontal padding even when collapsed to not feel edge-to-edge. Hide the WorkspaceSwitcher entirely when collapsed (it stacks awkwardly); only show the Compass icon centered.

4. **New Note button collapsed** (line ~586-608): Increase from `h-8 w-8` to `h-9 w-9` in collapsed mode for a larger touch target. Update the Plus icon to `h-4 w-4` (from `h-3.5 w-3.5`) when collapsed. Add `p-3` to the container (from `p-2`).

5. **Bottom user controls collapsed** (line ~211-238): Change container from `gap-1 p-1.5` to `flex-col gap-1.5 p-2` — stack notification bell and avatar vertically instead of horizontally. This gives each control its own row within the 60px width.

6. **Collapse toggle button** (line ~623-657): Increase the button height from `h-6` to `h-8` for a proper 32px touch target. Bump container padding from `p-1.5` to `p-2`.
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check</automated>
  </verify>
  <done>All collapsed-state elements have generous spacing: nav items use py-2 padding, section divider spans w-8, New Note button is h-9 w-9, user controls stack vertically, collapse toggle has h-8 touch target. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Improve active state visibility and vertical rhythm consistency</name>
  <files>frontend/src/components/layout/sidebar.tsx</files>
  <action>
Refine active state highlighting and normalize vertical spacing across the sidebar:

1. **Active state indicator** (line ~468-474): Replace the subtle `shadow-warm-sm` with a stronger visual cue. Add a left accent bar for active items using a pseudo-element approach via Tailwind: add `before:absolute before:left-0 before:top-1/2 before:-translate-y-1/2 before:h-4 before:w-[3px] before:rounded-full before:bg-primary` to the active state classes. Keep `bg-sidebar-accent` but upgrade to `font-semibold` (from `font-medium`) for active items. Remove `shadow-warm-sm` from nav items (keep it only on the New Note button where it's appropriate).

2. **Collapsed active state**: For collapsed mode, change the accent bar to a bottom dot indicator instead: when `collapsed && isActive`, add `after:absolute after:bottom-0 after:left-1/2 after:-translate-x-1/2 after:h-[3px] after:w-3 after:rounded-full after:bg-primary`. Also increase the background opacity: use `bg-sidebar-accent` (full) rather than relying on shadow for distinction.

3. **Vertical rhythm normalization**:
   - Navigation container (line ~426): Change `gap-0.5 p-2` to `gap-1 p-2` — slightly more space between nav items.
   - Section margin (line ~431): Change `mt-3` to `mt-4` between sections for clearer visual grouping.
   - Section label margin (line ~434): Keep `mb-1` as-is (this is fine).
   - Pinned notes section (line ~532): Change `mb-3` to `mb-4` for consistency with section spacing.
   - Pinned notes header (line ~533): Change `mb-1.5` to `mb-2` for consistency.
   - ScrollArea (line ~528): Change `py-1.5` to `py-2` to match the navigation container padding.

4. **Pinned note active state** (line ~553-558): Apply the same left accent bar pattern to active pinned notes. Remove `shadow-warm-sm` from pinned notes active state too.
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check && pnpm test -- --run --reporter=verbose src/components/layout/__tests__/sidebar-navigation.test.tsx</automated>
  </verify>
  <done>Active nav items show a visible left accent bar (expanded) or bottom dot (collapsed). Vertical spacing uses consistent gap-1 for items, mt-4 between sections, py-2 for containers. shadow-warm-sm removed from nav items. All existing sidebar tests pass.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm type-check` passes with no errors
2. `cd frontend && pnpm test -- --run src/components/layout/__tests__/sidebar-navigation.test.tsx` passes
3. `cd frontend && pnpm test -- --run src/components/layout/__tests__/SidebarUserControls.test.tsx` passes
4. Visual check: collapsed sidebar at 60px shows well-spaced icons, clear active indicator, comfortable toggle target
</verification>

<success_criteria>
- Collapsed sidebar icons have minimum py-2 vertical padding within 60px width
- Active items display a colored accent indicator (bar or dot) instead of relying solely on subtle shadow
- Section divider spans adequate width (w-8) in collapsed state
- Collapse toggle has 32px (h-8) minimum touch target
- New Note button is h-9 w-9 in collapsed state
- User controls stack vertically in collapsed state
- Consistent mt-4 between sections, gap-1 between items
- All TypeScript and existing test checks pass
</success_criteria>

<output>
After completion, create `.planning/quick/260323-0wz-improve-left-sidebar-panel-ui-fix-collap/260323-0wz-SUMMARY.md`
</output>
