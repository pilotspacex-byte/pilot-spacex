---
phase: 28-visual-design-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/app/layout.tsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Body and UI text renders in the system font stack, not DM Sans"
    - "Background, border, and muted colors are neutral/Notion-like, not warm-amber"
    - "Dark mode tokens are updated with matching neutral parity"
    - "Shadow and glass utility classes use neutral hue instead of warm hsl(30)"
    - "Fraunces display font is preserved for document titles and headings"
    - "Frontend compiles and all existing tests pass"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Updated design tokens: neutral palette, system font reference, neutral shadows"
      contains: "system-ui"
    - path: "frontend/src/app/layout.tsx"
      provides: "Cleaned font loading — DM Sans removed, Fraunces kept, system fonts primary"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/app/globals.css"
      via: "CSS variable --font-sans references system-ui stack"
      pattern: "system-ui"
    - from: "frontend/src/app/globals.css :root"
      to: "frontend/src/app/globals.css .dark"
      via: "Every light token has a dark counterpart"
      pattern: "--background.*#"
---

<objective>
Update design tokens in globals.css to a Notion-like neutral palette with system font stack, and clean up font loading in layout.tsx.

Purpose: Satisfy UI-01 — typography, spacing foundation, and color token refresh for a Notion-like feel.
Output: Updated globals.css with neutral tokens and cleaned layout.tsx font loading.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-visual-design-refresh/28-RESEARCH.md

@frontend/src/app/globals.css
@frontend/src/app/layout.tsx

<interfaces>
<!-- globals.css token structure (lines 11-97): @theme inline maps Tailwind utility classes to CSS custom properties -->
<!-- :root block (lines 102-192): Light theme token values -->
<!-- .dark block (lines 197-281): Dark theme token values -->
<!-- Custom classes (lines 310-409): shadow-warm-*, glass, ai-*, scrollbar-thin use hardcoded hsl() -->
<!-- layout.tsx: DM_Sans, DM_Mono, JetBrains_Mono loaded via next/font/google; Fraunces via <link> tag -->

From frontend/src/app/layout.tsx:
```typescript
import { DM_Sans, DM_Mono, JetBrains_Mono } from 'next/font/google';
// body className includes: ${dmSans.variable} ${dmMono.variable} ${jetbrainsMono.variable}
```

Current font token in globals.css:
```css
--font-sans: var(--font-dm-sans), var(--font-geist-sans), system-ui, sans-serif;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update globals.css design tokens to Notion-like neutral palette and system font stack</name>
  <files>frontend/src/app/globals.css</files>
  <action>
Update globals.css with the following changes. For every :root token changed, update the matching .dark token for parity.

**1. Font tokens (@theme inline, line 73-76):**
Replace `--font-sans` to lead with system fonts:
```css
--font-sans: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
```
Keep `--font-display` as `'Fraunces', Georgia, serif` — display headings keep the editorial font per research recommendation.
Keep `--font-mono` unchanged (DM Mono + Geist Mono + SF Mono is fine).

**2. Light theme color tokens (:root block):**
Shift from warm-amber to neutral Notion-like values:
```
--background: #ffffff           (was #fdfcfa, pure white like Notion)
--background-subtle: #f7f7f5   (was #f8f7f5, slightly cooler)
--background-muted: #f1f1ef    (was #f3f2ef, more neutral)
--foreground: #37352f           (was #1a1918, Notion's actual text color)
--foreground-muted: #787774     (was #6b6966, Notion-like secondary)
--card: #ffffff                 (was #fdfcfa)
--card-foreground: #37352f      (was #1a1918)
--popover: #ffffff              (was #fdfcfa)
--popover-foreground: #37352f   (was #1a1918)
--secondary: #f1f1ef            (was #f3f2ef)
--secondary-foreground: #37352f (was #1a1918)
--muted: #f1f1ef                (was #f3f2ef)
--muted-foreground: #787774     (was #6b6966)
--accent: #f1f1ef               (was #f3f2ef)
--accent-foreground: #37352f    (was #1a1918)
--border: #e9e9e7               (was #e8e6e3, slightly cooler)
--border-subtle: #efefed        (was #f0eeeb)
--input: #e9e9e7                (was #e8e6e3)
--sidebar: #f7f7f5              (was #f8f7f5)
--sidebar-foreground: #37352f   (was #1a1918)
--sidebar-accent: #f1f1ef       (was #f3f2ef)
--sidebar-accent-foreground: #37352f (was #1a1918)
--sidebar-border: #e9e9e7       (was #e8e6e3)
```
Keep primary teal (#29a386), destructive, warning, AI partner, issue state, and priority colors UNCHANGED — these are functional/brand colors, not palette tokens.

**3. Dark theme color tokens (.dark block):**
Update to match neutral direction:
```
--background: #191919           (was #1a1918, pure neutral)
--background-subtle: #222222    (was #232220)
--background-muted: #2c2c2c    (was #2d2c2a)
--foreground: #ebebeb           (was #edeceb, neutral)
--foreground-muted: #9b9b9b    (was #9c9590, neutral gray)
--card: #222222                 (was #232220)
--card-foreground: #ebebeb      (was #edeceb)
--popover: #222222              (was #232220)
--popover-foreground: #ebebeb   (was #edeceb)
--secondary: #2c2c2c            (was #2d2c2a)
--secondary-foreground: #ebebeb (was #edeceb)
--muted: #2c2c2c                (was #2d2c2a)
--muted-foreground: #9b9b9b    (was #9c9590)
--accent: #2c2c2c               (was #2d2c2a)
--accent-foreground: #ebebeb    (was #edeceb)
--border: #3c3c3c               (was #3d3c3a)
--border-subtle: #333333        (was #333230)
--input: #3c3c3c                (was #3d3c3a)
--sidebar: #141414              (was #151413)
--sidebar-foreground: #ebebeb   (was #edeceb)
--sidebar-accent: #2c2c2c       (was #2d2c2a)
--sidebar-accent-foreground: #ebebeb (was #edeceb)
--sidebar-border: #3c3c3c       (was #3d3c3a)
```
Keep dark primary, destructive, warning, AI partner, issue state, and priority colors UNCHANGED.

**4. Neutralize hardcoded shadow and glass utilities (lines 314-395):**
Replace warm `hsl(30 10% 10% / ...)` in shadow-warm-* classes with neutral `hsl(0 0% 0% / ...)`.
Replace warm `hsl(40 20% 98% / ...)` in .glass classes with `hsl(0 0% 98% / ...)`.
Replace warm `hsl(40 10% 90% / ...)` in .glass and scrollbar with neutral `hsl(0 0% 90% / ...)`.
Keep AI utility hardcoded blues as-is (they use hsl(210) which is the AI brand color).
Update the scrollbar-color in .scrollbar-thin from `hsl(40 10% 90%)` to `hsl(0 0% 90%)`.

**5. Update theme-color in viewport export (layout.tsx will handle this, but note):**
The :root comment at top should say "Neutral" not "Warm Editorial". Update the section comment from "Warm off-white (v4 exact values)" to "Neutral Notion-like base".

Do NOT change: border-radius tokens, animation tokens, layout dimensions, @theme inline color mappings (they reference variables, not values), or any Tailwind utility class names in components.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm type-check && pnpm lint</automated>
  </verify>
  <done>All :root and .dark palette tokens are neutral Notion-like values. Font-sans leads with system-ui. Shadow/glass utilities use neutral hue. Dark mode has matching parity for every changed light token.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up font loading in layout.tsx and update viewport theme color</name>
  <files>frontend/src/app/layout.tsx</files>
  <action>
**1. Remove DM Sans import and variable:**
- Remove the `DM_Sans` import from `'next/font/google'` (keep `DM_Mono` and `JetBrains_Mono`)
- Remove the `dmSans` const declaration and configuration
- Remove `${dmSans.variable}` from the body className string
- The `--font-sans` in globals.css no longer references `var(--font-dm-sans)`, so this variable is unused

**2. Keep Fraunces link tag as-is:**
The Fraunces `<link>` tag in `<head>` must remain — it provides the display font for `.document-title` and `.font-display` classes. The eslint-disable comment stays.

**3. Keep DM Mono, JetBrains Mono, Geist Sans, Geist Mono:**
These remain for monospace and code rendering. The body className should still include their variables.

**4. Update viewport theme colors:**
Change the light theme-color from `'#FDFCFA'` to `'#ffffff'` (matching new --background).
Change the dark theme-color from `'#1A1A1A'` to `'#191919'` (matching new dark --background).

**5. Resulting body className should be:**
```tsx
className={`${GeistSans.variable} ${GeistMono.variable} ${dmMono.variable} ${jetbrainsMono.variable} font-sans antialiased`}
```

Do NOT remove the Geist font imports or the preconnect links for Google Fonts (needed for Fraunces).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm type-check && pnpm test --run</automated>
  </verify>
  <done>DM Sans removed from font loading. Body uses system font stack via globals.css --font-sans. Viewport theme colors match new palette. Frontend compiles and all tests pass.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm type-check` passes (no TypeScript errors from layout.tsx changes)
2. `cd frontend && pnpm lint` passes (no ESLint warnings)
3. `cd frontend && pnpm test --run` passes (existing component tests green — no regressions)
4. `grep "system-ui" frontend/src/app/globals.css` returns a match in --font-sans
5. `grep "DM_Sans" frontend/src/app/layout.tsx` returns NO matches (DM Sans removed)
6. `grep "#fdfcfa" frontend/src/app/globals.css` returns NO matches (warm background gone)
7. `grep "hsl(30" frontend/src/app/globals.css` returns NO matches (warm shadows neutralized)
</verification>

<success_criteria>
- System font stack is the primary body font (no DM Sans web font load)
- All base palette tokens (background, foreground, border, muted, card, popover, sidebar) use neutral Notion-like values
- Dark mode tokens have 1:1 parity with light mode changes
- Shadow and glass utility classes use neutral hue
- Fraunces display font preserved for document titles
- Frontend compiles, lints clean, and all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/28-visual-design-refresh/28-01-SUMMARY.md`
</output>
