---
phase: 42-command-palette-and-breadcrumb-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/command-palette/types.ts
  - frontend/src/features/command-palette/registry/ActionRegistry.ts
  - frontend/src/features/command-palette/registry/ActionRegistry.test.ts
  - frontend/src/features/command-palette/hooks/useCommandPalette.ts
  - frontend/src/features/command-palette/hooks/useRecentActions.ts
  - frontend/src/features/command-palette/hooks/useRecentActions.test.ts
  - frontend/src/features/command-palette/components/CommandPalette.tsx
  - frontend/src/features/command-palette/components/CommandPalette.test.tsx
  - frontend/src/features/command-palette/actions/fileActions.ts
  - frontend/src/features/command-palette/actions/editActions.ts
  - frontend/src/features/command-palette/actions/viewActions.ts
  - frontend/src/features/command-palette/actions/navigateActions.ts
  - frontend/src/features/command-palette/actions/noteActions.ts
  - frontend/src/features/command-palette/actions/aiActions.ts
autonomous: true
requirements:
  - CMD-01
  - CMD-04

must_haves:
  truths:
    - "Pressing Cmd+Shift+P opens a full-width overlay at top of editor area with a search input"
    - "Typing in the palette filters actions by fuzzy match"
    - "Selecting an action executes it and closes the palette"
    - "Recently used actions (up to 5) appear at top when palette opens with empty query"
    - "Each action shows icon, label, category badge, and keyboard shortcut"
    - "Escape closes the palette"
  artifacts:
    - path: "frontend/src/features/command-palette/types.ts"
      provides: "PaletteAction interface, ActionCategory type"
      exports: ["PaletteAction", "ActionCategory"]
    - path: "frontend/src/features/command-palette/registry/ActionRegistry.ts"
      provides: "Central action registration Map with register/unregister/getAll"
      exports: ["registerAction", "unregisterAction", "getAllActions", "getActionsByCategory"]
    - path: "frontend/src/features/command-palette/components/CommandPalette.tsx"
      provides: "Full-width overlay UI using shadcn Command (cmdk)"
      exports: ["CommandPalette"]
    - path: "frontend/src/features/command-palette/hooks/useCommandPalette.ts"
      provides: "Open/close state, global Cmd+Shift+P keyboard listener"
      exports: ["useCommandPalette"]
    - path: "frontend/src/features/command-palette/hooks/useRecentActions.ts"
      provides: "localStorage-backed recently used actions (cap 5)"
      exports: ["useRecentActions"]
  key_links:
    - from: "CommandPalette.tsx"
      to: "ActionRegistry.ts"
      via: "getAllActions() call when palette opens"
      pattern: "getAllActions"
    - from: "useCommandPalette.ts"
      to: "window keydown"
      via: "Cmd+Shift+P event listener"
      pattern: "addEventListener.*keydown"
    - from: "useRecentActions.ts"
      to: "localStorage"
      via: "read/write on palette open/action execute"
      pattern: "localStorage"
---

<objective>
Build the command palette feature: a VS Code-style Cmd+Shift+P overlay with fuzzy-searchable actions, category badges, keyboard shortcuts display, and recently-used tracking.

Purpose: Give power users keyboard-first access to all editor actions without memorizing shortcuts.
Output: CommandPalette component, ActionRegistry, 6 action category modules, useCommandPalette hook, useRecentActions hook, unit tests.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-command-palette-and-breadcrumb-navigation/42-CONTEXT.md
@.planning/phases/42-command-palette-and-breadcrumb-navigation/42-RESEARCH.md

@frontend/src/features/file-browser/components/QuickOpen.tsx
@frontend/src/features/file-browser/hooks/useQuickOpen.ts
@frontend/src/components/ui/command.tsx
@frontend/src/features/editor/types.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/features/editor/types.ts:
```typescript
export type FileSource = 'artifact' | 'local' | 'note' | 'remote';
export type EditorMode = 'edit' | 'preview';
export interface OpenFile {
  id: string; name: string; path: string; source: FileSource;
  language: string; content: string; isDirty: boolean; isReadOnly: boolean;
}
```

From frontend/src/features/file-browser/hooks/useFileTree.ts:
```typescript
export interface FileTreeItem {
  id: string; name: string; path: string;
  type: 'file' | 'directory'; source: FileSource;
  language?: string; children?: FileTreeItem[];
}
```

From frontend/src/features/file-browser/stores/FileStore.ts:
```typescript
export class FileStore {
  openFiles: Map<string, OpenFile>;
  activeFileId: string | null;
  get activeFile(): OpenFile | undefined;
  get tabs(): OpenFile[];
  openFile(file: Omit<OpenFile, 'isDirty'>): void;
  closeFile(id: string): void;
  closeAll(): void;
  closeOthers(keepId: string): void;
  updateContent(id: string, content: string): void;
}
```

From QuickOpen.tsx pattern (cmdk usage):
```typescript
// Dialog + Command + CommandInput + CommandList + CommandEmpty + CommandGroup + CommandItem
// shouldFilter={false} for custom filtering
// CommandInput with value/onValueChange for controlled input
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, ActionRegistry, and useRecentActions with tests</name>
  <files>
    frontend/src/features/command-palette/types.ts,
    frontend/src/features/command-palette/registry/ActionRegistry.ts,
    frontend/src/features/command-palette/registry/ActionRegistry.test.ts,
    frontend/src/features/command-palette/hooks/useRecentActions.ts,
    frontend/src/features/command-palette/hooks/useRecentActions.test.ts
  </files>
  <read_first>
    frontend/src/features/file-browser/hooks/useQuickOpen.ts,
    frontend/src/features/editor/types.ts,
    frontend/src/components/ui/command.tsx
  </read_first>
  <behavior>
    ActionRegistry tests:
    - registerAction adds action to registry; getAllActions returns it
    - registerAction returns unregister function; calling it removes the action
    - getActionsByCategory('file') returns only file-category actions
    - getAllActions returns empty array when no actions registered
    - Registering action with same ID overwrites previous

    useRecentActions tests:
    - addRecent pushes action ID to front of recent list
    - Recent list capped at 5 items (oldest dropped)
    - Duplicate ID moves to front instead of adding twice
    - getRecent reads from localStorage on each call (not cached)
    - Works when localStorage is empty (returns [])
  </behavior>
  <action>
    1. Create `frontend/src/features/command-palette/types.ts`:
       ```typescript
       export type ActionCategory = 'file' | 'edit' | 'view' | 'navigate' | 'note' | 'ai';

       export interface PaletteAction {
         id: string;
         label: string;
         category: ActionCategory;
         icon: React.ComponentType<{ className?: string }>;
         shortcut?: string; // Display string e.g. "Cmd+S"
         execute: () => void;
         priority?: number; // Lower = higher priority
       }
       ```

    2. Create `ActionRegistry.ts` as a module-level Map<string, PaletteAction>:
       - `registerAction(action: PaletteAction): () => void` -- adds to map, returns unregister fn
       - `unregisterAction(id: string): void`
       - `getAllActions(): PaletteAction[]` -- returns array sorted by priority
       - `getActionsByCategory(category: ActionCategory): PaletteAction[]`
       NOT a MobX store -- static data, palette reads on open.

    3. Create `useRecentActions.ts`:
       - localStorage key: `pilot-space:recent-actions`
       - `addRecent(actionId: string)`: read current list, prepend, dedup, cap at 5, write back
       - `getRecent(): string[]`: read from localStorage fresh each time (no stale cache)
       - Returns `{ addRecent, getRecent }` tuple

    4. Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/frontend && pnpm test -- --run src/features/command-palette/registry/ActionRegistry.test.ts src/features/command-palette/hooks/useRecentActions.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "registerAction" frontend/src/features/command-palette/registry/ActionRegistry.ts
    - grep -q "getAllActions" frontend/src/features/command-palette/registry/ActionRegistry.ts
    - grep -q "getActionsByCategory" frontend/src/features/command-palette/registry/ActionRegistry.ts
    - grep -q "PaletteAction" frontend/src/features/command-palette/types.ts
    - grep -q "ActionCategory" frontend/src/features/command-palette/types.ts
    - grep -q "addRecent" frontend/src/features/command-palette/hooks/useRecentActions.ts
    - grep -q "localStorage" frontend/src/features/command-palette/hooks/useRecentActions.ts
    - All tests pass
  </acceptance_criteria>
  <done>ActionRegistry CRUD works, useRecentActions persists to localStorage with 5-item cap, all unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CommandPalette component, useCommandPalette hook, and 6 action modules</name>
  <files>
    frontend/src/features/command-palette/hooks/useCommandPalette.ts,
    frontend/src/features/command-palette/components/CommandPalette.tsx,
    frontend/src/features/command-palette/components/CommandPalette.test.tsx,
    frontend/src/features/command-palette/actions/fileActions.ts,
    frontend/src/features/command-palette/actions/editActions.ts,
    frontend/src/features/command-palette/actions/viewActions.ts,
    frontend/src/features/command-palette/actions/navigateActions.ts,
    frontend/src/features/command-palette/actions/noteActions.ts,
    frontend/src/features/command-palette/actions/aiActions.ts
  </files>
  <read_first>
    frontend/src/features/file-browser/components/QuickOpen.tsx,
    frontend/src/features/file-browser/hooks/useQuickOpen.ts,
    frontend/src/components/ui/command.tsx,
    frontend/src/features/command-palette/types.ts,
    frontend/src/features/command-palette/registry/ActionRegistry.ts,
    frontend/src/features/command-palette/hooks/useRecentActions.ts
  </read_first>
  <behavior>
    CommandPalette tests:
    - Renders nothing when isOpen is false (palette hidden)
    - Renders Dialog with CommandInput when isOpen is true
    - Filters actions by query: typing "save" shows only actions with "save" in label
    - Executes action on select: clicking an action calls action.execute() and closes palette
    - Shows recently used actions when query is empty
    - Shows "No matching actions" empty state when query matches nothing
    - Pressing Escape closes the palette
  </behavior>
  <action>
    1. Create `useCommandPalette.ts`:
       - `isOpen` boolean state, `open()`, `close()`, `toggle()` callbacks
       - Global `useEffect` keyboard listener: Cmd+Shift+P (metaKey||ctrlKey + shiftKey + key==='p') toggles
       - On open: reset query to '', read recent actions via getRecent()
       - Returns `{ isOpen, open, close, toggle }`

    2. Create `CommandPalette.tsx` following the QuickOpen pattern:
       - Uses shadcn `Dialog` + `Command` (cmdk) -- same structure as QuickOpen.tsx
       - Position: `top-12 translate-y-0` (full-width at top, like VS Code)
       - Width: `max-w-[640px]` (slightly wider than QuickOpen's 560px)
       - `shouldFilter={false}` -- custom fuzzy filtering via ActionRegistry
       - Input placeholder: "Type a command..."
       - Filter logic: fuzzy match query against action.label (reuse fuzzyMatch pattern from useQuickOpen)
       - Group results: Recently Used (from useRecentActions, only when query is empty), then by category
       - Each CommandItem shows: action.icon (16x16), action.label, category Badge, shortcut span (right-aligned, text-xs text-muted-foreground font-mono)
       - On select: execute action, addRecent(action.id), close palette
       - Empty state: "No matching actions" with muted suggestion text
       - NOT wrapped in observer() -- reads from ActionRegistry (non-MobX), not MobX stores

    3. Create `CommandPalette.test.tsx`:
       - Write behavioral tests per the <behavior> block above
       - Mock ActionRegistry (getAllActions) to return test actions
       - Mock useRecentActions to return controlled recent IDs
       - Use @testing-library/react for rendering + user interaction
       - Test that Dialog renders conditionally based on open state
       - Test fuzzy filter by setting query and asserting visible items
       - Test action execution by clicking a CommandItem and asserting execute() called

    4. Create 6 action modules (each exports a `register{Category}Actions(context)` function):

       `fileActions.ts` -- context needs FileStore:
       - "New File" (no shortcut), "Save" (Cmd+S), "Close Tab" (Cmd+W), "Close All Tabs"

       `editActions.ts` -- context needs editor instance:
       - "Undo" (Cmd+Z), "Redo" (Cmd+Shift+Z), "Find" (Cmd+F), "Replace" (Cmd+H)

       `viewActions.ts` -- context needs layout state:
       - "Toggle Sidebar" (Cmd+B), "Toggle Preview" (Cmd+Shift+V), "Toggle Outline" (Cmd+Shift+O)

       `navigateActions.ts` -- context needs FileStore + editor:
       - "Go to File" (Cmd+P) -- triggers QuickOpen, "Go to Line" (Cmd+G) -- opens go-to-line dialog, "Go to Symbol" (Cmd+Shift+O)

       `noteActions.ts` -- context needs editor:
       - "Insert PM Block: Decision", "Insert PM Block: Risk", etc. for each PM block type (no shortcuts)
       - "Toggle Focus Mode"

       `aiActions.ts` -- context needs AI state:
       - "Toggle Ghost Text", "Extract Issues from Note"

       Each register function takes a context object with the stores/instances it needs, calls `registerAction()` for each action, and returns an unregister-all cleanup function. Actions use closures over the context.

       Icons: use Lucide icons -- File, Save, X, Undo2, Redo2, Search, Replace, PanelLeft, Eye, Hash, Terminal, Sparkles, etc.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/frontend && pnpm test -- --run src/features/command-palette/components/CommandPalette.test.tsx --reporter=verbose && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useCommandPalette" frontend/src/features/command-palette/hooks/useCommandPalette.ts
    - grep -q "CommandPalette" frontend/src/features/command-palette/components/CommandPalette.tsx
    - grep -q "registerFileActions" frontend/src/features/command-palette/actions/fileActions.ts
    - grep -q "registerEditActions" frontend/src/features/command-palette/actions/editActions.ts
    - grep -q "registerViewActions" frontend/src/features/command-palette/actions/viewActions.ts
    - grep -q "registerNavigateActions" frontend/src/features/command-palette/actions/navigateActions.ts
    - grep -q "registerNoteActions" frontend/src/features/command-palette/actions/noteActions.ts
    - grep -q "registerAiActions" frontend/src/features/command-palette/actions/aiActions.ts
    - grep -q "CommandInput" frontend/src/features/command-palette/components/CommandPalette.tsx
    - grep -q "shouldFilter={false}" frontend/src/features/command-palette/components/CommandPalette.tsx
    - CommandPalette.test.tsx tests pass
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>CommandPalette renders as full-width overlay with fuzzy search, category grouping, recently-used section, and 6 action modules registered. Behavioral tests green. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- All command-palette tests pass: `cd frontend && pnpm test -- --run src/features/command-palette/ --reporter=verbose`
- TypeScript compiles: `cd frontend && pnpm type-check`
- ESLint passes: `cd frontend && pnpm lint`
</verification>

<success_criteria>
- ActionRegistry supports register/unregister/getAll/getByCategory
- CommandPalette opens on Cmd+Shift+P, shows fuzzy-filtered actions grouped by category
- Recently used actions appear at top (localStorage, cap 5)
- Each action displays icon, label, category badge, keyboard shortcut
- 6 action category modules define all actions from CONTEXT.md
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/42-command-palette-and-breadcrumb-navigation/42-01-SUMMARY.md`
</output>
