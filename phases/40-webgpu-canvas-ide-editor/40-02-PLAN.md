---
phase: 40-webgpu-canvas-ide-editor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/markdown-preview/MarkdownPreview.tsx
  - frontend/src/features/markdown-preview/plugins/remarkAdmonition.ts
  - frontend/src/features/markdown-preview/plugins/rehypeMermaid.ts
  - frontend/src/features/markdown-preview/__tests__/MarkdownPreview.test.tsx
autonomous: true
requirements:
  - PREVIEW-01

must_haves:
  truths:
    - "Markdown preview renders GFM tables, strikethrough, autolinks"
    - "Markdown preview renders KaTeX inline and block math"
    - "Markdown preview renders Mermaid diagrams"
    - "Markdown preview renders code blocks with syntax highlighting"
    - "Markdown preview renders custom admonition containers"
  artifacts:
    - path: "frontend/src/features/markdown-preview/MarkdownPreview.tsx"
      provides: "Full-featured markdown preview component"
      exports: ["MarkdownPreview"]
      min_lines: 50
    - path: "frontend/src/features/markdown-preview/plugins/remarkAdmonition.ts"
      provides: "Remark plugin for :::note/:::warning/:::tip containers"
      exports: ["remarkAdmonition"]
    - path: "frontend/src/features/markdown-preview/plugins/rehypeMermaid.ts"
      provides: "Rehype plugin to render mermaid code blocks"
      exports: ["rehypeMermaid"]
  key_links:
    - from: "frontend/src/features/markdown-preview/MarkdownPreview.tsx"
      to: "frontend/src/features/notes/editor/extensions/pm-blocks/renderers/"
      via: "MermaidPreview component reuse"
      pattern: "MermaidPreview"
---

<objective>
Create the standalone markdown preview component with full extended markdown support: GFM, KaTeX math, Mermaid diagrams, syntax-highlighted code blocks, and custom admonition containers.

Purpose: This component is used in the Edit/Preview toggle mode and can be developed independently of Monaco.
Output: MarkdownPreview component with remark/rehype plugin stack.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-CONTEXT.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-RESEARCH.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MarkdownPreview component with full remark/rehype plugin stack</name>
  <files>
    frontend/src/features/markdown-preview/MarkdownPreview.tsx,
    frontend/src/features/markdown-preview/plugins/remarkAdmonition.ts,
    frontend/src/features/markdown-preview/plugins/rehypeMermaid.ts,
    frontend/src/features/markdown-preview/__tests__/MarkdownPreview.test.tsx
  </files>
  <read_first>
    frontend/src/features/artifacts/components/renderers/MarkdownRenderer.tsx,
    frontend/src/features/notes/editor/extensions/pm-blocks/renderers/DecisionRenderer.tsx,
    .planning/phases/40-webgpu-canvas-ide-editor/40-UI-SPEC.md,
    .planning/phases/40-webgpu-canvas-ide-editor/40-RESEARCH.md
  </read_first>
  <behavior>
    - Renders GFM table markdown into an HTML table element
    - Renders `$E=mc^2$` into KaTeX inline math (contains class "katex")
    - Renders `$$\sum_{i=1}^n x_i$$` into KaTeX block math (contains class "katex-display")
    - Renders ```mermaid code block into MermaidPreview component
    - Renders ```typescript code block with syntax highlighting (contains class "hljs")
    - Renders :::note container into admonition div with data-admonition="note"
    - Renders :::warning container into admonition div with data-admonition="warning"
    - Renders :::tip container into admonition div with data-admonition="tip"
    - Sanitizes raw HTML (DOMPurify) to prevent XSS
    - Empty string input renders nothing (no crash)
  </behavior>
  <action>
    1. Create `frontend/src/features/markdown-preview/plugins/remarkAdmonition.ts`:
       - Import `visit` from `unist-util-visit` and types from `remark-directive`
       - Export `remarkAdmonition` remark plugin function
       - Transform `:::note`, `:::warning`, `:::tip`, `:::danger`, `:::info` container directives
       - Convert each to a `<div data-admonition="{type}" class="admonition admonition-{type}">` with child content
       - Use `h` (hast) properties: `data.hName = 'div'`, `data.hProperties = { 'data-admonition': node.name, className: ['admonition', 'admonition-' + node.name] }`

    2. Create `frontend/src/features/markdown-preview/plugins/rehypeMermaid.ts`:
       - Export `rehypeMermaid` rehype plugin function
       - Visit `element` nodes, find `<pre><code class="language-mermaid">` patterns
       - Replace with a custom element: `<div data-mermaid="{chart content}"></div>`
       - The MarkdownPreview component maps `div[data-mermaid]` to the `MermaidPreview` React component

    3. Create `frontend/src/features/markdown-preview/MarkdownPreview.tsx`:
       - `'use client'` directive
       - Import ReactMarkdown from `react-markdown`
       - Import plugins: `remarkGfm`, `remarkMath`, `remarkDirective`, `rehypeKatex`, `rehypeRaw`, `rehypeHighlight`
       - Import custom plugins: `remarkAdmonition`, `rehypeMermaid`
       - Import `DOMPurify` from `dompurify`
       - Import `katex/dist/katex.min.css`
       - Import existing `MermaidPreview` from `@/features/notes/editor/extensions/pm-blocks/renderers/` (or its current location -- check read_first)

       Component `MarkdownPreview({ content, className }: { content: string; className?: string })`:
       - Sanitize content via `DOMPurify.sanitize(content)`
       - Render `<ReactMarkdown>` with plugin chain:
         ```
         remarkPlugins={[remarkGfm, remarkMath, remarkDirective, remarkAdmonition]}
         rehypePlugins={[rehypeRaw, rehypeKatex, rehypeHighlight, rehypeMermaid]}
         ```
       - Custom `components` override:
         - `code`: If `className` matches `language-mermaid`, render `<MermaidPreview chart={String(children)} />`
         - `div`: If has `data-mermaid` attribute, render `<MermaidPreview chart={props['data-mermaid']} />`
         - Default: pass through
       - Outer wrapper: `<div className={cn("prose prose-sm dark:prose-invert max-w-[720px] mx-auto px-8", className)}>` (720px max-width per UI-SPEC)
       - Code blocks in preview: `bg-background-subtle rounded-md p-2 font-mono text-sm` (matching UI-SPEC)

    4. Write tests in `frontend/src/features/markdown-preview/__tests__/MarkdownPreview.test.tsx`:
       - Mock `MermaidPreview` component (it relies on browser APIs)
       - Mock `dompurify` sanitize to return input unchanged (unit test focus)
       - Test all behaviors listed above using `render()` + `screen.getByText` / container queries
       - For KaTeX: check that rendered output contains element with class `katex`
       - For code blocks: check `hljs` class in output
       - For admonitions: check `data-admonition` attribute
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/frontend && pnpm test -- --run src/features/markdown-preview/__tests__/MarkdownPreview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `MarkdownPreview.tsx` contains `import ReactMarkdown` and `import remarkGfm`
    - `MarkdownPreview.tsx` contains `remarkMath` and `rehypeKatex`
    - `MarkdownPreview.tsx` contains `DOMPurify.sanitize`
    - `MarkdownPreview.tsx` contains `max-w-[720px]`
    - `remarkAdmonition.ts` exports function `remarkAdmonition`
    - `remarkAdmonition.ts` contains `data-admonition`
    - `rehypeMermaid.ts` exports function `rehypeMermaid`
    - `MarkdownPreview.test.tsx` exits 0 with all tests passing
  </acceptance_criteria>
  <done>MarkdownPreview component renders GFM, KaTeX math, Mermaid diagrams, syntax-highlighted code, and admonition containers with DOMPurify sanitization. All tests pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test -- --run src/features/markdown-preview/__tests__/` — all tests pass
- `cd frontend && pnpm type-check` — no TypeScript errors
- MarkdownPreview component exists with full plugin stack
</verification>

<success_criteria>
MarkdownPreview component renders all extended markdown features (GFM, KaTeX, Mermaid, admonitions, code highlighting) with proper sanitization.
</success_criteria>

<output>
After completion, create `.planning/phases/40-webgpu-canvas-ide-editor/40-02-SUMMARY.md`
</output>
