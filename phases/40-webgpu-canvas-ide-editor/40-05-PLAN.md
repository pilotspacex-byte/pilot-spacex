---
phase: 40-webgpu-canvas-ide-editor
plan: 05
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontend/src/features/file-browser/components/FileTree.tsx
  - frontend/src/features/file-browser/components/FileTreeNode.tsx
  - frontend/src/features/file-browser/components/TabBar.tsx
  - frontend/src/features/file-browser/components/QuickOpen.tsx
  - frontend/src/features/file-browser/hooks/useFileTree.ts
  - frontend/src/features/file-browser/hooks/useQuickOpen.ts
  - frontend/src/features/editor/MonacoFileEditor.tsx
  - frontend/src/features/file-browser/__tests__/useFileTree.test.ts
  - frontend/src/features/file-browser/__tests__/TabBar.test.tsx
autonomous: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-03

must_haves:
  truths:
    - "File tree sidebar displays files from all four sources"
    - "User can expand/collapse directory nodes in the tree"
    - "User can click a file to open it in a tab"
    - "Tab bar shows all open files with close buttons"
    - "Cmd+P opens fuzzy finder overlay for quick file navigation"
    - "Code files open in MonacoFileEditor with syntax highlighting"
  artifacts:
    - path: "frontend/src/features/file-browser/components/FileTree.tsx"
      provides: "VS Code-style file tree sidebar"
      exports: ["FileTree"]
      min_lines: 60
    - path: "frontend/src/features/file-browser/components/TabBar.tsx"
      provides: "Open file tabs strip"
      exports: ["TabBar"]
      min_lines: 40
    - path: "frontend/src/features/file-browser/components/QuickOpen.tsx"
      provides: "Cmd+P fuzzy finder dialog"
      exports: ["QuickOpen"]
    - path: "frontend/src/features/editor/MonacoFileEditor.tsx"
      provides: "Code/artifact file editor"
      exports: ["default"]
    - path: "frontend/src/features/file-browser/hooks/useFileTree.ts"
      provides: "Tree flattening, expand/collapse, keyboard navigation"
      exports: ["useFileTree"]
  key_links:
    - from: "frontend/src/features/file-browser/components/FileTree.tsx"
      to: "frontend/src/features/file-browser/stores/FileStore.ts"
      via: "useFileStore().openFile()"
      pattern: "openFile"
    - from: "frontend/src/features/file-browser/components/TabBar.tsx"
      to: "frontend/src/features/file-browser/stores/FileStore.ts"
      via: "useFileStore().tabs, closeFile, activeFileId"
      pattern: "closeFile|activeFileId"
---

<objective>
Build the IDE file browser UI: file tree sidebar, tab bar, Cmd+P quick open, and MonacoFileEditor for code files. These components consume FileStore from Plan 01.

Purpose: Provide VS Code-style file navigation and multi-tab file editing.
Output: FileTree, TabBar, QuickOpen, MonacoFileEditor components.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-CONTEXT.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-RESEARCH.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-UI-SPEC.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useFileTree hook + FileTree + FileTreeNode</name>
  <files>
    frontend/src/features/file-browser/hooks/useFileTree.ts,
    frontend/src/features/file-browser/components/FileTree.tsx,
    frontend/src/features/file-browser/components/FileTreeNode.tsx,
    frontend/src/features/file-browser/__tests__/useFileTree.test.ts
  </files>
  <read_first>
    frontend/src/features/file-browser/stores/FileStore.ts,
    frontend/src/features/editor/types.ts,
    .planning/phases/40-webgpu-canvas-ide-editor/40-UI-SPEC.md
  </read_first>
  <behavior>
    useFileTree tests (pure logic, no DOM needed):
    - flattenedItems with no items returns empty array
    - flattenedItems with flat file list returns items at depth 0
    - flattenedItems with nested directories returns children at depth+1 when parent expanded
    - flattenedItems hides children of collapsed directories
    - toggleExpand adds id to expandedIds if absent, removes if present
    - expandAll expands all directory ids
    - collapseAll clears expandedIds
    - handleKeyDown ArrowDown moves selectedId to next item in flattenedItems
    - handleKeyDown ArrowUp moves selectedId to previous item
    - handleKeyDown ArrowRight on collapsed directory expands it
    - handleKeyDown ArrowRight on expanded directory moves to first child
    - handleKeyDown ArrowLeft on expanded directory collapses it
    - handleKeyDown ArrowLeft on file/collapsed dir moves to parent
    - handleKeyDown Enter on file calls onOpen callback
  </behavior>
  <action>
    1. Write `useFileTree.test.ts` FIRST (TDD red phase):
       - Import `renderHook`, `act` from `@testing-library/react`
       - Create test fixtures: nested FileTreeItem[] with 2 directories (one nested), 3 files
       - Test all behaviors listed above
       - Run tests — should fail (hook doesn't exist yet)

    2. Create `useFileTree.ts`:
       - `'use client'` directive
       - Interface `FileTreeItem`:
         ```typescript
         interface FileTreeItem {
           id: string;
           name: string;
           path: string;
           type: 'file' | 'directory';
           source: FileSource;
           language?: string;
           children?: FileTreeItem[];
         }
         ```
       - Hook `useFileTree(items: FileTreeItem[], onOpen?: (item: FileTreeItem) => void)` returns:
         - `expandedIds: Set<string>` — tracks which directories are expanded
         - `toggleExpand(id: string)` — toggles directory expansion
         - `expandAll()` / `collapseAll()`
         - `selectedId: string | null` — currently focused tree node
         - `setSelectedId(id: string | null)`
         - `flattenedItems: (FileTreeItem & { depth: number; isExpanded: boolean })[]` — flattened for virtualized rendering, respects expand state. Use `useMemo` with deps on `items` and `expandedIds`.
         - `handleKeyDown(e: KeyboardEvent)` — ArrowDown/Up/Left/Right/Enter handling
       - Flatten algorithm: recursive DFS, only descend into children when parent id is in expandedIds

    3. Create `FileTreeNode.tsx`:
       - `'use client'` directive
       - Props: `{ item: FileTreeItem & { depth: number; isExpanded: boolean }; isSelected: boolean; onSelect: () => void; onToggle: () => void; onOpen: (item: FileTreeItem) => void }`
       - Row height: 28px (per UI-SPEC file tree row height)
       - Indent: `paddingLeft: depth * 16px` (md spacing per level per UI-SPEC)
       - Directory: chevron icon (Lucide `ChevronRight`) rotates 90deg with 150ms ease-out transition on expand
       - File: icon mapped by extension using Lucide icons:
         - `.ts/.tsx/.js/.jsx` -> `FileCode`
         - `.md/.mdx` -> `FileText`
         - `.json` -> `FileJson`
         - `.css/.scss` -> `FileType`
         - `.png/.jpg/.svg` -> `FileImage`
         - default -> `File`
       - Selected state: `bg-background-muted` + 2px left border in `--primary` color
       - Hover state: `bg-background-subtle`
       - Text: 14px, --font-sans (Body typography per UI-SPEC)
       - Click: if directory, toggle expand; if file, call `onOpen(item)`
       - Context menu (right-click): `<DropdownMenu>` with "Open", "Copy Path" options. "Reveal in Finder" option only if `isTauri()` returns true.
       - ARIA: `role="treeitem"`, `aria-expanded` for directories, `aria-selected`

    4. Create `FileTree.tsx`:
       - `'use client'` directive
       - Wrapped in `observer()` (consumes FileStore)
       - Props: `{ items: FileTreeItem[]; className?: string }`
       - Uses `useFileTree(items, onOpen)` hook where `onOpen` calls `useFileStore().openFile()`
       - Background: `--background-subtle` (per UI-SPEC)
       - Header: section label "FILES" in 12px medium weight (Label typography)
       - Outer container: `<ScrollArea>` for scrolling
       - Virtualized via `react-virtuoso` `Virtuoso` component:
         - `totalCount={flattenedItems.length}`
         - `itemContent` renders `<FileTreeNode>` for each item
       - ARIA: `role="tree"` on outer container
       - Empty state (no items): "No files yet" heading + "Open a project folder or create a new note to get started." body (per copywriting contract)
       - `onKeyDown` handler wired to `useFileTree.handleKeyDown`

    5. Run tests — should pass (TDD green phase)
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/frontend && pnpm test -- --run src/features/file-browser/__tests__/useFileTree.test.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `useFileTree.test.ts` passes with all flatten, expand/collapse, and keyboard navigation tests
    - `FileTree.tsx` contains `observer(` wrapper and `useFileStore`
    - `FileTree.tsx` contains `role="tree"` and `Virtuoso`
    - `FileTree.tsx` contains `"No files yet"` empty state text
    - `FileTreeNode.tsx` contains `role="treeitem"` and `aria-expanded`
    - `FileTreeNode.tsx` contains `h-[28px]` or `height: 28` (28px row height)
    - `FileTreeNode.tsx` contains `ChevronRight` and `transition` (150ms rotate)
    - `FileTreeNode.tsx` contains `DropdownMenu` for context menu
    - `useFileTree.ts` exports `useFileTree` with `expandedIds`, `toggleExpand`, `flattenedItems`
    - `pnpm type-check` exits 0
  </acceptance_criteria>
  <done>FileTree sidebar with virtualized rendering, keyboard navigation, context menus, and proper ARIA tree semantics. useFileTree hook fully tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TabBar + QuickOpen + MonacoFileEditor</name>
  <files>
    frontend/src/features/file-browser/components/TabBar.tsx,
    frontend/src/features/file-browser/components/QuickOpen.tsx,
    frontend/src/features/file-browser/hooks/useQuickOpen.ts,
    frontend/src/features/editor/MonacoFileEditor.tsx,
    frontend/src/features/file-browser/__tests__/TabBar.test.tsx
  </files>
  <read_first>
    frontend/src/features/file-browser/stores/FileStore.ts,
    frontend/src/features/editor/types.ts,
    frontend/src/components/ui/command.tsx,
    frontend/src/components/ui/dialog.tsx,
    frontend/src/components/ui/popover.tsx,
    frontend/src/components/ui/tooltip.tsx,
    .planning/phases/40-webgpu-canvas-ide-editor/40-UI-SPEC.md
  </read_first>
  <behavior>
    TabBar tests:
    - Renders tab for each open file from FileStore
    - Active tab has primary bottom border class
    - Inactive tab has muted foreground text class
    - Click on tab calls setActiveFile
    - Close button on hover removes tab (calls closeFile)
    - Middle-click closes tab
    - Dirty indicator dot appears when file isDirty
    - Overflow: when more than visible width, shows "..." popover with remaining tabs
  </behavior>
  <action>
    1. Create `TabBar.tsx`:
       - `'use client'` directive
       - Wrapped in `observer()` (reads FileStore.tabs, activeFileId)
       - Height: 36px (per UI-SPEC tab bar height)
       - Background: `--background-subtle`
       - Layout: horizontal flex with `gap-2` (sm=8px per UI-SPEC tab bar item gap)
       - Each tab:
         - Active: 2px bottom border in `--primary`, text in `--foreground`
         - Inactive: no bottom border, text in `--muted-foreground`
         - Close `X` icon: appears on hover, 4px padding (xs spacing)
         - Middle-click: `onAuxClick` handler calls `closeFile`
         - Dirty indicator: 6px circle dot before filename in `--primary` color when `isDirty`. Tooltip: "Unsaved changes"
         - Appear animation: opacity 0->1 + translateX(-8px->0), 150ms ease-out
         - Close animation: opacity 1->0 + width collapse, 150ms ease-out
       - Tab order: insertion order (most recently opened on the right)
       - Overflow: when tabs overflow container width, show `<Popover>` with `...` trigger listing hidden tabs
       - ARIA: `role="tablist"` on container, `role="tab"` + `aria-selected` on each tab

    2. Create `useQuickOpen.ts`:
       - Hook `useQuickOpen(allFiles: FileTreeItem[])` returns:
         - `isOpen: boolean`
         - `open()` / `close()`
         - `query: string` / `setQuery(q: string)`
         - `results: FileTreeItem[]` — fuzzy-filtered, max 10 items
         - `selectedIndex: number` — keyboard selection
         - `moveUp()` / `moveDown()` / `selectCurrent()`
       - Fuzzy matching: simple substring match on file name and path. Matched characters should be trackable for highlighting.
       - Global keyboard listener: `Cmd+P` (metaKey + p) opens; Escape closes

    3. Create `QuickOpen.tsx`:
       - `'use client'` directive
       - Uses shadcn `<Dialog>` + `<Command>` (cmdk) components
       - Position: centered horizontally, 48px from top (2xl spacing per UI-SPEC)
       - Width: `max-w-[560px] w-[calc(100%-32px)]` (per UI-SPEC)
       - Input: 40px height, `--font-mono` for path display, auto-focus
       - Results: max 10 visible, keyboard arrow navigation
       - Match highlighting: matched characters in `--primary` color, bold weight
       - File path shown below file name in `--muted-foreground` (12px Label typography)
       - Open animation: opacity 0->1 + translateY(-8px->0), 200ms ease-spring
       - Close animation: opacity 1->0, 150ms ease-out
       - Dismiss: Escape or click outside
       - Select: Enter opens file via `fileStore.openFile()`
       - Empty state: "No matching files" / "Try a different search term." (per copywriting contract)

    4. Create `MonacoFileEditor.tsx`:
       - `'use client'` directive
       - Import `Editor` from `@monaco-editor/react`
       - Props: `{ file: OpenFile; onChange?: (content: string) => void }`
       - Uses `useMonacoTheme` for theming
       - Editor options (per UI-SPEC):
         ```typescript
         {
           fontSize: 14,
           lineHeight: 22.4,
           fontFamily: 'var(--font-mono)',
           wordWrap: 'off',  // code files don't wrap (unlike markdown)
           minimap: { enabled: false },
           lineNumbers: 'on',
           scrollbar: { verticalScrollbarSize: 7, horizontalScrollbarSize: 7 },
           readOnly: file.isReadOnly,
           cursorStyle: 'line',
           cursorBlinking: 'smooth',
           smoothScrolling: true,
         }
         ```
       - Language: derived from `file.language` (Monaco auto-detects from extension)
       - `data-lenis-prevent` on container
       - Loading: `<Skeleton className="h-full w-full" />`
       - Read-only files show "Read-only" badge in top-right corner

    5. Write `TabBar.test.tsx`:
       - Mock FileStore with observable tabs array
       - Test all behaviors listed above
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/frontend && pnpm test -- --run src/features/file-browser/__tests__/TabBar.test.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `TabBar.tsx` contains `observer(` and `role="tablist"`
    - `TabBar.tsx` contains `h-[36px]` or equivalent 36px height
    - `TabBar.tsx` contains `"Unsaved changes"` tooltip
    - `TabBar.tsx` contains `onAuxClick` for middle-click close
    - `QuickOpen.tsx` contains `Command` (cmdk) and `Dialog`
    - `QuickOpen.tsx` contains `max-w-[560px]`
    - `QuickOpen.tsx` contains `"No matching files"` empty state text
    - `MonacoFileEditor.tsx` contains `import Editor from '@monaco-editor/react'`
    - `MonacoFileEditor.tsx` contains `wordWrap: 'off'` and `data-lenis-prevent`
    - `MonacoFileEditor.tsx` contains `"Read-only"` badge text
    - `TabBar.test.tsx` passes
    - `pnpm type-check` exits 0
  </acceptance_criteria>
  <done>TabBar, QuickOpen, and MonacoFileEditor provide multi-file navigation with fuzzy search, tabs, and code editing.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test -- --run src/features/file-browser/__tests__/` — all tests pass (useFileTree + TabBar)
- `cd frontend && pnpm type-check` — no TypeScript errors
- FileTree, TabBar, QuickOpen, MonacoFileEditor all exist
</verification>

<success_criteria>
Complete file browser UI: tree sidebar with virtualized rendering, tab bar with dirty indicators, Cmd+P quick open with fuzzy search, and MonacoFileEditor for code files.
</success_criteria>

<output>
After completion, create `.planning/phases/40-webgpu-canvas-ide-editor/40-05-SUMMARY.md`
</output>
