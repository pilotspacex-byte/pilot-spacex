---
phase: 40-webgpu-canvas-ide-editor
plan: 03
type: execute
wave: 2
depends_on: ["40-01", "40-02"]
files_modified:
  - frontend/src/features/editor/MonacoNoteEditor.tsx
  - frontend/src/features/editor/hooks/useMonacoNote.ts
  - frontend/src/features/editor/hooks/useMonacoTheme.ts
  - frontend/src/features/editor/hooks/useMonacoViewZones.ts
  - frontend/src/features/editor/view-zones/PMBlockViewZone.tsx
  - frontend/src/features/editor/view-zones/ViewZoneManager.ts
  - frontend/src/features/editor/decorations/markdownDecorations.ts
  - frontend/src/features/editor/decorations/markdownDecorations.css
  - frontend/src/features/editor/EditorToolbar.tsx
  - frontend/src/features/editor/__tests__/ViewZoneManager.test.ts
  - frontend/src/features/editor/__tests__/markdownDecorations.test.ts
autonomous: true
requirements:
  - EDITOR-01
  - EDITOR-02
  - EDITOR-06
  - UX-03

must_haves:
  truths:
    - "Monaco editor renders note content as markdown text"
    - "PM blocks appear as React-rendered view zones inside the editor viewport"
    - "Rich formatting (bold, italic, headings, lists) shows visual decorations on markdown syntax"
    - "Editor toolbar shows Edit/Preview toggle and file metadata"
    - "View zone height dynamically adjusts via ResizeObserver"
  artifacts:
    - path: "frontend/src/features/editor/MonacoNoteEditor.tsx"
      provides: "Main Monaco-based note editor component"
      exports: ["default"]
      min_lines: 80
    - path: "frontend/src/features/editor/hooks/useMonacoViewZones.ts"
      provides: "Hook managing PM block view zones with React portals"
      exports: ["useMonacoViewZones"]
    - path: "frontend/src/features/editor/decorations/markdownDecorations.ts"
      provides: "Markdown formatting decorations for Monaco"
      exports: ["applyMarkdownDecorations"]
    - path: "frontend/src/features/editor/EditorToolbar.tsx"
      provides: "Edit/Preview toggle toolbar"
      exports: ["EditorToolbar"]
    - path: "frontend/src/features/editor/view-zones/ViewZoneManager.ts"
      provides: "View zone lifecycle manager with ResizeObserver"
      exports: ["ViewZoneManager"]
  key_links:
    - from: "frontend/src/features/editor/MonacoNoteEditor.tsx"
      to: "frontend/src/features/editor/hooks/useMonacoViewZones.ts"
      via: "hook call in component"
      pattern: "useMonacoViewZones"
    - from: "frontend/src/features/editor/hooks/useMonacoViewZones.ts"
      to: "frontend/src/features/editor/markers/pmBlockMarkers.ts"
      via: "parsePMBlockMarkers call"
      pattern: "parsePMBlockMarkers"
    - from: "frontend/src/features/editor/view-zones/PMBlockViewZone.tsx"
      to: "frontend/src/features/notes/editor/extensions/pm-blocks/renderers/"
      via: "dynamic import of block type renderers"
      pattern: "import.*Renderer"
    - from: "frontend/src/features/editor/MonacoNoteEditor.tsx"
      to: "frontend/src/features/markdown-preview/MarkdownPreview.tsx"
      via: "preview mode rendering"
      pattern: "MarkdownPreview"
---

<objective>
Build the core MonacoNoteEditor component that replaces NoteCanvasEditor. This includes Monaco initialization with the Pilot Space theme, markdown text decorations for rich formatting, PM block view zone rendering via React portals, and the Edit/Preview toolbar.

Purpose: This is the heart of the migration -- the note editing experience powered by Monaco's canvas renderer.
Output: MonacoNoteEditor component with view zones, decorations, and toolbar.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-CONTEXT.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-RESEARCH.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-UI-SPEC.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-01-SUMMARY.md
@.planning/phases/40-webgpu-canvas-ide-editor/40-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Monaco hooks — theme, markdown decorations, view zone manager</name>
  <files>
    frontend/src/features/editor/hooks/useMonacoTheme.ts,
    frontend/src/features/editor/decorations/markdownDecorations.ts,
    frontend/src/features/editor/decorations/markdownDecorations.css,
    frontend/src/features/editor/view-zones/ViewZoneManager.ts,
    frontend/src/features/editor/hooks/useMonacoViewZones.ts,
    frontend/src/features/editor/view-zones/PMBlockViewZone.tsx,
    frontend/src/features/editor/__tests__/ViewZoneManager.test.ts,
    frontend/src/features/editor/__tests__/markdownDecorations.test.ts
  </files>
  <read_first>
    frontend/src/features/editor/types.ts,
    frontend/src/features/editor/themes/pilotSpaceTheme.ts,
    frontend/src/features/editor/markers/pmBlockMarkers.ts,
    frontend/src/features/notes/editor/extensions/pm-blocks/renderers/DecisionRenderer.tsx,
    frontend/src/features/notes/editor/extensions/pm-blocks/renderers/RACIRenderer.tsx,
    .planning/phases/40-webgpu-canvas-ide-editor/40-UI-SPEC.md,
    .planning/phases/40-webgpu-canvas-ide-editor/40-RESEARCH.md
  </read_first>
  <behavior>
    ViewZoneManager tests (mock monaco editor API):
    - addZone returns an HTMLDivElement and tracks zone in internal map
    - removeZone removes zone from map and disconnects ResizeObserver
    - removeAll clears all zones
    - getZoneDomNode returns the div for a known blockId, undefined for unknown
    - updatePositions calls editor.changeViewZones to update afterLineNumber

    markdownDecorations tests (regex parsing — no Monaco needed):
    - Heading regex: "# Title" matches H1, "## Sub" matches H2, "### Third" matches H3
    - Bold regex: "**bold text**" matches bold range
    - Italic regex: "*italic*" and "_italic_" match italic range
    - Inline code regex: "`code`" matches code range
    - List regex: "- item", "* item", "1. item" match list bullet
    - Blockquote regex: "> quote" matches blockquote
    - Mixed line: "# **Bold heading**" matches both heading and bold
    - No false positives: "not_italic_text" does not match italic (underscores within words)
  </behavior>
  <action>
    1. Create `useMonacoTheme.ts`:
       - `'use client'` directive
       - Hook `useMonacoTheme(monaco: typeof import('monaco-editor') | null)` that:
         a. Calls `definePilotSpaceThemes(monaco)` on first render when monaco is available
         b. Detects current theme from document class or CSS variable (check `document.documentElement.classList.contains('dark')`)
         c. Returns `currentTheme: string` — either `THEME_LIGHT` or `THEME_DARK`
         d. Listens for theme changes via MutationObserver on `<html>` class attribute

    2. Create `markdownDecorations.ts`:
       - Export function `applyMarkdownDecorations(editor: monaco.editor.IStandaloneCodeEditor, monacoInstance: typeof monaco): IDisposable`
       - ALSO export the individual regex patterns and a `parseMarkdownLine(line: string): MarkdownDecoration[]` pure function for testability
       - On each model content change, scan lines for markdown syntax and apply decorations:
         - `# Heading` lines: `fontStyle: 'bold'`, fontSize scaled (H1=24px, H2=20px, H3=16px) via `inlineClassName`
         - `**bold**` ranges: `fontStyle: 'bold'` via inline className
         - `*italic*` / `_italic_` ranges: `fontStyle: 'italic'` via inline className
         - `- list item` / `* list item` / `1. list item`: list bullet gets `--muted-foreground` color
         - `` `inline code` ``: `font-family: --font-mono`, `background: --background-subtle` via inline className
         - `> blockquote`: left border decoration via `glyphMarginClassName`
       - Use `editor.deltaDecorations()` for efficient updates
       - Return a disposable that cleans up the content change listener
       - CSS classes defined in companion `markdownDecorations.css`:
         ```css
         .md-h1 { font-size: 24px; font-weight: 700; }
         .md-h2 { font-size: 20px; font-weight: 600; }
         .md-h3 { font-size: 16px; font-weight: 600; }
         .md-bold { font-weight: 700; }
         .md-italic { font-style: italic; }
         .md-inline-code { font-family: var(--font-mono); background: hsl(var(--background-subtle)); border-radius: 3px; padding: 1px 4px; }
         .md-blockquote-glyph { border-left: 3px solid hsl(var(--primary)); }
         .md-list-bullet { color: hsl(var(--muted-foreground)); }
         ```

    3. Create `ViewZoneManager.ts`:
       - Class `ViewZoneManager` managing view zone lifecycle:
         - `private zones: Map<string, { zoneId: string; domNode: HTMLDivElement; observer: ResizeObserver }>`
         - `constructor(private editor: monaco.editor.IStandaloneCodeEditor)`
         - `addZone(blockId: string, afterLineNumber: number, initialHeight: number): HTMLDivElement` — creates domNode, adds view zone, attaches ResizeObserver that calls `editor.changeViewZones()` to update `heightInPx` on resize (debounced 50ms)
         - `removeZone(blockId: string)` — disconnects ResizeObserver, removes view zone, returns domNode for React cleanup
         - `removeAll()` — removes all zones
         - `getZoneDomNode(blockId: string): HTMLDivElement | undefined`
         - `updatePositions(blocks: PMBlockMarker[])` — recalculates afterLineNumber for each zone

    4. Create `useMonacoViewZones.ts`:
       - Hook `useMonacoViewZones(editor: monaco.editor.IStandaloneCodeEditor | null, content: string)`
       - Calls `parsePMBlockMarkers(content)` to find PM blocks
       - Manages a `ViewZoneManager` instance (ref)
       - On block changes: adds/removes zones as needed
       - Returns `portals: ReactPortal[]` — array of `createPortal(<PMBlockViewZone ...>, domNode)` for each active zone
       - Cleanup effect: calls `manager.removeAll()` on unmount
       - IMPORTANT: Do NOT wrap view zone React content in `observer()` — React 19 flushSync constraint (see project memory)

    5. Create `PMBlockViewZone.tsx`:
       - `'use client'` directive
       - Component `PMBlockViewZone({ type, data, raw }: { type: PMBlockType; data: Record<string,unknown> | null; raw: string })`
       - NOT wrapped in `observer()` — plain React component
       - Outer div: `bg-background-subtle border-y border-border-subtle p-4` (per UI-SPEC: --background-subtle, 1px top/bottom border, 16px padding)
       - Min height: 80px (per UI-SPEC view zone minimum)
       - Collapse/expand toggle: chevron icon, 200ms height transition
       - Lazy-loads the appropriate renderer based on `type`:
         ```typescript
         const rendererMap: Record<PMBlockType, React.LazyExoticComponent<any>> = {
           'decision': lazy(() => import('@/features/notes/editor/extensions/pm-blocks/renderers/DecisionRenderer')),
           'raci': lazy(() => import('@/features/notes/editor/extensions/pm-blocks/renderers/RACIRenderer')),
           // ... all 10 types
         };
         ```
       - Wraps lazy component in `<Suspense fallback={<Skeleton className="h-20 w-full" />}>`
       - Passes `data` as props to the renderer (adapting from TipTap attrs to plain props — check each renderer's expected props in read_first)

    6. Write tests:
       a. `frontend/src/features/editor/__tests__/ViewZoneManager.test.ts`:
          - Create a mock Monaco editor with `changeViewZones` spy
          - Test `addZone` creates domNode and calls `changeViewZones`
          - Test `removeZone` calls `changeViewZones` and disconnects ResizeObserver
          - Test `removeAll` clears all zones
          - Test `getZoneDomNode` returns correct node or undefined
          - Test `updatePositions` calls `changeViewZones` with new line numbers

       b. `frontend/src/features/editor/__tests__/markdownDecorations.test.ts`:
          - Import `parseMarkdownLine` (pure function) and regex patterns
          - Test each markdown pattern: H1-H3, bold, italic, inline code, list, blockquote
          - Test mixed patterns on same line
          - Test no false positives (e.g., underscores within words)
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/frontend && pnpm test -- --run src/features/editor/__tests__/ViewZoneManager.test.ts src/features/editor/__tests__/markdownDecorations.test.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `useMonacoTheme.ts` exports `useMonacoTheme` and contains `definePilotSpaceThemes`
    - `markdownDecorations.ts` exports `applyMarkdownDecorations` and `parseMarkdownLine`
    - `markdownDecorations.css` exists with `.md-h1`, `.md-bold`, `.md-italic` classes
    - `ViewZoneManager.ts` exports `class ViewZoneManager` with methods `addZone`, `removeZone`, `removeAll`
    - `ViewZoneManager.ts` contains `ResizeObserver`
    - `useMonacoViewZones.ts` exports `useMonacoViewZones` and contains `createPortal` and `parsePMBlockMarkers`
    - `PMBlockViewZone.tsx` does NOT contain `observer(` — plain component
    - `PMBlockViewZone.tsx` contains `Suspense` and `lazy(`
    - `PMBlockViewZone.tsx` contains `bg-background-subtle` and `min-h-[80px]`
    - `ViewZoneManager.test.ts` and `markdownDecorations.test.ts` pass
    - `pnpm type-check` exits 0
  </acceptance_criteria>
  <done>All Monaco hooks and view zone infrastructure created — theme binding, markdown decorations, ResizeObserver-based view zone management, and PM block portal rendering. Tests cover ViewZoneManager lifecycle and markdown regex parsing.</done>
</task>

<task type="auto">
  <name>Task 2: MonacoNoteEditor component + EditorToolbar</name>
  <files>
    frontend/src/features/editor/MonacoNoteEditor.tsx,
    frontend/src/features/editor/EditorToolbar.tsx
  </files>
  <read_first>
    frontend/src/components/editor/NoteCanvasEditor.tsx,
    frontend/src/features/editor/hooks/useMonacoTheme.ts,
    frontend/src/features/editor/hooks/useMonacoViewZones.ts,
    frontend/src/features/editor/decorations/markdownDecorations.ts,
    frontend/src/features/editor/types.ts,
    frontend/src/features/markdown-preview/MarkdownPreview.tsx,
    .planning/phases/40-webgpu-canvas-ide-editor/40-UI-SPEC.md
  </read_first>
  <action>
    1. Create `EditorToolbar.tsx`:
       - `'use client'` directive
       - Props: `{ mode: EditorMode; onModeChange: (mode: EditorMode) => void; fileName: string; isDirty: boolean; isReadOnly: boolean; language: string }`
       - Layout: horizontal flex bar with:
         - Left: file name text (14px, --font-sans), language badge (`<Badge variant="outline">`), read-only badge if applicable ("Read-only" per copywriting contract)
         - Center: Edit/Preview toggle buttons (text labels "Edit" / "Preview" per UI-SPEC, not icons)
         - Right: dirty indicator dot (8px circle in --primary color, scale animation 150ms --ease-spring, tooltip "Unsaved changes")
       - Toggle buttons: active state has `--primary` text + 2px bottom border; inactive has `--muted-foreground` text
       - Height: 36px (tab bar height from UI-SPEC)
       - Background: `--background-subtle`

    2. Create `MonacoNoteEditor.tsx`:
       - `'use client'` directive
       - Import `Editor` from `@monaco-editor/react`
       - Import hooks: `useMonacoTheme`, `useMonacoViewZones`
       - Import `applyMarkdownDecorations` from decorations
       - Import `EditorToolbar` and `MarkdownPreview` (from plan 02)

       Props interface `MonacoNoteEditorProps`:
       ```typescript
       {
         noteId: string;
         initialContent: string;
         onChange?: (content: string) => void;
         isReadOnly?: boolean;
         className?: string;
       }
       ```

       Component logic:
       - `const [mode, setMode] = useState<EditorMode>('edit')`
       - `const [content, setContent] = useState(initialContent)`
       - `const editorRef = useRef<monaco.editor.IStandaloneCodeEditor | null>(null)`
       - `const monacoRef = useRef<typeof monaco | null>(null)`
       - `const theme = useMonacoTheme(monacoRef.current)`
       - `const viewZonePortals = useMonacoViewZones(editorRef.current, content)`

       `onMount` callback:
       - Store editor and monaco refs
       - Call `applyMarkdownDecorations(editor, monacoInstance)` — store disposable for cleanup
       - Set editor language to `'markdown'`

       `onChange` callback:
       - Update content state
       - Call `props.onChange?.(newContent)` for parent auto-save

       Editor options (per UI-SPEC):
       ```typescript
       {
         fontSize: 14,
         lineHeight: 22.4,  // 14 * 1.6
         fontFamily: 'var(--font-mono)',
         wordWrap: 'on',      // markdown files wrap
         minimap: { enabled: false },
         lineNumbers: 'on',
         glyphMargin: true,
         scrollbar: { verticalScrollbarSize: 7, horizontalScrollbarSize: 7, verticalHasArrows: false },
         padding: { top: 16 },
         renderLineHighlight: 'line',
         cursorStyle: 'line',
         cursorBlinking: 'smooth',
         smoothScrolling: true,
         readOnly: isReadOnly,
       }
       ```

       Render:
       - `data-lenis-prevent` on outer container div (prevent Lenis scroll hijacking)
       - `<EditorToolbar>` at top
       - Crossfade wrapper (200ms opacity transition per UI-SPEC):
         - If `mode === 'edit'`: render `<Editor>` from `@monaco-editor/react`
         - If `mode === 'preview'`: render `<MarkdownPreview content={content} />`
       - Render `{viewZonePortals}` after Editor (portals render into view zone DOM nodes)

       Loading state: `<Skeleton className="h-full w-full" />` via Editor's `loading` prop
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `MonacoNoteEditor.tsx` contains `import Editor from '@monaco-editor/react'`
    - `MonacoNoteEditor.tsx` contains `data-lenis-prevent`
    - `MonacoNoteEditor.tsx` contains `useMonacoTheme` and `useMonacoViewZones`
    - `MonacoNoteEditor.tsx` contains `minimap: { enabled: false }` and `fontSize: 14`
    - `MonacoNoteEditor.tsx` contains `wordWrap: 'on'`
    - `MonacoNoteEditor.tsx` imports `MarkdownPreview`
    - `EditorToolbar.tsx` contains `"Edit"` and `"Preview"` text labels
    - `EditorToolbar.tsx` contains `"Unsaved changes"` tooltip text
    - `EditorToolbar.tsx` contains `"Read-only"` badge text
    - `pnpm type-check` exits 0
  </acceptance_criteria>
  <done>MonacoNoteEditor renders markdown in Monaco with theme, decorations, PM block view zones, and Edit/Preview toggle toolbar. Crossfade transition between edit and preview modes.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test -- --run src/features/editor/__tests__/` — ViewZoneManager and markdownDecorations tests pass
- `cd frontend && pnpm type-check` — no TypeScript errors
- MonacoNoteEditor component exists with Monaco Editor, view zones, decorations, toolbar
- EditorToolbar has Edit/Preview toggle with UI-SPEC copywriting
</verification>

<success_criteria>
MonacoNoteEditor component replaces the TipTap editor rendering layer. Notes display in Monaco with markdown decorations, PM blocks as view zones, and Edit/Preview mode toggle.
</success_criteria>

<output>
After completion, create `.planning/phases/40-webgpu-canvas-ide-editor/40-03-SUMMARY.md`
</output>
