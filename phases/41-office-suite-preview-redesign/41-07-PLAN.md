---
phase: 41-office-suite-preview-redesign
plan: 07
type: execute
wave: 3
depends_on: ["41-06"]
files_modified: []
autonomous: false
requirements:
  - XLSX-RENDER
  - DOCX-RENDER
  - PPTX-RENDER
  - ANNOT-PANEL
  - RESPONSIVE
  - KEYBOARD

must_haves:
  truths:
    - "All six success criteria from roadmap are visually confirmed"
    - "XLSX frozen headers, search, sheet tabs, column resize work"
    - "DOCX renders in sandboxed iframe with ToC navigation"
    - "PPTX renders slides with keyboard nav, fullscreen, thumbnails, annotations"
    - "Annotation panel creates/edits/deletes with persistence"
    - "All previews respond correctly to maximize/restore"
    - "Keyboard navigation works seamlessly"
  artifacts: []
  key_links: []
---

<objective>
Run full quality gates (type-check, lint, tests) and verify all six phase success criteria visually. Human confirms the Office Suite Preview redesign is complete.

Purpose: Final gatekeeper before marking phase complete -- automated tests verify code, human verifies UX.
Output: Phase 41 verified and complete.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-office-suite-preview-redesign/41-UI-SPEC.md
@.planning/phases/41-office-suite-preview-redesign/41-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full quality gates</name>
  <files></files>
  <read_first>
    frontend/package.json,
    backend/pyproject.toml
  </read_first>
  <action>
    Run all quality gates for both frontend and backend:

    **Frontend:**
    ```bash
    cd frontend && pnpm type-check
    cd frontend && pnpm lint
    cd frontend && pnpm test -- --run
    ```

    **Backend:**
    ```bash
    cd backend && uv run ruff check
    cd backend && uv run pyright
    ```

    Fix any failures that are caused by Phase 41 changes (annotation files, FilePreviewModal changes). Do NOT fix pre-existing failures unrelated to this phase.

    Report pass/fail for each gate.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/frontend && pnpm type-check 2>&1 | tail -3 && pnpm test -- --run 2>&1 | tail -5 && cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/backend && uv run ruff check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm type-check` exits 0 (or only pre-existing errors)
    - `pnpm lint` exits 0 (or only pre-existing warnings)
    - `pnpm test -- --run` all tests pass (including XlsxRenderer, DocxRenderer, PptxRenderer, PptxAnnotationPanel tests)
    - `uv run ruff check` passes on all annotation backend files
  </acceptance_criteria>
  <done>All automated quality gates pass for Phase 41 code</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all six success criteria</name>
  <what-built>
    Complete Office Suite Preview redesign with three renderers (XLSX, DOCX, PPTX), annotation panel, responsive layouts, and keyboard navigation.
  </what-built>
  <how-to-verify>
    Start dev servers: `cd backend && uv run uvicorn pilot_space.main:app --reload --port 8000` and `cd frontend && pnpm dev`

    **Criterion 1 — XLSX Preview (Google Sheets feel):**
    1. Navigate to a project with an uploaded .xlsx file
    2. Click the file to open FilePreviewModal
    3. Verify: frozen header row stays visible while scrolling down
    4. Verify: column resize handles appear on header right edges, drag to resize
    5. Verify: sheet tabs at bottom switch between sheets
    6. Verify: search bar filters and highlights matching cells
    7. Verify: if >500 rows, amber truncation banner appears

    **Criterion 2 — DOCX Preview (Google Docs feel):**
    1. Upload or navigate to a .docx file
    2. Click to open preview
    3. Verify: document renders with clean prose formatting
    4. Verify: ToC sidebar shows extracted headings (click ToC toggle button)
    5. Verify: clicking a heading scrolls the document to that heading

    **Criterion 3 — PPTX Preview (Google Slides feel):**
    1. Upload or navigate to a .pptx file
    2. Click to open preview
    3. Verify: slide renders in 16:9 aspect ratio with canvas
    4. Verify: Left/Right arrow keys navigate slides
    5. Verify: thumbnail strip shows on toggle button click
    6. Verify: fullscreen mode works (Play button)
    7. Verify: slide counter shows "N / M" format

    **Criterion 4 — Annotation Panel:**
    1. In PPTX preview, annotation panel should be visible on the right (320px)
    2. Type a note in the textarea, click "Add Note" (or Cmd+Enter)
    3. Verify: annotation appears immediately (optimistic)
    4. Click pencil icon to edit, save with Check icon
    5. Click trash icon to delete -- should disappear immediately
    6. Navigate to different slide -- verify annotations are per-slide
    7. Collapse panel -- verify count badge shows annotation count

    **Criterion 5 — Responsive:**
    1. Click maximize button on modal -- all renderers should adapt
    2. Click restore -- should return to normal size
    3. Try each renderer type in both normal and maximized states

    **Criterion 6 — Keyboard:**
    1. In PPTX: Left/Right arrows navigate slides
    2. In PPTX fullscreen: navigation still works
    3. Escape closes fullscreen (or modal if not in fullscreen)
    4. Cmd+Enter submits annotation in panel
  </how-to-verify>
  <resume-signal>Type "approved" if all six criteria pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
All six roadmap success criteria verified by human interaction.
</verification>

<success_criteria>
- All automated quality gates pass
- Human confirms all 6 success criteria from ROADMAP.md
- Phase 41 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/41-office-suite-preview-redesign/41-07-SUMMARY.md`
</output>
