---
phase: 38-packaging-signing-auto-update
plan: 03
type: execute
wave: 3
depends_on: ["38-01", "38-02"]
files_modified:
  - tauri-app/src-tauri/Cargo.toml
  - tauri-app/src-tauri/src/lib.rs
  - tauri-app/src-tauri/tauri.conf.json
  - tauri-app/src-tauri/capabilities/default.json
  - frontend/package.json
  - frontend/src/lib/tauri.ts
  - frontend/src/components/desktop/update-notification.tsx
  - frontend/src/app/(workspace)/[workspaceSlug]/workspace-slug-layout.tsx
  - .github/workflows/tauri-release.yml
autonomous: true
requirements: [PKG-06]

must_haves:
  truths:
    - "When a new GitHub Release is created, the app detects the update on next launch"
    - "User sees an in-app notification banner with version info and an Install button"
    - "Update downloads in the background without blocking the user"
    - "Update installs on next app restart (not forced immediately)"
    - "A dedicated release workflow creates GitHub Releases with signed artifacts and update manifests"
  artifacts:
    - path: "tauri-app/src-tauri/Cargo.toml"
      provides: "tauri-plugin-updater dependency"
      contains: "tauri-plugin-updater"
    - path: "tauri-app/src-tauri/src/lib.rs"
      provides: "Updater plugin registration"
      contains: "tauri_plugin_updater"
    - path: "frontend/src/lib/tauri.ts"
      provides: "checkForUpdates IPC wrapper"
      contains: "checkForUpdates"
    - path: "frontend/src/components/desktop/update-notification.tsx"
      provides: "In-app update notification banner component"
      contains: "UpdateNotification"
    - path: ".github/workflows/tauri-release.yml"
      provides: "Release workflow that creates GitHub Releases with update manifests"
      contains: "tauri-action"
    - path: "tauri-app/src-tauri/tauri.conf.json"
      provides: "Updater endpoint configuration pointing to GitHub Releases"
      contains: "updater"
  key_links:
    - from: "frontend/src/components/desktop/update-notification.tsx"
      to: "frontend/src/lib/tauri.ts"
      via: "checkForUpdates() wrapper that invokes Tauri updater API"
      pattern: "checkForUpdates"
    - from: "tauri-app/src-tauri/src/lib.rs"
      to: "tauri-plugin-updater"
      via: "Plugin registration in Tauri builder chain"
      pattern: "tauri_plugin_updater::init"
    - from: ".github/workflows/tauri-release.yml"
      to: "GitHub Releases"
      via: "tauri-action creates release with update manifest JSON"
      pattern: "tagName.*releaseName"
---

<objective>
Add tauri-plugin-updater for in-app auto-update with a non-blocking notification banner, and create a dedicated GitHub Release workflow that produces signed installers with update manifests for all 4 platforms.

Purpose: Users need to receive app updates without manually downloading new installers. The update flow must be non-intrusive — notify and download in background, install on restart.
Output: Updater plugin wired in Rust + frontend, UpdateNotification component, GitHub Release workflow
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-packaging-signing-auto-update/38-01-SUMMARY.md
@.planning/phases/38-packaging-signing-auto-update/38-02-SUMMARY.md
@tauri-app/src-tauri/Cargo.toml
@tauri-app/src-tauri/src/lib.rs
@tauri-app/src-tauri/tauri.conf.json
@tauri-app/src-tauri/capabilities/default.json
@frontend/src/lib/tauri.ts
@frontend/src/app/(workspace)/[workspaceSlug]/workspace-slug-layout.tsx
@frontend/src/components/desktop/tray-notification-listener.tsx
@.github/workflows/tauri-build.yml

<interfaces>
<!-- Existing lib.rs plugin chain: -->
tauri::Builder::default()
    .plugin(tauri_plugin_shell::init())
    .plugin(tauri_plugin_store::Builder::default().build())
    .plugin(tauri_plugin_deep_link::init())
    .plugin(tauri_plugin_dialog::init())
    .plugin(tauri_plugin_fs::init())
    .plugin(tauri_plugin_notification::init())
    // ... (add updater here)

<!-- Existing tauri.ts pattern — all wrappers use dynamic import: -->
export async function someCommand(): Promise<T> {
  if (!isTauri()) return fallback;
  const { invoke } = await import('@tauri-apps/api/core');
  return invoke<T>('command_name', { args });
}

<!-- workspace-slug-layout.tsx already dynamically imports: -->
<!-- TrayNotificationListener (ssr: false, Tauri-only) -->
<!-- Use the same dynamic import pattern for UpdateNotification -->

<!-- Existing frontend package.json has: -->
<!-- "@tauri-apps/api": "2.10.1" -->
<!-- "@tauri-apps/plugin-deep-link": "^2.4.7" -->
<!-- "@tauri-apps/plugin-store": "^2.4.2" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tauri-plugin-updater to Rust backend and configure updater endpoint</name>
  <files>tauri-app/src-tauri/Cargo.toml, tauri-app/src-tauri/src/lib.rs, tauri-app/src-tauri/tauri.conf.json, tauri-app/src-tauri/capabilities/default.json</files>
  <read_first>
    - tauri-app/src-tauri/Cargo.toml (current deps)
    - tauri-app/src-tauri/src/lib.rs (current plugin chain)
    - tauri-app/src-tauri/tauri.conf.json (current config — after Plan 01 + 02 changes)
    - tauri-app/src-tauri/capabilities/default.json (current permissions)
  </read_first>
  <action>
1. **Add tauri-plugin-updater to Cargo.toml:**
   Under `[dependencies]`, add:
   ```toml
   tauri-plugin-updater = "2"
   ```

2. **Register updater plugin in lib.rs:**
   Add `.plugin(tauri_plugin_updater::Builder::new().build())` to the builder chain, AFTER the existing plugins but BEFORE `.manage(...)`:
   ```rust
   .plugin(tauri_plugin_updater::Builder::new().build())
   ```
   Note: Use `Builder::new().build()` (not `init()`) — the Builder pattern allows configuring custom headers or intervals later.

3. **Add updater configuration to tauri.conf.json:**
   Add a `"plugins"` section for the updater (merge with existing `"plugins"` which has `"deep-link"`):
   ```json
   "plugins": {
     "deep-link": { ... },
     "updater": {
       "active": true,
       "dialog": false,
       "pubkey": "",
       "endpoints": [
         "https://github.com/YOUR_ORG/pilot-space/releases/latest/download/latest.json"
       ]
     }
   }
   ```
   IMPORTANT: Set `"dialog": false` — we handle the update UI ourselves (non-blocking banner, not a modal dialog).
   IMPORTANT: `"pubkey"` must be set to the public key generated by `tauri signer generate`. For now, set to empty string `""` — the release workflow will generate and configure the key pair. Add a comment: `"pubkey": ""` with a note that this needs to be populated with the output of `tauri signer generate -w ~/.tauri/pilot-space.key`.
   IMPORTANT: Replace `YOUR_ORG` with the actual GitHub org/user. Read the existing `tauri-build.yml` to determine the repo URL pattern or use a placeholder `OWNER/pilot-space` that the executor can fill.

4. **Add updater permission to capabilities/default.json:**
   Add to the `permissions` array:
   ```json
   "updater:default"
   ```

5. **Verify cargo check passes** after adding the dependency:
   The executor should run `cd tauri-app/src-tauri && cargo check` to verify.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/tauri-app && grep -q "tauri-plugin-updater" tauri-app/src-tauri/Cargo.toml && grep -q "tauri_plugin_updater" tauri-app/src-tauri/src/lib.rs && grep -q "updater" tauri-app/src-tauri/tauri.conf.json && grep -q "updater:default" tauri-app/src-tauri/capabilities/default.json && echo "PASS"</automated>
  </verify>
  <done>
    - tauri-plugin-updater = "2" in Cargo.toml
    - Plugin registered in lib.rs builder chain
    - Updater config in tauri.conf.json plugins section with dialog: false and GitHub endpoint
    - updater:default permission in capabilities
    - cargo check passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create frontend update check wrapper and UpdateNotification component</name>
  <files>frontend/package.json, frontend/src/lib/tauri.ts, frontend/src/components/desktop/update-notification.tsx, frontend/src/app/(workspace)/[workspaceSlug]/workspace-slug-layout.tsx</files>
  <read_first>
    - frontend/src/lib/tauri.ts (existing IPC wrappers — add checkForUpdates here)
    - frontend/src/app/(workspace)/[workspaceSlug]/workspace-slug-layout.tsx (layout where UpdateNotification mounts)
    - frontend/src/components/desktop/tray-notification-listener.tsx (reference for dynamic import + Tauri-only component pattern)
    - frontend/package.json (to add @tauri-apps/plugin-updater)
  </read_first>
  <action>
1. **Add @tauri-apps/plugin-updater to frontend/package.json:**
   ```
   pnpm add @tauri-apps/plugin-updater --filter frontend
   ```
   This adds the JS bindings for the updater plugin.

2. **Add update check wrapper to frontend/src/lib/tauri.ts:**
   Add at the bottom of the file (after the notification commands section):

   ```typescript
   // --- Update commands (Phase 38) ---

   export interface UpdateInfo {
     available: boolean;
     currentVersion: string;
     version: string;
     date: string | null;
     body: string | null;
   }

   /**
    * Check for app updates via tauri-plugin-updater.
    * Returns update info if available, or null if up to date / not in Tauri mode.
    * Non-blocking — safe to call on app launch.
    */
   export async function checkForUpdates(): Promise<UpdateInfo | null> {
     if (!isTauri()) return null;
     try {
       const { check } = await import('@tauri-apps/plugin-updater');
       const update = await check();
       if (update) {
         return {
           available: true,
           currentVersion: update.currentVersion,
           version: update.version,
           date: update.date ?? null,
           body: update.body ?? null,
         };
       }
       return null;
     } catch (e) {
       console.warn('[tauri] Update check failed:', e);
       return null;
     }
   }

   /**
    * Download and install the available update.
    * Downloads in background, then installs on next restart.
    * Caller should prompt user before calling this.
    */
   export async function downloadAndInstallUpdate(): Promise<void> {
     if (!isTauri()) return;
     const { check } = await import('@tauri-apps/plugin-updater');
     const update = await check();
     if (update) {
       await update.downloadAndInstall();
     }
   }
   ```

   IMPORTANT: The `check()` function from @tauri-apps/plugin-updater returns an Update object or null.
   IMPORTANT: `downloadAndInstall()` downloads in background and stages for install on restart. It does NOT force-restart the app.
   IMPORTANT: Wrap check() in try/catch — network failures should not crash the app.

3. **Create frontend/src/components/desktop/update-notification.tsx:**
   A non-blocking banner component that:
   - Checks for updates on mount (once, on app launch)
   - Shows a subtle banner at the top of the layout when an update is available
   - Has "Update" button that triggers download + install
   - Has "Dismiss" button that hides the banner
   - Shows download progress state (checking / downloading / ready to restart)

   ```tsx
   'use client';

   import { useEffect, useState } from 'react';
   import { checkForUpdates, downloadAndInstallUpdate, type UpdateInfo } from '@/lib/tauri';
   import { Button } from '@/components/ui/button';
   import { X, Download, RefreshCw } from 'lucide-react';

   type UpdateState = 'idle' | 'checking' | 'available' | 'downloading' | 'ready' | 'dismissed';

   export function UpdateNotification() {
     const [state, setState] = useState<UpdateState>('idle');
     const [updateInfo, setUpdateInfo] = useState<UpdateInfo | null>(null);

     useEffect(() => {
       let cancelled = false;
       const doCheck = async () => {
         setState('checking');
         const info = await checkForUpdates();
         if (cancelled) return;
         if (info?.available) {
           setUpdateInfo(info);
           setState('available');
         } else {
           setState('idle');
         }
       };
       // Check on mount (app launch) — delay 5 seconds to not compete with app initialization
       const timer = setTimeout(doCheck, 5000);
       return () => { cancelled = true; clearTimeout(timer); };
     }, []);

     const handleUpdate = async () => {
       setState('downloading');
       try {
         await downloadAndInstallUpdate();
         setState('ready');
       } catch (e) {
         console.error('[update] Download failed:', e);
         setState('available'); // revert to allow retry
       }
     };

     if (state === 'idle' || state === 'checking' || state === 'dismissed') return null;

     return (
       <div className="flex items-center justify-between gap-3 border-b border-border bg-muted/50 px-4 py-2 text-sm">
         <div className="flex items-center gap-2">
           <Download className="h-4 w-4 text-primary" />
           {state === 'available' && (
             <span>
               A new version <strong>v{updateInfo?.version}</strong> is available.
             </span>
           )}
           {state === 'downloading' && (
             <span className="flex items-center gap-2">
               <RefreshCw className="h-3 w-3 animate-spin" />
               Downloading update...
             </span>
           )}
           {state === 'ready' && (
             <span>Update downloaded. Restart the app to apply.</span>
           )}
         </div>
         <div className="flex items-center gap-2">
           {state === 'available' && (
             <Button size="sm" variant="default" onClick={handleUpdate}>
               Update
             </Button>
           )}
           <Button
             size="sm"
             variant="ghost"
             onClick={() => setState('dismissed')}
             aria-label="Dismiss update notification"
           >
             <X className="h-4 w-4" />
           </Button>
         </div>
       </div>
     );
   }
   ```

   Design notes (per brand: "warm, capable, collaborative"):
   - Subtle muted background, not alarming red/yellow
   - Text is informative, not pushy ("A new version is available" not "UPDATE NOW!")
   - Dismiss button always visible — user controls their experience
   - 5-second delay on mount to avoid competing with app initialization

4. **Mount UpdateNotification in workspace-slug-layout.tsx:**
   Add a dynamic import (same pattern as TrayNotificationListener):
   ```tsx
   const UpdateNotification = dynamic(
     () => import('@/components/desktop/update-notification').then(m => ({ default: m.UpdateNotification })),
     { ssr: false }
   );
   ```
   Render it at the top of the layout (before main content), conditionally on isTauri():
   ```tsx
   {isTauri() && <UpdateNotification />}
   ```
   Place it ABOVE the main content area, INSIDE the layout wrapper — it should appear as a banner at the top.

IMPORTANT: Use the existing `dynamic()` + `ssr: false` pattern for Tauri-only components.
IMPORTANT: Import `isTauri` from `@/lib/tauri` (already imported in the layout).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/tauri-app && grep -q "checkForUpdates" frontend/src/lib/tauri.ts && grep -q "downloadAndInstallUpdate" frontend/src/lib/tauri.ts && test -f frontend/src/components/desktop/update-notification.tsx && grep -q "UpdateNotification" frontend/src/components/desktop/update-notification.tsx && grep -q "UpdateNotification" frontend/src/app/\(workspace\)/\[workspaceSlug\]/workspace-slug-layout.tsx && echo "PASS"</automated>
  </verify>
  <done>
    - @tauri-apps/plugin-updater added to frontend/package.json
    - checkForUpdates() and downloadAndInstallUpdate() wrappers in tauri.ts
    - UpdateNotification component renders non-blocking banner
    - Component mounted in workspace-slug-layout via dynamic import (ssr: false)
    - Update check delayed 5 seconds on mount
    - Banner shows version info, download progress, dismiss button
  </done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub Release workflow with signed artifacts and update manifest</name>
  <files>.github/workflows/tauri-release.yml</files>
  <read_first>
    - .github/workflows/tauri-build.yml (after Plan 01 + 02 changes — use as template for release workflow)
    - .github/workflows/pilot-cli-build.yml (sidecar build — release workflow must build sidecar first)
  </read_first>
  <action>
Create `.github/workflows/tauri-release.yml` — a dedicated release workflow triggered by version tags:

```yaml
name: Tauri Release

on:
  push:
    tags: ['v*']

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false  # Never cancel a release build

jobs:
  # Job 1: Build sidecar binaries for all platforms
  build-sidecar:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target_triple: aarch64-apple-darwin
            binary_ext: ""
          - platform: macos-13
            target_triple: x86_64-apple-darwin
            binary_ext: ""
          - platform: ubuntu-22.04
            target_triple: x86_64-unknown-linux-gnu
            binary_ext: ""
          - platform: windows-latest
            target_triple: x86_64-pc-windows-msvc
            binary_ext: ".exe"
    runs-on: ${{ matrix.platform }}
    name: Sidecar ${{ matrix.target_triple }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: astral-sh/setup-uv@v4
      - name: Build sidecar
        working-directory: cli
        run: |
          uv sync
          uv pip install pyinstaller>=6.19.0
          uv run pyinstaller pilot.spec --noconfirm --clean
      - name: Rename and stage sidecar
        shell: bash
        env:
          TARGET_TRIPLE: ${{ matrix.target_triple }}
          BINARY_EXT: ${{ matrix.binary_ext }}
        run: |
          mkdir -p sidecar-output
          cp "cli/dist/pilot-cli/pilot-cli${BINARY_EXT}" "sidecar-output/pilot-cli-${TARGET_TRIPLE}${BINARY_EXT}"
          # Copy onedir support files
          BUNDLE_DIR="sidecar-output/pilot-cli-${TARGET_TRIPLE}-bundle"
          mkdir -p "$BUNDLE_DIR"
          cp -r cli/dist/pilot-cli/. "$BUNDLE_DIR/"
          rm -f "$BUNDLE_DIR/pilot-cli${BINARY_EXT}"
      - uses: actions/upload-artifact@v4
        with:
          name: sidecar-${{ matrix.target_triple }}
          path: sidecar-output/
          retention-days: 1

  # Job 2: Build and release Tauri app for all platforms
  build-release:
    needs: build-sidecar
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
            artifact-name: pilot-space-macos-arm64
          - platform: macos-13
            target: x86_64-apple-darwin
            artifact-name: pilot-space-macos-x86_64
          - platform: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            artifact-name: pilot-space-linux-x64
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            artifact-name: pilot-space-windows-x64
    runs-on: ${{ matrix.platform }}
    name: Release ${{ matrix.artifact-name }}
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux system dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: './tauri-app/src-tauri -> target'

      # Download the sidecar binary built in the previous job
      - name: Download sidecar artifact
        uses: actions/download-artifact@v4
        with:
          name: sidecar-${{ matrix.target }}
          path: tauri-app/src-tauri/binaries/

      - name: Install Azure SignTool (Windows)
        if: runner.os == 'Windows'
        run: dotnet tool install --global AzureSignTool

      # Sign sidecar on macOS before Tauri bundles the app
      - name: Sign sidecar binary (macOS)
        if: runner.os == 'macOS' && env.APPLE_SIGNING_IDENTITY != ''
        env:
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        run: |
          SIDECAR="tauri-app/src-tauri/binaries/pilot-cli-${{ matrix.target }}"
          if [ -f "$SIDECAR" ]; then
            codesign --force --options runtime \
              --sign "$APPLE_SIGNING_IDENTITY" \
              --entitlements tauri-app/src-tauri/Entitlements.plist \
              "$SIDECAR"
            echo "Signed sidecar: $SIDECAR"
          fi

      # Sign sidecar on Windows before Tauri bundles the app
      - name: Sign sidecar binary (Windows)
        if: runner.os == 'Windows' && env.AZURE_KEY_VAULT_URI != ''
        env:
          AZURE_KEY_VAULT_URI: ${{ secrets.AZURE_KEY_VAULT_URI }}
          AZURE_KEY_VAULT_CLIENT_ID: ${{ secrets.AZURE_KEY_VAULT_CLIENT_ID }}
          AZURE_KEY_VAULT_CLIENT_SECRET: ${{ secrets.AZURE_KEY_VAULT_CLIENT_SECRET }}
          AZURE_KEY_VAULT_TENANT_ID: ${{ secrets.AZURE_KEY_VAULT_TENANT_ID }}
          AZURE_KEY_VAULT_CERT_NAME: ${{ secrets.AZURE_KEY_VAULT_CERT_NAME }}
        shell: pwsh
        run: |
          $sidecar = "tauri-app\src-tauri\binaries\pilot-cli-x86_64-pc-windows-msvc.exe"
          if (Test-Path $sidecar) {
            AzureSignTool sign -kvu $env:AZURE_KEY_VAULT_URI `
              -kvi $env:AZURE_KEY_VAULT_CLIENT_ID `
              -kvs $env:AZURE_KEY_VAULT_CLIENT_SECRET `
              -kvt $env:AZURE_KEY_VAULT_TENANT_ID `
              -kvc $env:AZURE_KEY_VAULT_CERT_NAME `
              -tr "http://timestamp.digicert.com" -td sha256 `
              $sidecar
          }

      - name: Install frontend dependencies
        working-directory: frontend
        run: pnpm install --frozen-lockfile

      - name: Install Tauri CLI dependencies
        working-directory: tauri-app
        run: pnpm install --frozen-lockfile

      # tauri-action creates the GitHub Release and uploads signed installers
      # It also generates the latest.json update manifest automatically
      - name: Build and release Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NEXT_TAURI: 'true'
          NEXT_PUBLIC_API_URL: 'https://api.pilotspace.io/api/v1'
          # Updater signing key (generated by: tauri signer generate -w ~/.tauri/pilot-space.key)
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # macOS signing
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          # Windows signing
          AZURE_KEY_VAULT_URI: ${{ secrets.AZURE_KEY_VAULT_URI }}
          AZURE_KEY_VAULT_CLIENT_ID: ${{ secrets.AZURE_KEY_VAULT_CLIENT_ID }}
          AZURE_KEY_VAULT_CLIENT_SECRET: ${{ secrets.AZURE_KEY_VAULT_CLIENT_SECRET }}
          AZURE_KEY_VAULT_TENANT_ID: ${{ secrets.AZURE_KEY_VAULT_TENANT_ID }}
          AZURE_KEY_VAULT_CERT_NAME: ${{ secrets.AZURE_KEY_VAULT_CERT_NAME }}
        with:
          projectPath: './tauri-app'
          tauriScript: 'pnpm tauri'
          args: '--target ${{ matrix.target }}'
          tagName: ${{ github.ref_name }}
          releaseName: 'Pilot Space ${{ github.ref_name }}'
          releaseBody: 'See the assets to download this version and install.'
          releaseDraft: true
          prerelease: false
```

Key points:
- `releaseDraft: true` — creates a draft release that must be manually published (human reviews before going live)
- `tagName` and `releaseName` use the git tag (e.g., `v0.2.0`)
- `TAURI_SIGNING_PRIVATE_KEY` is the updater signing key — tauri-action uses it to sign the update bundles and generate `latest.json` manifest
- The `latest.json` file is automatically uploaded to the GitHub Release by tauri-action when TAURI_SIGNING_PRIVATE_KEY is set
- Each platform runner uploads its artifacts to the same release (tauri-action handles this via GITHUB_TOKEN)
- Sidecar is built and signed before tauri-action bundles the app
- cancel-in-progress: false — never cancel a release build mid-flight

User setup required:
- Generate updater key pair: `cd tauri-app && pnpm tauri signer generate -w ~/.tauri/pilot-space.key`
- Add TAURI_SIGNING_PRIVATE_KEY (contents of the .key file) and TAURI_SIGNING_PRIVATE_KEY_PASSWORD to GitHub repo secrets
- Add the PUBLIC key to tauri.conf.json plugins.updater.pubkey
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/tauri-app && test -f .github/workflows/tauri-release.yml && grep -q "TAURI_SIGNING_PRIVATE_KEY" .github/workflows/tauri-release.yml && grep -q "tagName" .github/workflows/tauri-release.yml && grep -q "releaseDraft: true" .github/workflows/tauri-release.yml && grep -q "build-sidecar" .github/workflows/tauri-release.yml && echo "PASS"</automated>
  </verify>
  <done>
    - tauri-release.yml exists with v* tag trigger
    - Sidecar build job runs before release build job
    - All 4 platform runners in release matrix
    - TAURI_SIGNING_PRIVATE_KEY env var wired for update manifest signing
    - All Apple and Azure signing secrets wired
    - Release is draft (requires manual publish)
    - latest.json update manifest generated automatically by tauri-action
  </done>
</task>

</tasks>

<verification>
1. `cargo check` passes in tauri-app/src-tauri/ (updater plugin compiles)
2. `grep -q "tauri-plugin-updater" tauri-app/src-tauri/Cargo.toml` — dependency present
3. `grep -q "updater:default" tauri-app/src-tauri/capabilities/default.json` — permission granted
4. `pnpm lint` passes in frontend/ (no ESLint errors in new component)
5. `pnpm type-check` passes in frontend/ (TypeScript types correct)
6. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/tauri-release.yml'))"` — valid YAML
7. UpdateNotification component renders conditionally in workspace layout
</verification>

<success_criteria>
- tauri-plugin-updater registered in Rust backend and configured in tauri.conf.json
- Frontend has checkForUpdates() and downloadAndInstallUpdate() typed wrappers
- UpdateNotification component shows non-blocking banner with version info
- Banner checks on launch (5s delay), shows Update button, dismiss button
- Download happens in background, installs on next restart
- GitHub Release workflow produces signed artifacts + latest.json manifest for all 4 platforms
- Release is draft by default (human publishes)
- Updater signing key wired via TAURI_SIGNING_PRIVATE_KEY secret
</success_criteria>

<output>
After completion, create `.planning/phases/38-packaging-signing-auto-update/38-03-SUMMARY.md`
</output>
