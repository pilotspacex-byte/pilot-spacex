---
phase: 43-lsp-integration-and-code-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - frontend/src/features/editor/language/python-worker.ts
  - frontend/src/features/editor/hooks/usePythonLanguage.ts
  - frontend/src/features/command-palette/actions/lspNavigateActions.ts
  - frontend/src/features/editor/MonacoFileEditor.tsx
autonomous: true
requirements: [LSP-02, LSP-05, LSP-06]

must_haves:
  truths:
    - "Python files show autocomplete suggestions and type info when Pyright WASM finishes loading"
    - "A loading indicator appears while Pyright worker initializes on first .py file open"
    - "User can press F12 (Go to Definition) and Shift+F12 (Find References) within a TypeScript or Python file"
    - "Go to Definition and Find All References appear as actions in the command palette"
  artifacts:
    - path: "frontend/src/features/editor/language/python-worker.ts"
      provides: "Lazy Pyright WASM loader with initialized flag and fallback handling"
      exports: ["ensurePythonLanguage"]
    - path: "frontend/src/features/editor/hooks/usePythonLanguage.ts"
      provides: "React hook to lazy-load Python intelligence when .py file is active"
      exports: ["usePythonLanguage"]
    - path: "frontend/src/features/command-palette/actions/lspNavigateActions.ts"
      provides: "Go to Definition and Find References palette actions"
      exports: ["registerLSPNavigateActions"]
    - path: "frontend/src/features/editor/MonacoFileEditor.tsx"
      provides: "Updated file editor with Python language hook and LSP action registration"
      contains: "usePythonLanguage"
  key_links:
    - from: "frontend/src/features/editor/hooks/usePythonLanguage.ts"
      to: "frontend/src/features/editor/language/python-worker.ts"
      via: "import ensurePythonLanguage"
      pattern: "ensurePythonLanguage"
    - from: "frontend/src/features/editor/MonacoFileEditor.tsx"
      to: "frontend/src/features/editor/hooks/usePythonLanguage.ts"
      via: "calls usePythonLanguage with file.language"
      pattern: "usePythonLanguage"
    - from: "frontend/src/features/editor/MonacoFileEditor.tsx"
      to: "frontend/src/features/command-palette/actions/lspNavigateActions.ts"
      via: "registers LSP navigate actions in useEffect"
      pattern: "registerLSPNavigateActions"
---

<objective>
Add Python code intelligence via lazy-loaded Pyright WASM, register Go to Definition / Find References in the command palette, and wire F12/Shift+F12 keybindings in MonacoFileEditor.

Purpose: Complete the language coverage (Python secondary language) and navigation features (go-to-definition, find references) that make the editor feel like a real IDE. Python support is lazy-loaded to avoid impacting initial bundle size.

Output: Python files get autocomplete + hover + diagnostics when Pyright loads. F12 and Shift+F12 work for navigation. Command palette includes LSP navigation actions.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lsp-integration-and-code-intelligence/43-CONTEXT.md
@.planning/phases/43-lsp-integration-and-code-intelligence/43-RESEARCH.md
@.planning/phases/43-lsp-integration-and-code-intelligence/43-01-SUMMARY.md

@frontend/src/features/editor/MonacoFileEditor.tsx
@frontend/src/features/command-palette/actions/navigateActions.ts
@frontend/src/features/command-palette/registry/ActionRegistry.ts
@frontend/src/features/command-palette/types.ts

<interfaces>
<!-- Existing contracts this plan extends -->

From frontend/src/features/command-palette/actions/navigateActions.ts:
```typescript
interface NavigateActionsContext {
  goToFile?: () => void;
  goToLine?: () => void;
  goToSymbol?: () => void;
}
export function registerNavigateActions(context: NavigateActionsContext): () => void;
```

From frontend/src/features/command-palette/registry/ActionRegistry.ts:
```typescript
export function registerAction(action: PaletteAction): () => void;
```

From frontend/src/features/editor/MonacoFileEditor.tsx:
```typescript
interface MonacoFileEditorProps {
  file: OpenFile;
  onChange?: (content: string) => void;
}
// Already has: useMonaco(), useMonacoTheme(), useTypeScriptDefaults() (from Plan 01)
// Already registers: editActions, navigateActions
// Already has keybindings: Cmd+Shift+P, Cmd+Shift+O, Cmd+G
```

From Plan 01 - frontend/src/features/editor/hooks/useTypeScriptDefaults.ts:
```typescript
export function useTypeScriptDefaults(monaco: typeof monacoNs | null): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python language lazy loader and usePythonLanguage hook</name>
  <files>
    frontend/src/features/editor/language/python-worker.ts,
    frontend/src/features/editor/hooks/usePythonLanguage.ts
  </files>
  <read_first>
    frontend/src/features/editor/hooks/useMonacoTheme.ts,
    frontend/src/features/editor/hooks/useTypeScriptDefaults.ts,
    frontend/src/features/editor/MonacoFileEditor.tsx,
    .planning/phases/43-lsp-integration-and-code-intelligence/43-RESEARCH.md
  </read_first>
  <action>
    1. Install monaco-pyright-lsp:
       `cd frontend && pnpm add monaco-pyright-lsp`

    2. Create `frontend/src/features/editor/language/python-worker.ts`:
       - Module-level `let pyrightLoaded = false` and `let pyrightLoading = false`
       - Export `async function ensurePythonLanguage(monaco: typeof monacoNs): Promise<boolean>`
         - If pyrightLoaded, return true immediately
         - If pyrightLoading, return false (loading in progress)
         - Set pyrightLoading = true
         - Try: dynamic import `monaco-pyright-lsp`, call its init/setup function with monaco
         - Set pyrightLoaded = true, pyrightLoading = false, return true
         - Catch: console.warn("Failed to load Pyright WASM, falling back to syntax highlighting"), set pyrightLoading = false, return false
         - IMPORTANT: The exact API of monaco-pyright-lsp needs to be checked during implementation. If the package API differs from expectation, adapt accordingly. If the package fails to work with Monaco 0.55.1, skip Pyright and leave Python with syntax highlighting only (per research recommendation).

    3. Create `frontend/src/features/editor/hooks/usePythonLanguage.ts`:
       - Export `usePythonLanguage(monaco: typeof monacoNs | null, language: string): { isLoading: boolean }`
       - useState: `isLoading` (boolean, default false)
       - useEffect: if monaco is null or language is not 'python', return (and set isLoading false)
         - Set isLoading true
         - Call `ensurePythonLanguage(monaco).then((loaded) => setIsLoading(false))`
         - Cleanup: no-op (worker persists)
       - Return `{ isLoading }`
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ensurePythonLanguage" frontend/src/features/editor/language/python-worker.ts
    - grep -q "pyrightLoaded" frontend/src/features/editor/language/python-worker.ts
    - grep -q "usePythonLanguage" frontend/src/features/editor/hooks/usePythonLanguage.ts
    - grep -q "isLoading" frontend/src/features/editor/hooks/usePythonLanguage.ts
    - grep -q "monaco-pyright-lsp" frontend/package.json
  </acceptance_criteria>
  <done>Python language support lazy-loads Pyright WASM on first .py file open. Loading state is exposed for UI indicator. Failure falls back gracefully to syntax highlighting only.</done>
</task>

<task type="auto">
  <name>Task 2: LSP navigate actions + MonacoFileEditor wiring (F12, Shift+F12, Python, palette)</name>
  <files>
    frontend/src/features/command-palette/actions/lspNavigateActions.ts,
    frontend/src/features/editor/MonacoFileEditor.tsx
  </files>
  <read_first>
    frontend/src/features/command-palette/actions/navigateActions.ts,
    frontend/src/features/command-palette/types.ts,
    frontend/src/features/editor/MonacoFileEditor.tsx,
    frontend/src/features/editor/hooks/usePythonLanguage.ts
  </read_first>
  <action>
    1. Create `frontend/src/features/command-palette/actions/lspNavigateActions.ts`:
       - Follow the exact pattern of navigateActions.ts
       - Interface `LSPNavigateActionsContext`: `goToDefinition?: () => void`, `findReferences?: () => void`
       - Export `registerLSPNavigateActions(context: LSPNavigateActionsContext): () => void`
       - Register two actions:
         a. id: 'navigate:go-to-definition', label: 'Go to Definition', category: 'navigate', icon: Locate (from lucide-react), shortcut: 'F12', priority: 43, execute: context.goToDefinition?.()
         b. id: 'navigate:find-references', label: 'Find All References', category: 'navigate', icon: ListTree (from lucide-react), shortcut: 'Shift+F12', priority: 44, execute: context.findReferences?.()
       - Return combined unregister function (same pattern as navigateActions.ts)

    2. Update `frontend/src/features/editor/MonacoFileEditor.tsx`:
       - Import `usePythonLanguage` from `./hooks/usePythonLanguage`
       - Import `registerLSPNavigateActions` from `@/features/command-palette/actions/lspNavigateActions`
       - Call `const { isLoading: isPythonLoading } = usePythonLanguage(monaco, file.language)`
       - Add F12 keybinding override in the existing keybindings useEffect:
         ```
         editorInstance.addCommand(monaco.KeyCode.F12, () => {
           editorInstance.getAction('editor.action.revealDefinition')?.run();
         });
         ```
       - Add Shift+F12 keybinding override:
         ```
         editorInstance.addCommand(monaco.KeyMod.Shift | monaco.KeyCode.F12, () => {
           editorInstance.getAction('editor.action.goToReferences')?.run();
         });
         ```
       - Register LSP navigate actions in the existing action registration useEffect:
         ```
         registerLSPNavigateActions({
           goToDefinition: () => editor.getAction('editor.action.revealDefinition')?.run(),
           findReferences: () => editor.getAction('editor.action.goToReferences')?.run(),
         })
         ```
         Add this cleanup to the existing cleanups array.
       - Show Python loading indicator: if isPythonLoading is true, render a small Badge below the read-only badge area: `<Badge variant="secondary" className="absolute right-3 bottom-3 text-xs animate-pulse">Loading Python IntelliSense...</Badge>`
       - IMPORTANT: F12 and Shift+F12 trigger Monaco's built-in actions which already work for TypeScript (built-in) and Python (after Pyright loads). No custom definition/reference providers needed.
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "registerLSPNavigateActions" frontend/src/features/command-palette/actions/lspNavigateActions.ts
    - grep -q "go-to-definition" frontend/src/features/command-palette/actions/lspNavigateActions.ts
    - grep -q "find-references" frontend/src/features/command-palette/actions/lspNavigateActions.ts
    - grep -q "usePythonLanguage" frontend/src/features/editor/MonacoFileEditor.tsx
    - grep -q "registerLSPNavigateActions" frontend/src/features/editor/MonacoFileEditor.tsx
    - grep -q "revealDefinition" frontend/src/features/editor/MonacoFileEditor.tsx
    - grep -q "goToReferences" frontend/src/features/editor/MonacoFileEditor.tsx
    - grep -q "isPythonLoading" frontend/src/features/editor/MonacoFileEditor.tsx
  </acceptance_criteria>
  <done>F12 triggers Go to Definition, Shift+F12 triggers Find References in MonacoFileEditor. Both actions appear in command palette. Python files show loading indicator while Pyright initializes, then get full IntelliSense.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm type-check` passes
2. `cd frontend && pnpm lint` passes
3. monaco-pyright-lsp is in package.json dependencies
4. lspNavigateActions.ts registers Go to Definition and Find All References
5. MonacoFileEditor has F12 and Shift+F12 keybindings
6. Python loading indicator appears for .py files
</verification>

<success_criteria>
- Python intelligence lazy-loads only when a .py file is opened (not at startup)
- Pyright failure falls back gracefully to syntax highlighting (no crash)
- Loading indicator ("Loading Python IntelliSense...") appears during Pyright initialization
- F12 triggers revealDefinition (within-file jump to symbol definition)
- Shift+F12 triggers goToReferences (within-file peek references)
- Both actions are registered in the command palette with correct icons and shortcuts
- No new dependencies loaded eagerly — monaco-pyright-lsp is always dynamic import()
</success_criteria>

<output>
After completion, create `.planning/phases/43-lsp-integration-and-code-intelligence/43-03-SUMMARY.md`
</output>
