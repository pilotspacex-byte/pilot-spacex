---
phase: 43-lsp-integration-and-code-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/editor/language/typescript-config.ts
  - frontend/src/features/editor/language/diagnostics.ts
  - frontend/src/features/editor/hooks/useTypeScriptDefaults.ts
  - frontend/src/features/editor/hooks/useDiagnostics.ts
  - frontend/src/features/editor/themes/pilotSpaceTheme.ts
autonomous: true
requirements: [LSP-01, LSP-03]

must_haves:
  truths:
    - "TypeScript files opened in MonacoFileEditor show autocomplete suggestions with type info on dot-trigger and Ctrl+Space"
    - "TypeScript files show inline red/yellow squiggly underlines for type errors and warnings"
    - "JavaScript files also get syntax and semantic validation diagnostics"
    - "JSON, CSS, and HTML files continue to receive Monaco's built-in language intelligence (no regression from TS/JS configuration)"
  artifacts:
    - path: "frontend/src/features/editor/language/typescript-config.ts"
      provides: "TypeScript compiler options and diagnostic options constants"
      exports: ["configureTypeScriptDefaults"]
    - path: "frontend/src/features/editor/language/diagnostics.ts"
      provides: "Diagnostic type, marker subscription utility, severity mapping"
      exports: ["Diagnostic", "subscribeToDiagnostics", "severityToString", "DiagnosticCounts"]
    - path: "frontend/src/features/editor/hooks/useTypeScriptDefaults.ts"
      provides: "React hook to configure TS defaults on Monaco mount"
      exports: ["useTypeScriptDefaults"]
    - path: "frontend/src/features/editor/hooks/useDiagnostics.ts"
      provides: "React hook to subscribe to Monaco markers and produce Diagnostic[]"
      exports: ["useDiagnostics"]
    - path: "frontend/src/features/editor/themes/pilotSpaceTheme.ts"
      provides: "Diagnostic-related editor colors added to both light and dark themes"
      contains: "editorError.foreground"
  key_links:
    - from: "frontend/src/features/editor/hooks/useTypeScriptDefaults.ts"
      to: "frontend/src/features/editor/language/typescript-config.ts"
      via: "import configureTypeScriptDefaults"
      pattern: "configureTypeScriptDefaults"
    - from: "frontend/src/features/editor/hooks/useDiagnostics.ts"
      to: "frontend/src/features/editor/language/diagnostics.ts"
      via: "import subscribeToDiagnostics"
      pattern: "subscribeToDiagnostics"
---

<objective>
Configure Monaco's built-in TypeScript/JavaScript language service for full IntelliSense and create the diagnostics data layer (types, marker subscription hook, theme colors).

Purpose: This is the foundation for all code intelligence features. TypeScript configuration must happen before any editor model is created (Pitfall 1 from research). The diagnostics types and subscription hook are consumed by Plan 02 (panel UI) and Plan 03 (Python integration).

Output: TypeScript IntelliSense works out of the box in MonacoFileEditor. Diagnostic squiggles appear inline. Diagnostics data layer ready for panel UI.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lsp-integration-and-code-intelligence/43-CONTEXT.md
@.planning/phases/43-lsp-integration-and-code-intelligence/43-RESEARCH.md

@frontend/src/features/editor/MonacoFileEditor.tsx
@frontend/src/features/editor/EditorLayout.tsx
@frontend/src/features/editor/themes/pilotSpaceTheme.ts
@frontend/src/features/editor/hooks/useMonacoTheme.ts
@frontend/src/features/editor/types.ts

<interfaces>
<!-- Key types and contracts from existing codebase -->

From frontend/src/features/editor/types.ts:
```typescript
export interface OpenFile {
  id: string;
  name: string;
  path: string;
  source: FileSource;
  language: string;
  content: string;
  isDirty: boolean;
  isReadOnly: boolean;
}
```

From frontend/src/features/editor/themes/pilotSpaceTheme.ts:
```typescript
export const THEME_LIGHT = 'pilot-space';
export const THEME_DARK = 'pilot-space-dark';
export function definePilotSpaceThemes(monacoInstance: typeof monaco): void;
```

From frontend/src/features/editor/hooks/useMonacoTheme.ts:
```typescript
// Pattern: useEffect-based hook that takes `monaco: typeof monacoNs | null`
// Returns theme name string. Registers themes on first call.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript/JS language config, diagnostics types, and theme colors</name>
  <files>
    frontend/src/features/editor/language/typescript-config.ts,
    frontend/src/features/editor/language/diagnostics.ts,
    frontend/src/features/editor/themes/pilotSpaceTheme.ts
  </files>
  <read_first>
    frontend/src/features/editor/themes/pilotSpaceTheme.ts,
    frontend/src/features/editor/hooks/useMonacoTheme.ts,
    frontend/src/features/editor/MonacoFileEditor.tsx,
    .planning/phases/43-lsp-integration-and-code-intelligence/43-RESEARCH.md
  </read_first>
  <action>
    1. Create `frontend/src/features/editor/language/typescript-config.ts`:
       - Export `configureTypeScriptDefaults(monaco: typeof monacoNs): void`
       - Set TypeScript compiler options: target ESNext, module ESNext, moduleResolution NodeJs, jsx ReactJSX, allowJs true, checkJs false, strict true, noEmit true, esModuleInterop true, allowNonTsExtensions true, lib ['esnext', 'dom', 'dom.iterable']
       - Set TypeScript diagnostics options: noSemanticValidation false, noSyntaxValidation false, noSuggestionDiagnostics false
       - Set JavaScript defaults diagnostics options: noSemanticValidation false, noSyntaxValidation false
       - Use `monaco.languages.typescript.typescriptDefaults` and `monaco.languages.typescript.javascriptDefaults`
       - LSP-03 NON-REGRESSION: Do NOT modify or reconfigure `monaco.languages.json`, `monaco.languages.css`, or `monaco.languages.html` defaults. These are Monaco built-ins that work out of the box. The function MUST only touch `typescriptDefaults` and `javascriptDefaults`.

    2. Create `frontend/src/features/editor/language/diagnostics.ts`:
       - Export `Diagnostic` interface: severity ('error' | 'warning' | 'info' | 'hint'), message, source, startLineNumber, startColumn, endLineNumber, endColumn, modelUri (string), fileName (string)
       - Export `DiagnosticCounts` interface: errors (number), warnings (number), infos (number)
       - Export `severityToString(severity: monacoNs.MarkerSeverity): Diagnostic['severity']` -- map 8->error, 4->warning, 2->info, default->hint
       - Export `subscribeToDiagnostics(monaco, onUpdate: (diagnostics: Diagnostic[]) => void): IDisposable` -- uses `monaco.editor.onDidChangeMarkers` to collect all markers via `getModelMarkers({})`, map to Diagnostic[], call onUpdate
       - Export `countDiagnostics(diagnostics: Diagnostic[]): DiagnosticCounts` -- count by severity

    3. Update `frontend/src/features/editor/themes/pilotSpaceTheme.ts`:
       - Add diagnostic colors to THEME_LIGHT colors object:
         'editorError.foreground': '#ef4444',
         'editorWarning.foreground': '#f59e0b',
         'editorInfo.foreground': '#3b82f6',
         'editorHint.foreground': '#6b7280',
         'editorError.border': '#fecaca',
         'editorWarning.border': '#fde68a'
       - Add diagnostic colors to THEME_DARK colors object:
         'editorError.foreground': '#f87171',
         'editorWarning.foreground': '#fbbf24',
         'editorInfo.foreground': '#60a5fa',
         'editorHint.foreground': '#9ca3af'
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "configureTypeScriptDefaults" frontend/src/features/editor/language/typescript-config.ts
    - grep -q "export interface Diagnostic" frontend/src/features/editor/language/diagnostics.ts
    - grep -q "subscribeToDiagnostics" frontend/src/features/editor/language/diagnostics.ts
    - grep -q "countDiagnostics" frontend/src/features/editor/language/diagnostics.ts
    - grep -q "editorError.foreground" frontend/src/features/editor/themes/pilotSpaceTheme.ts
    - grep -q "editorWarning.foreground" frontend/src/features/editor/themes/pilotSpaceTheme.ts
    - LSP-03 non-regression: grep -rL "json\|css\|html" frontend/src/features/editor/language/typescript-config.ts confirms no JSON/CSS/HTML defaults are modified (file must NOT contain references to json/css/html language defaults)
  </acceptance_criteria>
  <done>TypeScript config function exported, Diagnostic types and subscription utility exported, theme includes diagnostic colors for both light and dark modes. JSON/CSS/HTML built-in language services are untouched (LSP-03).</done>
</task>

<task type="auto">
  <name>Task 2: useTypeScriptDefaults and useDiagnostics hooks + MonacoFileEditor wiring</name>
  <files>
    frontend/src/features/editor/hooks/useTypeScriptDefaults.ts,
    frontend/src/features/editor/hooks/useDiagnostics.ts,
    frontend/src/features/editor/MonacoFileEditor.tsx
  </files>
  <read_first>
    frontend/src/features/editor/hooks/useMonacoTheme.ts,
    frontend/src/features/editor/hooks/useMonacoGhostText.ts,
    frontend/src/features/editor/MonacoFileEditor.tsx,
    frontend/src/features/editor/language/typescript-config.ts,
    frontend/src/features/editor/language/diagnostics.ts
  </read_first>
  <action>
    1. Create `frontend/src/features/editor/hooks/useTypeScriptDefaults.ts`:
       - Export `useTypeScriptDefaults(monaco: typeof monacoNs | null): void`
       - useEffect: if monaco is null, return early. Call `configureTypeScriptDefaults(monaco)`.
       - Use a module-level `let configured = false` flag to ensure it only runs once (TS defaults are global singleton). Reset is not needed.
       - No cleanup needed (TS defaults persist for Monaco lifetime).

    2. Create `frontend/src/features/editor/hooks/useDiagnostics.ts`:
       - Export `useDiagnostics(monaco: typeof monacoNs | null): { diagnostics: Diagnostic[], counts: DiagnosticCounts }`
       - Use useState for diagnostics array, initialized to [].
       - Use useMemo to derive counts from diagnostics via `countDiagnostics()`.
       - useEffect: if monaco is null, return. Call `subscribeToDiagnostics(monaco, setDiagnostics)`. Return `disposable.dispose` as cleanup.

    3. Update `frontend/src/features/editor/MonacoFileEditor.tsx`:
       - Import and call `useTypeScriptDefaults(monaco)` -- MUST be called before Editor renders so TS defaults are set before model creation.
       - Do NOT add useDiagnostics here -- that will be consumed at EditorLayout level in Plan 02.
       - No other changes to MonacoFileEditor behavior or UI.
       - LSP-03 NON-REGRESSION: Verify that the useTypeScriptDefaults hook does NOT disable or reconfigure any language service other than TypeScript and JavaScript. JSON, CSS, and HTML must continue to work with Monaco's built-in defaults.
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useTypeScriptDefaults" frontend/src/features/editor/hooks/useTypeScriptDefaults.ts
    - grep -q "useDiagnostics" frontend/src/features/editor/hooks/useDiagnostics.ts
    - grep -q "useTypeScriptDefaults" frontend/src/features/editor/MonacoFileEditor.tsx
    - grep -q "configureTypeScriptDefaults" frontend/src/features/editor/hooks/useTypeScriptDefaults.ts
    - grep -q "subscribeToDiagnostics" frontend/src/features/editor/hooks/useDiagnostics.ts
  </acceptance_criteria>
  <done>useTypeScriptDefaults hook configures TS compiler options on Monaco mount. useDiagnostics hook subscribes to marker changes and returns typed Diagnostic[]. MonacoFileEditor calls useTypeScriptDefaults so IntelliSense is active for TS/JS files. JSON/CSS/HTML language services remain untouched (LSP-03).</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm type-check` passes with no new errors
2. `cd frontend && pnpm lint` passes
3. New files exist: language/typescript-config.ts, language/diagnostics.ts, hooks/useTypeScriptDefaults.ts, hooks/useDiagnostics.ts
4. pilotSpaceTheme.ts contains diagnostic color entries for both themes
5. MonacoFileEditor.tsx imports and calls useTypeScriptDefaults
6. LSP-03 non-regression: typescript-config.ts only touches typescriptDefaults and javascriptDefaults -- no json/css/html language service configuration
</verification>

<success_criteria>
- TypeScript compiler options are configured globally before any editor model creation
- Diagnostic types (Diagnostic, DiagnosticCounts) are exported for downstream consumption
- Marker subscription utility (subscribeToDiagnostics) uses onDidChangeMarkers event (not polling)
- Theme colors include error/warning/info foreground for both light and dark modes
- All new code follows existing hook patterns (useEffect with cleanup, nullable monaco parameter)
- JSON/CSS/HTML built-in language services are not modified or disabled (LSP-03 non-regression)
</success_criteria>

<output>
After completion, create `.planning/phases/43-lsp-integration-and-code-intelligence/43-01-SUMMARY.md`
</output>
