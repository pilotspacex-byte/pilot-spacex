---
phase: 24-page-tree-data-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/079_add_page_tree_columns.py
autonomous: true
requirements: [TREE-01, TREE-04, TREE-05]

must_haves:
  truths:
    - "Migration 079 adds parent_id, depth, and position columns to the notes table"
    - "Migration 079 creates CHECK constraints enforcing depth 0-2 and no self-parent"
    - "Migration 079 creates 4 new indexes for tree queries and personal page RLS"
    - "Migration 079 classifies existing project notes (project_id NOT NULL) with sequential positions"
    - "Migration 079 classifies existing personal notes (project_id NULL) with sequential positions"
    - "Migration 079 atomically replaces notes_workspace_member RLS policy with notes_project_page_policy + notes_personal_page_policy + notes_service_role"
    - "Migration 079 has a complete downgrade() that reverses all changes"
    - "Alembic reports a single head after migration file is created"
  artifacts:
    - path: "backend/alembic/versions/079_add_page_tree_columns.py"
      provides: "Complete DDL + DML + RLS migration for page tree data model"
      min_lines: 100
  key_links:
    - from: "backend/alembic/versions/079_add_page_tree_columns.py"
      to: "backend/alembic/versions/078_fix_rls_policies.py"
      via: "down_revision reference"
      pattern: "down_revision.*078"
---

<objective>
Create Alembic migration 079 that adds tree columns to the notes table, migrates existing data into project/personal page classifications, and atomically replaces RLS policies for owner-only personal page visibility.

Purpose: Materializes the page tree data model in PostgreSQL with all constraints, indexes, data migration, and RLS policies in a single atomic migration.
Output: Migration file `079_add_page_tree_columns.py` with complete upgrade() and downgrade().
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-page-tree-data-model/24-CONTEXT.md
@.planning/phases/24-page-tree-data-model/24-RESEARCH.md
@backend/alembic/versions/078_fix_rls_policies.py
@backend/alembic/versions/005_note_entities.py

<interfaces>
<!-- Migration chain context -->

The current Alembic head is `078_fix_rls_policies`.
New migration must set `down_revision` to the revision ID from that file.

The existing `notes` table has these columns (from model + migrations):

- id (UUID PK), title, content (JSONB), summary, word_count, reading_time_mins
- is_pinned, is_guided_template, template_id (FK templates), owner_id (FK users, NOT NULL)
- project_id (FK projects, nullable), source_chat_session_id (FK ai_sessions)
- workspace_id (from WorkspaceScopedModel), is_deleted, created_at, updated_at

Existing RLS on notes table:

- `notes_workspace_member` policy — created in migration 005, grants access to workspace members
- RLS is ENABLED and FORCED on the table
- No `notes_service_role` bypass exists (migration 005 did not create one)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 079 — DDL, DML, and RLS</name>
  <files>backend/alembic/versions/079_add_page_tree_columns.py</files>
  <action>
    **First:** Read `backend/alembic/versions/078_fix_rls_policies.py` to get the exact `revision` string for `down_revision`.

    **Create** `backend/alembic/versions/079_add_page_tree_columns.py` with:

    **Header:**
    - `revision` = a new unique ID (use Alembic convention, e.g., short hex string)
    - `down_revision` = exact revision ID from 078 migration
    - Docstring: "Add page tree columns to notes table, migrate existing data, replace RLS policies"

    **upgrade() function — 7 steps per RESEARCH.md:**

    1. **DDL — Add columns:**
       - `parent_id` UUID nullable
       - `depth` INTEGER NOT NULL server_default="0"
       - `position` INTEGER NOT NULL server_default="0"

    2. **DDL — Self-referencing FK:**
       - `op.create_foreign_key("fk_notes_parent_id", "notes", "notes", ["parent_id"], ["id"], ondelete="SET NULL")`

    3. **DDL — CHECK constraints via op.execute(text(...)):**
       - `chk_notes_depth_range`: `CHECK (depth >= 0 AND depth <= 2)`
       - `chk_notes_no_self_parent`: `CHECK (parent_id != id)`

    4. **DDL — Indexes:**
       - `ix_notes_parent_id` on `(parent_id)`
       - `ix_notes_parent_position` on `(parent_id, position)`
       - `ix_notes_depth` on `(depth)`
       - `ix_notes_owner_workspace` on `(owner_id, workspace_id)`

    5. **DML — Classify project pages:**
       ```sql
       UPDATE notes SET depth = 0, parent_id = NULL, position = sub.pos
       FROM (SELECT id, ROW_NUMBER() OVER (PARTITION BY project_id ORDER BY created_at) * 1000 AS pos
             FROM notes WHERE project_id IS NOT NULL) sub
       WHERE notes.id = sub.id AND notes.project_id IS NOT NULL
       ```

    6. **DML — Classify personal pages:**
       ```sql
       UPDATE notes SET depth = 0, parent_id = NULL, position = sub.pos
       FROM (SELECT id, ROW_NUMBER() OVER (PARTITION BY owner_id, workspace_id ORDER BY created_at) * 1000 AS pos
             FROM notes WHERE project_id IS NULL) sub
       WHERE notes.id = sub.id AND notes.project_id IS NULL
       ```

    7. **RLS — Atomic policy replacement** (single op.execute with multiple SQL statements):
       - `DROP POLICY IF EXISTS "notes_workspace_member" ON notes;`
       - `CREATE POLICY "notes_project_page_policy" ON notes FOR ALL USING (project_id IS NOT NULL AND workspace_id IN (SELECT wm.workspace_id FROM workspace_members wm WHERE wm.user_id = current_setting('app.current_user_id', true)::uuid AND wm.is_deleted = false)) WITH CHECK (same condition);`
       - `CREATE POLICY "notes_personal_page_policy" ON notes FOR ALL USING (project_id IS NULL AND owner_id = current_setting('app.current_user_id', true)::uuid) WITH CHECK (same condition);`
       - `DROP POLICY IF EXISTS "notes_service_role" ON notes;` then `CREATE POLICY "notes_service_role" ON notes FOR ALL TO service_role USING (true) WITH CHECK (true);`

    **downgrade() function:**
    1. RLS: Drop 3 new policies, recreate `notes_workspace_member` (original from migration 005)
    2. DDL: Drop indexes (all 4), drop FK `fk_notes_parent_id`, drop CHECK constraints, drop columns (parent_id, depth, position)

    **Anti-patterns to avoid (per RESEARCH.md):**
    - Do NOT drop `notes_service_role` without recreating it — the upgrade creates it fresh
    - Do NOT use `op.add_column()` with inline FK for self-referencing table — use `op.create_foreign_key()` separately
    - Do NOT filter `WHERE is_deleted = false` in the bulk UPDATE — assign positions to ALL rows
    - Do NOT split into multiple migration files — single file for atomicity
    - Use `op.execute(text(...))` for CHECK constraints and RLS, not `op.create_check_constraint()`

  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run alembic heads</automated>
  </verify>
  <done>
    - Migration file exists at backend/alembic/versions/079_add_page_tree_columns.py
    - `alembic heads` reports a single head (079)
    - upgrade() has all 7 steps: 3 columns, 1 FK, 2 CHECKs, 4 indexes, 2 bulk UPDATEs, atomic RLS swap
    - downgrade() reverses all changes in correct order
    - No service_role policy gap — created fresh in upgrade, restored original in downgrade
  </done>
</task>

</tasks>

<verification>
cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run alembic heads
cd /Users/tindang/workspaces/tind-repo/pilot-space/backend && uv run alembic check
</verification>

<success_criteria>

- `alembic heads` shows single head (079_add_page_tree_columns)
- Migration file has complete upgrade() with DDL + DML + RLS in correct order
- Migration file has complete downgrade() that reverses all changes
- down_revision correctly references 078's revision ID
  </success_criteria>

<output>
After completion, create `.planning/phases/24-page-tree-data-model/24-02-SUMMARY.md`
</output>
