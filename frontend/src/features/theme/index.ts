// Theme system barrel export
export type { ThemeMode, AccentColor, ThemePreferences, AccentPreset } from './types';
export { DEFAULT_PREFERENCES } from './types';
export { ACCENT_PRESETS, applyAccentColor } from './accent-colors';
