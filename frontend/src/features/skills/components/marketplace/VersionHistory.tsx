'use client';

/**
 * VersionHistory - Version timeline with changelogs.
 * Placeholder for Task 1; fully built in Task 2.
 *
 * Source: Phase 055, P55-03
 */

interface VersionHistoryProps {
  listingId: string;
  workspaceId: string;
}

export function VersionHistory({ listingId, workspaceId }: VersionHistoryProps) {
  void listingId;
  void workspaceId;
  return <div>Version history loading...</div>;
}
