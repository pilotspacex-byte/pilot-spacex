'use client';

/**
 * ReviewSection - Reviews list with star rating submission form.
 * Placeholder for Task 1; fully built in Task 2.
 *
 * Source: Phase 055, P55-03
 */

interface ReviewSectionProps {
  listingId: string;
  workspaceId: string;
}

export function ReviewSection({ listingId, workspaceId }: ReviewSectionProps) {
  void listingId;
  void workspaceId;
  return <div>Reviews loading...</div>;
}
