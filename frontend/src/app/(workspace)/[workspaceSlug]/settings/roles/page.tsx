/**
 * Custom roles settings page — App Router entry point.
 *
 * AUTH-04, AUTH-05: Renders RolesSettingsPage for custom RBAC role management.
 */

import { RolesSettingsPage } from '@/features/settings/pages';

export default function RolesPage() {
  return <RolesSettingsPage />;
}
