# Phase 28: Legacy Code Cleanup & Migration

**Epic/PR**: Claude Agent SDK Migration - Legacy Cleanup
**Task Count**: 65 tasks
**Combined Complexity**: 🟡 130/100 (avg 2/task)
**Type**: CLEANUP
**Blocked by**: T108 (All quality gates pass)

---

## Overview

Remove all legacy AI code after SDK migration is verified complete. This phase ensures clean codebase with no deprecated patterns.

---

## 28.1 Delete Legacy Agent Base Classes (T248-T251)

**Complexity**: 🟢 2/20 each

### T248: Archive base.py

```bash
# Create archive directory
mkdir -p backend/src/pilot_space/ai/_archive

# Archive legacy base
cp backend/src/pilot_space/ai/agents/base.py \
   backend/src/pilot_space/ai/_archive/base.py.bak
```

### T249: Delete base.py

```bash
# Verify no imports remain
grep -r "from pilot_space.ai.agents.base import" backend/src/
grep -r "from pilot_space.ai.agents.base import" backend/tests/
# Should return nothing

# Delete
rm backend/src/pilot_space/ai/agents/base.py
```

### T250: Update Agent Exports

```python
# backend/src/pilot_space/ai/agents/__init__.py
"""AI Agents using Claude Agent SDK."""

from pilot_space.ai.agents.sdk_base import SDKBaseAgent, AgentContext
from pilot_space.ai.agents.ghost_text_agent import GhostTextAgent
from pilot_space.ai.agents.ai_context_agent import AIContextAgent
from pilot_space.ai.agents.pr_review_agent import PRReviewAgent
from pilot_space.ai.agents.issue_extractor_agent import IssueExtractorAgent
from pilot_space.ai.agents.margin_annotation_agent import MarginAnnotationAgent
from pilot_space.ai.agents.conversation_agent import ConversationAgent
# ... other agents

__all__ = [
    "SDKBaseAgent",
    "AgentContext",
    "GhostTextAgent",
    "AIContextAgent",
    # ...
]
```

### T251: Remove BaseAgent Test Imports

```bash
# Update all test imports
find backend/tests/unit/ai/agents -name "*.py" -exec \
  sed -i '' 's/from pilot_space.ai.agents.base import/from pilot_space.ai.agents.sdk_base import/g' {} \;
```

---

## 28.2 Delete Legacy Direct Provider Implementations (T252-T255)

**Complexity**: 🟢 2/20 each

### T252-T253: Archive and Delete Old Ghost Text

```bash
# Archive old Gemini-based implementation
cp backend/src/pilot_space/ai/agents/ghost_text_agent.py \
   backend/src/pilot_space/ai/_archive/ghost_text_agent_gemini.py.bak

# Note: The new SDK-based ghost_text_agent.py should already be in place
```

### T254: Remove google.generativeai Dependency

```toml
# backend/pyproject.toml
# REMOVE:
# "google-generativeai>=0.4.0",
```

### T255: Verify No Gemini Imports

```bash
grep -r "import google.generativeai" backend/src/
grep -r "from google.generativeai" backend/src/
# Should return nothing
```

---

## 28.3 Delete Legacy Orchestrator (T256-T260)

**Complexity**: 🟢 2/20 each

### T256-T257: Archive and Delete orchestrator.py

```bash
cp backend/src/pilot_space/ai/orchestrator.py \
   backend/src/pilot_space/ai/_archive/orchestrator.py.bak

rm backend/src/pilot_space/ai/orchestrator.py
```

### T258: Update All Orchestrator Imports

```bash
# Find and replace all imports
grep -rl "from pilot_space.ai.orchestrator import" backend/src/ | \
  xargs sed -i '' 's/from pilot_space.ai.orchestrator import/from pilot_space.ai.sdk_orchestrator import/g'

# Replace class name if different
grep -rl "AIOrchestrator" backend/src/ | \
  xargs sed -i '' 's/AIOrchestrator/SDKOrchestrator/g'
```

### T259-T260: Update DI Container

```python
# backend/src/pilot_space/container.py
# REMOVE:
# from pilot_space.ai.orchestrator import AIOrchestrator

# UPDATE:
from pilot_space.ai.sdk_orchestrator import SDKOrchestrator

class Container(containers.DeclarativeContainer):
    # REMOVE old orchestrator provider
    # orchestrator = providers.Singleton(AIOrchestrator, ...)

    # KEEP new SDK orchestrator
    sdk_orchestrator = providers.Singleton(
        SDKOrchestrator,
        key_storage=...,
        approval_service=...,
        cost_tracker=...,
        session_manager=...,
        provider_selector=...,
        mcp_server_factory=...,
    )
```

---

## 28.4 Delete Legacy Mock Provider (T261-T264)

**Complexity**: 🟢 2/20 each

### T261-T263: Archive and Delete Mock Provider

```bash
# Archive
cp backend/src/pilot_space/ai/providers/mock.py \
   backend/src/pilot_space/ai/_archive/mock.py.bak

cp -r backend/src/pilot_space/ai/providers/mock_generators \
   backend/src/pilot_space/ai/_archive/mock_generators.bak

# Delete
rm backend/src/pilot_space/ai/providers/mock.py
rm -rf backend/src/pilot_space/ai/providers/mock_generators
```

### T264: Create SDK-Compatible Test Fixtures

```python
# backend/tests/fixtures/ai/mock_sdk_responses.py
"""Mock responses for SDK-based agent tests."""

MOCK_GHOST_TEXT_RESPONSE = {
    "content": "validate user credentials before proceeding",
    "tokens": {"input": 50, "output": 12},
}

MOCK_AI_CONTEXT_RESPONSE = {
    "phases": [...],
    "claude_code_prompt": "...",
    "related_docs": [...],
}

# Fixture for mocking SDK query()
@pytest.fixture
def mock_sdk_query():
    with patch("claude_agent_sdk.query") as mock:
        mock.return_value = MagicMock(content="test response")
        yield mock
```

---

## 28.5 Clean Up Legacy Prompt Templates (T265-T272)

**Complexity**: 🟢 2/20 each

### T265: Archive All Old Prompts

```bash
mkdir -p backend/src/pilot_space/ai/_archive/prompts

cp backend/src/pilot_space/ai/prompts/*.py \
   backend/src/pilot_space/ai/_archive/prompts/
```

### T266-T271: Delete Old Prompt Files

These are replaced by prompts embedded in SDK agent implementations:

```bash
# Delete old string-template prompts (now embedded in agents)
rm backend/src/pilot_space/ai/prompts/ghost_text.py
rm backend/src/pilot_space/ai/prompts/margin_annotation.py
rm backend/src/pilot_space/ai/prompts/issue_extraction.py
rm backend/src/pilot_space/ai/prompts/issue_enhancement.py
rm backend/src/pilot_space/ai/prompts/ai_context.py
rm backend/src/pilot_space/ai/prompts/pr_review.py
```

### T272: Verify Prompts in SDK Format

```bash
# Verify new prompt structure in agents
grep -r "def _get_system_prompt" backend/src/pilot_space/ai/agents/
# Should return all SDK agents with embedded prompts
```

---

## 28.6 Clean Up Legacy Agent Imports (T273-T281)

**Complexity**: 🟢 2/20 each

Remove direct `import anthropic` from all agents (now via SDK):

### T273-T281: Remove Direct Anthropic Imports

```bash
# For each agent file, remove:
# import anthropic
# from anthropic import Anthropic

# Files to update:
# - conversation_agent.py
# - issue_extractor_agent.py
# - margin_annotation_agent.py
# - issue_enhancer_agent.py
# - duplicate_detector_agent.py
# - pr_review_agent.py
# - ai_context_agent.py
# - commit_linker_agent.py
# - assignee_recommender_agent.py

# Verify no direct imports remain:
grep -r "import anthropic" backend/src/pilot_space/ai/agents/
grep -r "from anthropic import" backend/src/pilot_space/ai/agents/
# Should return nothing
```

---

## 28.7 Clean Up Legacy Infrastructure (T282-T286)

**Complexity**: 🟢 2/20 each

### T282: Delete In-Memory RateLimiter

```python
# Remove from old orchestrator (already deleted)
# The SDK handles rate limiting, so no replacement needed
```

### T283-T284: Remove Legacy Routing Dicts

```python
# Delete from any remaining files:
# PROVIDER_ROUTING = {...}
# DEFAULT_MODELS = {...}
# These are now handled by ProviderSelector with DD-011 table
```

### T285: Update AgentContext

```python
# backend/src/pilot_space/ai/context.py
"""Minimal agent context for SDK."""
from dataclasses import dataclass
from uuid import UUID

@dataclass
class AgentContext:
    """Context passed to all SDK agents."""
    workspace_id: UUID
    user_id: UUID
    request_id: str | None = None
    # Removed: provider, model, max_tokens (now in SDK options)
```

### T286: Remove AgentResult Class

```python
# DELETE AgentResult[OutputT] - SDK uses message types
# Remove from context.py or types.py
```

---

## 28.8 Clean Up Legacy Telemetry (T287-T290)

**Complexity**: 🟢 2/20 each

### T287-T290: Update Telemetry

```python
# backend/src/pilot_space/ai/telemetry.py

class AIProvider(str, Enum):
    ANTHROPIC = "anthropic"
    OPENAI = "openai"
    # REMOVE: GEMINI = "gemini"  # No longer supported

TOKEN_COSTS = {
    # Updated pricing for Claude Opus 4.5 and Haiku
    "claude-opus-4-5-20251101": {"input": 15.0, "output": 75.0},
    "claude-sonnet-4-20250514": {"input": 3.0, "output": 15.0},
    "claude-3-5-haiku-20241022": {"input": 0.25, "output": 1.25},
    "text-embedding-3-large": {"input": 0.13, "output": 0.0},
    # REMOVE Gemini entries
}

class AIOperation(str, Enum):
    GHOST_TEXT = "ghost_text"
    AI_CONTEXT = "ai_context"
    PR_REVIEW = "pr_review"
    # ... matches SDK tool names
```

---

## 28.9 Clean Up Legacy Dependencies (T291-T294)

**Complexity**: 🟢 2/20 each

### T291: Remove Unused Dependencies

```toml
# backend/pyproject.toml
# REMOVE:
# "google-generativeai>=0.4.0",

# KEEP:
# "anthropic>=0.30.0",  # Only used via claude-agent-sdk
```

### T292: Verify Anthropic Only Via SDK

```bash
# Check that anthropic is only imported via SDK, not directly
grep -r "from anthropic import" backend/src/pilot_space/
# Should only appear in SDK wrapper if at all
```

### T293: Check Tenacity Usage

```bash
# SDK handles retries, remove tenacity if only used for AI
grep -r "from tenacity import" backend/src/pilot_space/ai/
# Remove if no longer needed
```

### T294: Verify Dependencies

```bash
cd backend
uv pip check
# Should report no broken dependencies
```

---

## 28.10 Clean Up Frontend Legacy (T295-T297)

**Complexity**: 🟢 2/20 each

### T295-T297: Frontend Cleanup

```bash
# T295: Remove direct API calls bypassing stores
grep -r "fetch.*\/ai\/" frontend/src/features/
# Should only be in services/ai-api.ts

# T296: Remove deprecated types
# Check frontend/src/types/ai.ts for old response types

# T297: Verify SSE event types match backend
# Compare frontend SSE handlers with backend SSE emitters
```

---

## 28.11 Clean Up Tests (T298-T301)

**Complexity**: 🟢 2/20 each

### T298-T301: Test Cleanup

```bash
# T298: Delete tests for deleted classes
rm backend/tests/unit/ai/agents/test_base_agent.py
rm backend/tests/unit/ai/test_orchestrator.py

# T299: Update imports
find backend/tests -name "*.py" -exec \
  sed -i '' 's/from pilot_space.ai.agents.base import/from pilot_space.ai.agents.sdk_base import/g' {} \;

# T300: Replace mock provider tests
rm backend/tests/unit/ai/providers/test_mock_provider.py
# Use SDK fixtures instead

# T301: Update integration tests
grep -rl "AIOrchestrator" backend/tests/integration/ | \
  xargs sed -i '' 's/AIOrchestrator/SDKOrchestrator/g'
```

---

## 28.12 Documentation Cleanup (T302-T305)

**Complexity**: 🟢 2/20 each

### T302-T305: Update Documentation

```bash
# T302: Remove outdated AI architecture docs
# Review and update docs/architect/ai-layer.md

# T303: Update CLAUDE.md
# - Replace AIOrchestrator references with SDKOrchestrator
# - Update agent patterns section

# T304: Update ai-layer.md
# - Remove direct provider call patterns
# - Document SDK patterns (query, ClaudeSDKClient)

# T305: Archive old docs
mkdir -p docs/_archive
mv docs/architect/old-ai-*.md docs/_archive/
```

---

## 28.13 Final Verification (T306-T312)

**Complexity**: 🟢 3/20 each

### T306-T310: Grep Verification

```bash
# T306: No Gemini imports
grep -r "google.generativeai" backend/src/
# Should return 0 results

# T307: No old base imports
grep -r "from pilot_space.ai.agents.base import" backend/
# Should return 0 results

# T308: No old orchestrator imports
grep -r "from pilot_space.ai.orchestrator import" backend/
# Should return 0 results

# T309: No MockProvider (outside archive)
grep -r "MockProvider" backend/src/ backend/tests/
# Should return 0 results (archive excluded)

# T310: Archive in gitignore
echo "_archive/" >> backend/src/pilot_space/ai/.gitignore
# Or remove archive before production
```

### T311-T312: Full Test Suite

```bash
# T311: Run full test suite
cd backend
uv run pytest --cov=pilot_space.ai -v
# All tests must pass, coverage >80%

# T312: Run quality gates
uv run ruff check .
uv run pyright
# No errors
```

---

## Definition of Done

- [ ] All legacy base classes archived and deleted
- [ ] All direct provider imports removed
- [ ] Legacy orchestrator replaced with SDKOrchestrator
- [ ] Mock provider deleted, SDK fixtures created
- [ ] Old prompt templates archived
- [ ] Agent imports cleaned up
- [ ] Infrastructure classes updated
- [ ] Telemetry updated for SDK
- [ ] Dependencies cleaned up
- [ ] Tests updated
- [ ] Documentation updated
- [ ] All grep verifications pass (0 legacy patterns)
- [ ] Full test suite passes with >80% coverage
- [ ] Quality gates pass
